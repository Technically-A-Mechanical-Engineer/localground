# v3.1.0 Hardening-and-Hygiene — Release Arc COORDINATOR State Snapshot

**Date:** 2026-06-30
**Branch:** master (branching_strategy=none)
**Context threshold reached at:** 54% (sub-Tier-1 by %; invoked at a clean break ahead of a heavy, context-hungry release arc to avoid being forced to compact mid-milestone-close)
**Strategic natural break:** Phase 23 fully closed — COMPREHENSION AFFIRMED + DeepWiki badge quick task done; tree clean.

## §1 Current arc state
- Workflow position: Phase 23 (Decoder Trailing-Edge Fix, CORE-16) FULLY CLOSED — executed + verified (6/6) + secured (5/5, threats_open:0) + comprehension AFFIRMED. **v3.1.0 milestone fully implemented** (all 5 reqs: SEC-01, CLI-06, BUILD-01, CORE-15, CORE-16). The npm-release arc has NOT started.
- Branch state: origin/master = `c71b494`; **~63 local/unpushed commits; tip `d29c9b2`**. First push will fire the Phase-21 `pinact` CI verify-pins gate (never run yet).
- Tree: clean except untracked `AGENTS.md` (pre-existing, NOT ours — leave it).

## §2 Locked decisions / state
- **DECISION (user, this session): the npm release will MATCH the GSD milestone → publish packages as 3.1.0** (currently 3.0.2).
- Phase 23 artifacts: `23-VERIFICATION.md` (passed 6/6), `23-REVIEW.md` (0 crit / 3 warn / 1 info), `23-SECURITY.md` (5/5, threats_open:0), `23-COMPREHENSION.md` (status: AFFIRMED).
- Backlog: **999.9** (WR-01 lossy-encode sibling-collision, advisory, also accepted-risk AR-23-04) + **999.8** (`Foo&&`) on ROADMAP + PROJECT mirror; **999.5 → CLI-05 → v3.2.0**.
- release.yml (Phase 20): tag-triggered OIDC publish + provenance; preflight asserts `tag == mcp version == cli version`; Node 22.x; dry-run-both gate. **OIDC = no local npm login needed.**

## §3 EXECUTOR paste-block
N/A — solo orchestration via GSD subagents (Task tool), no user-relay paste-block.

## §4 REVIEWER state + invocation calibration
N/A for the release arc. Phase-23 review chain already closed: code-reviewer (advisory), gsd-verifier (passed), gsd-security-auditor (SECURED). Plan-time cross-model stress test (Codex gpt-5.5) already done; not needed for release.

## §5 Carry-forward calibration (recurring error patterns / lessons)
- **phase.complete does NOT flip the ROADMAP top-level phase checkbox** — manually flip `[ ]`→`[x]` (recurring; fixed this session in `4ecbe9d`). [[feedback_phase_completion]]
- **`.planning/` is gitignored-but-tracked** → orchestrator commits need `git add -f`; `gsd-sdk query commit` FAILS on `.planning/` paths. (`git check-ignore` misleadingly reports "not ignored" for already-tracked files — don't trust it.) [[feedback_planning_gitignore]]
- **dist/ staleness trap:** tests resolve `@localground/core` to `./dist/`; run `npm test` from REPO ROOT (pretest rebuilds), NOT `npx vitest` inside packages/core.
- **use_worktrees=false (Windows)** → executors run sequential on main tree, no isolation.

## §6 Resumption flow + forward work

### Resumption checklist (post-compaction) — 6 recovery dimensions
1. **commit SHAs:** origin/master=`c71b494`; local tip=`d29c9b2`. Phase-23 chain: `0742622`(RED) `66f6331`(GREEN) `91b4867`(matrix) `1c2a69a`(REVIEW) `35f30a5`(complete) `4ecbe9d`(PROJECT+checkbox) `b15b8b5`(SECURITY) `2c7ac41`(COMPREHENSION) `ce73157`(badge) `d29c9b2`(badge-tracking).
2. **REVIEWER verdicts:** code-review issues_found (WR-01 advisory→999.9, no blockers); verifier passed 6/6; security SECURED 5/5.
3. **drift discoveries + corrections:** ROADMAP checkbox cascade gap (corrected `4ecbe9d`); .planning gitignore→`git add -f`; dist staleness (run from root).
4. **task tracker mid-state:** Phase 23 DONE. Release arc NOT started — Stage 1 pending.
5. **files to re-read on resume:** `~/.claude/projects/C--Users-rlasalle-Projects-localground/memory/project_next_step.md`; `.planning/ROADMAP.md`; `.planning/REQUIREMENTS.md` (CORE-16); root + per-package `package.json` (for the bump); `$HOME/.claude/get-shit-done/workflows/complete-milestone.md`; `.github/workflows/release.yml`.
6. **next-action sequence:** Stage 1 → Stage 2 → Stage 3 (below).

### Forward work map — the v3.1.0 release (3 stages)
- **Stage 1 — validate first:** push `master` → first-ever `pinact` CI gate run → confirm green. (The one real unknown; de-risk before bumping. NEEDS user go-ahead — first public push.)
- **Stage 2 — bump 3.0.2→3.1.0:** via `/gsd-quick` — version across all package manifests + lockfile regen (mirror Phase 20-04 `4818cfb`). D-10: bump post-CI-green, pre-tag. Push → confirm CI green. (Confirm exact manifest file set by reading before editing.)
- **Stage 3 — milestone-close + publish:** `/gsd-complete-milestone`; pre-close `audit-open` (Acknowledge known deferred items); let its git_tag step create+push `v3.1.0` → release.yml publishes 3.1.0 (OIDC + provenance). NEEDS user go-ahead at the tag-push (irreversible publish trigger). milestone-close also: preserves ROADMAP Backlog (extract+re-append), `git rm REQUIREMENTS.md`, writes RETROSPECTIVE.md.

### Compaction trigger guidance for future-coord-self
- Next natural break: after Stage 1 CI verdict, or after Stage 2 bump commit — re-snapshot if context climbs past ~65% before Stage 3 (milestone-close is the heaviest step).

### Open optional (post-release, needs npm login)
- `npm deprecate @localground/*@3.0.1`.
