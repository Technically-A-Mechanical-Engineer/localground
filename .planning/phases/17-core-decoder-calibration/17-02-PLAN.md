---
phase: 17-core-decoder-calibration
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md
  - .planning/PROJECT.md
  - .planning/milestones/v3.0.0-ROADMAP.md
autonomous: true
requirements:
  - CORE-14
must_haves:
  objective: >-
    Close CORE-14 by documenting the diagnostic resolution of every
    `no_candidates` from the original 23-path-hash sample (17 round-trip, 6
    documented as deleted source folders) in 17-VERIFICATION.md, and by
    annotating WR-01 as closed in PROJECT.md "Key Decisions" with a forward
    pointer added to the v3.0.0 milestone backlog provenance entry.
  truths:
    - "D-03: stale-entry classification deferred to backlog — Phase 17 closes CORE-14 by DOCUMENTING the diagnostic, not by adding an `undecodable` reason or surfacing path. Consumer-side audit/cleanup-scan UX changes captured as new 999.x backlog item, not implemented in Phase 17"
    - "D-04: Phase 17 file scope is narrow — only decode.ts, decode.test.ts, 17-VERIFICATION.md, and the PROJECT.md Key Decisions row append (plus the v3.0.0 milestone WR-01 forward-pointer line). No CLI/MCP/audit/cleanup-scan consumer touches"
    - "D-08: 17-VERIFICATION.md lists ALL 6 deleted-source path-hash entries individually with decoded targets and 'deleted source folder' notation, satisfying CORE-14's 'every no_candidates either resolves or has a documented reason' criterion without code-level classification"
    - "D-09: WR-01 marked closed in PROJECT.md Key Decisions table — new row added with back-pointer to 17-VERIFICATION.md; forward-pointer line appended at v3.0.0 milestone backlog provenance entry (line 144 of .planning/milestones/v3.0.0-ROADMAP.md, where 999.6 was promoted)"
  artifacts:
    - path: .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md
      provides: "Phase 17 verification report with CORE-14 closure trail (diagnostic + 6-entry deleted-source table)"
      contains: "## Path-Hash Diagnostic — CORE-14 Closure"
    - path: .planning/PROJECT.md
      provides: "Key Decisions row marking WR-01 closed with link to 17-VERIFICATION.md"
      contains: "WR-01"
    - path: .planning/milestones/v3.0.0-ROADMAP.md
      provides: "Forward pointer at 999.6/WR-01 backlog provenance entry to Phase 17"
      contains: "Resolved by Phase 17"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions table (new row)"
      to: ".planning/phases/17-core-decoder-calibration/17-VERIFICATION.md"
      via: "Markdown link in row's Outcome cell"
      pattern: "17-VERIFICATION\\.md"
    - from: ".planning/milestones/v3.0.0-ROADMAP.md line ~144 (999.6 entry)"
      to: ".planning/phases/17-core-decoder-calibration/17-VERIFICATION.md"
      via: "Forward-pointer annotation `Resolved by Phase 17`"
      pattern: "Resolved by Phase 17"
    - from: "17-VERIFICATION.md Path-Hash Diagnostic table"
      to: "Original 23-path-hash sample resolution"
      via: "Six rows listing deleted-source path-hashes from project_diagnostic_23_paths.md"
      pattern: "Deleted source folder"
---

<objective>
Author the Phase 17 verification report and close the WR-01 thread in project-level documentation. The verification report documents the 2026-04-27 diagnostic resolution of all 23 path-hashes in the original sample (17 round-trip, 6 documented as deleted source folders) — this is the closure half of CORE-14. PROJECT.md gets a Key Decisions row marking WR-01 closed; the v3.0.0 milestone roadmap gets a forward-pointer at the 999.6 backlog provenance entry. Pure documentation phase; no code touched.

Purpose: Without the 6-entry deleted-source table, CORE-14's "every `no_candidates` either resolves or has a documented reason" criterion has no concrete trail. With it, the closure is auditable: 17/17 currently-extant decode, 6/6 documented as deleted, new tests cover the seven CORE-13 classes — every category accounted for. The PROJECT.md row and forward-pointer make WR-01 traceable from project-level docs back to the verification evidence.

Output: 17-VERIFICATION.md (new file, ~80-120 lines following 16-VERIFICATION.md template), 1 new row in PROJECT.md Key Decisions table, 1 new annotation line at v3.0.0-ROADMAP.md:144.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-core-decoder-calibration/17-CONTEXT.md
@.planning/phases/17-core-decoder-calibration/17-PATTERNS.md
@.planning/phases/17-core-decoder-calibration/17-01-PLAN.md
@.planning/phases/16-test-infrastructure-hardening/16-VERIFICATION.md
@.planning/milestones/v3.0.0-ROADMAP.md

<source_of_truth>
The 6 deleted-source path-hash entries are pinned in the user's MEMORY.md. Read this file ONCE at the start of the verification-doc task — it contains the exact hashes, decoded targets, and verification status. DO NOT re-run the diagnostic probe.

Absolute path: `C:\Users\rlasalle\.claude\projects\C--Users-rlasalle-Projects-localground\memory\project_diagnostic_23_paths.md`

The "The 6 failures (all deleted on disk)" section in that file lists the exact entries to copy into the 17-VERIFICATION.md table.
</source_of_truth>

<file_path_clarification>
17-CONTEXT.md and 17-PATTERNS.md reference "14-REVIEW.md WR-01 entry" as the forward-pointer target. **There is no `14-REVIEW.md` file in the repo** — the WR-01 (regex calibration) provenance is the v3.0.0 milestone roadmap's backlog promotion entry at `.planning/milestones/v3.0.0-ROADMAP.md` line 144 (the `**999.6** — encode() regex calibration (WR-01)` bullet). That is the correct forward-pointer target.

Note: `.planning/phases/16-test-infrastructure-hardening/16-REVIEW.md` contains a *different* WR-01 (the Phase 16 `fs.rm` ordering finding, already resolved in commit `0136116` and documented in `16-REVIEW-FIX.md`). Do NOT touch the Phase 16 WR-01 — it's a separate thread.
</file_path_clarification>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 17-VERIFICATION.md including the CORE-14 closure trail</name>
  <files>.planning/phases/17-core-decoder-calibration/17-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-test-infrastructure-hardening/16-VERIFICATION.md (template — copy frontmatter shape, section structure, table formats)
    - C:\Users\rlasalle\.claude\projects\C--Users-rlasalle-Projects-localground\memory\project_diagnostic_23_paths.md (the 6 deleted-source entries — pinned source of truth, do NOT re-run the probe)
    - .planning/phases/17-core-decoder-calibration/17-CONTEXT.md (D-08 verification doc shape; `<domain>` diagnostic anchor for the 23-path-hash sample)
    - .planning/phases/17-core-decoder-calibration/17-PATTERNS.md (lines 162-228 — 17-VERIFICATION template structure and the new "Path-Hash Diagnostic — CORE-14 Closure" section format)
    - .planning/phases/17-core-decoder-calibration/17-01-SUMMARY.md (will exist after Wave 1 — pulls in the actual `npm test` result counts and confirms regex change shipped)
    - .planning/ROADMAP.md (Phase 17 success criteria 1-4, used to populate the Observable Truths table)
  </read_first>
  <action>
    Create a new file at `.planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` following the 16-VERIFICATION.md template structure.

    **Frontmatter** (copy shape from 16-VERIFICATION.md lines 1-8, substitute fields):

    ```yaml
    ---
    phase: 17-core-decoder-calibration
    verified: <ISO-8601 timestamp at the moment the executor runs verification, e.g., 2026-04-28T12:00:00Z>
    status: passed
    score: 4/4 must-haves verified
    overrides_applied: 0
    requirements_verified: 2/2
    ---
    ```

    **Header** (copy 16-VERIFICATION.md lines 10-16 shape):

    ```markdown
    # Phase 17: Core Decoder Calibration Verification Report

    **Phase Goal:** Path-hash decoding round-trips correctly for the full set of special characters Claude Code actually encodes, eliminating silent `no_candidates` failures.

    **Verified:** <same ISO-8601 as frontmatter>
    **Status:** passed
    **Re-verification:** No — initial verification
    ```

    **Required sections** (in this order — mirror 16-VERIFICATION.md):

    1. `## Goal Achievement`

       1a. `### Observable Truths (Roadmap Success Criteria)` — table with one row per ROADMAP.md Phase 17 Success Criterion (4 rows: SC1 round-trip, SC2 23-path-hash sample resolved, SC3 unit tests pass cross-platform, SC4 WR-01 closed). Columns: `#`, `Truth`, `Status`, `Evidence`. Status uses `✓ VERIFIED` checkmarks. Evidence cites:
       - SC1: `grep -F '/[\\\\/: ,().'\''&\[\]+=%]/g' packages/core/src/environment/decode.ts` returns 1 match; 6 new round-trip tests in decode.test.ts (per Wave 1 plan 17-01)
       - SC2: This document's "Path-Hash Diagnostic — CORE-14 Closure" section (see 1c below)
       - SC3: `npm test` exits 0 with ≥85 passed / 2 skipped (cite the actual numbers from 17-01-SUMMARY.md); Phase 20 / PIPE-01 will validate cross-OS execution on master push
       - SC4: WR-01 row added to PROJECT.md Key Decisions table (this plan's Task 2); forward-pointer at v3.0.0-ROADMAP.md:144

       1b. `### Required Artifacts` — table listing the artifacts changed by Phase 17. Columns: `Artifact`, `Expected`, `Status`, `Details`. One row each for: `decode.ts` (regex widened), `decode.test.ts` (6 new tests), `17-VERIFICATION.md` (this file, self-referential), PROJECT.md row.

       1c. `### Path-Hash Diagnostic — CORE-14 Closure` — **THIS IS THE D-08 SECTION**. Copy this exact opening paragraph:

       ```markdown
       The 23-path-hash sample probed during the 2026-04-27 diagnostic (per 17-CONTEXT.md `<domain>`)
       resolved as follows: 17/23 decoded successfully against existing folders. The remaining 6
       returned `no_candidates` because the source folder no longer exists on disk. Each is
       documented below, satisfying CORE-14's "every `no_candidates` either resolves or has a
       documented reason" criterion.
       ```

       Then a markdown table with EXACTLY 6 rows (one per failure from the diagnostic file). Columns: `#`, `Path-hash directory name`, `Decoded target (best inference)`, `Status`. Pull entries verbatim from the "The 6 failures" section of `C:\Users\rlasalle\.claude\projects\C--Users-rlasalle-Projects-localground\memory\project_diagnostic_23_paths.md`:

       | # | Path-hash directory name | Decoded target (best inference) | Status |
       |---|---------------------------|----------------------------------|--------|
       | 1 | `C--Users-rlasalle-Documents-Claude-Projects` | `C:\Users\rlasalle\Documents\Claude Projects` | Deleted source folder — verified absent on disk |
       | 2 | `C--Users-rlasalle-OneDrive---ThermoTek--Inc-Desktop-ATP-NTS-Test-DATA` | `C:\Users\rlasalle\OneDrive - ThermoTek, Inc\Desktop\ATP-NTS-Test-DATA` | Deleted source folder — verified absent on disk |
       | 3 | `C--Users-rlasalle-OneDrive---ThermoTek--Inc-Desktop-Cowork-Test1` | `C:\Users\rlasalle\OneDrive - ThermoTek, Inc\Desktop\Cowork-Test1` | Deleted source folder — verified absent on disk |
       | 4 | `C--Users-rlasalle-OneDrive---ThermoTek--Inc-Documents-Projects-Training-With-Amit-2026-03-29` | `C:\Users\rlasalle\OneDrive - ThermoTek, Inc\Documents\Projects\Training With Amit 2026-03-29` | Deleted source folder — verified absent on disk |
       | 5 | `C--Users-rlasalle-OneDrive---ThermoTek--Inc-General-Current-Hotness-0246-2026-Management-Review-RL-Claude-Sandbox` | `C:\Users\rlasalle\OneDrive - ThermoTek, Inc\General\Current Hotness\0246-2026-Management-Review-RL-Claude-Sandbox` | Deleted source folder — verified absent on disk |
       | 6 | `C--Users-rlasalle-Projects-claude-code-cloud-sync-migration` | `C:\Users\rlasalle\Projects\claude-code-cloud-sync-migration` | Deleted source folder — renamed to `localground` 2026-04-12 |

       Close the section with this exact closing paragraph:

       ```markdown
       **Closure:** No regex undercoverage in the active environment. The Phase 17 regex widening
       is defensive/forward-looking — preventing silent failure on FUTURE folders containing the
       seven CORE-13 classes — not a fix for an active environment defect.
       ```

    2. `### Key Link Verification` — table wiring the regex change to the test coverage and the docs. Mirror 16-VERIFICATION.md columns: `From`, `To`, `Via`, `Status`, `Details`. At minimum:
       - decode.test.ts new tests → decode.ts encode()/decode() → encode(subDir) → decode(hash) round-trip
       - 17-VERIFICATION.md (this file) → PROJECT.md Key Decisions row → markdown link
       - v3.0.0-ROADMAP.md line 144 → 17-VERIFICATION.md → forward-pointer "Resolved by Phase 17"

    3. `### Behavioral Spot-Checks` — table with `Behavior`, `Command`, `Result`, `Status` columns. At minimum:
       - Widened regex form check: `grep -F '/[\\\\/: ,().'\''&\[\]+=%]/g' packages/core/src/environment/decode.ts` → 1 match
       - Old regex absent: `grep -F '/[\\\\/: ,().]/g' packages/core/src/environment/decode.ts` → 0 matches
       - Test count: `grep -c "round-trips encode/decode for a folder name containing" packages/core/test/environment/decode.test.ts` → 6
       - Strict gate: `npm run build:check` → exit 0
       - Test suite: `npm test` → exit 0, ≥85 passed, 2 skipped (cite Wave 1 actuals)
       - TEST-04 invariant preserved: `grep -E "expect\(typeof.*success\)\.toBe\(['\"]boolean['\"]\)" packages/core/test/environment/decode.test.ts` → 0 matches

    4. `### Requirements Coverage` — table with one row per requirement. Columns: `Requirement`, `Source Plan`, `Description`, `Status`, `Evidence`.
       - CORE-13 → 17-01 → "encode() regex calibrated against the seven char classes" → ✓ SATISFIED → cite the regex grep + 6 round-trip tests
       - CORE-14 → 17-02 → "WR-01 closed; every no_candidates resolved or documented" → ✓ SATISFIED → cite the 6-entry table in this file + PROJECT.md row + v3.0.0-ROADMAP.md forward-pointer

    5. `### Anti-Patterns Found` — likely "None." Phase 17 is a calibration phase touching one regex line and adding 6 isomorphic tests; no new anti-pattern surface.

    6. `### Human Verification Required` — None. All criteria are shell-verifiable.

    7. `### Gaps Summary` — "No gaps. CORE-13 + CORE-14 satisfied. WR-01 closed." Cross-reference Phase 18 (packaging), Phase 19 (UAT), Phase 20 (release) as the v3.0.1 work that lands after this phase.

    **Footer** (mirror 16-VERIFICATION.md lines 117-121):
    ```markdown
    ---

    *Verified: <ISO-8601 timestamp>*
    *Verifier: Claude (gsd-verifier)*
    ```

    Do NOT:
    - Re-run the diagnostic probe (the result is preserved in `project_diagnostic_23_paths.md` per the file's "How to apply" section)
    - Add a 7th, 8th, or 24th row to the Path-Hash Diagnostic table — exactly 6 deleted-source entries (the 17 successful are summarized as a count, not enumerated)
    - Reference a `14-REVIEW.md` file (it doesn't exist — see `<file_path_clarification>` in this plan's `<context>`)
    - Touch any code files or run any test commands beyond the verification grep checks
  </action>
  <verify>
    <automated>test -f .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md && grep -c "Deleted source folder" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` exists
    - File contains the literal heading `## Goal Achievement` (verified via `grep -c "^## Goal Achievement$" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returning `1`)
    - File contains the literal heading `### Path-Hash Diagnostic — CORE-14 Closure` (verified via `grep -c "Path-Hash Diagnostic — CORE-14 Closure" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returning `1`)
    - File lists exactly 6 entries with "Deleted source folder" status (verified via `grep -c "Deleted source folder" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returning `6`)
    - Each of the 6 specific path-hashes from the diagnostic file appears verbatim:
      - `grep -c "C--Users-rlasalle-Documents-Claude-Projects" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returns ≥1
      - `grep -c "C--Users-rlasalle-OneDrive---ThermoTek--Inc-Desktop-ATP-NTS-Test-DATA" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returns ≥1
      - `grep -c "C--Users-rlasalle-OneDrive---ThermoTek--Inc-Desktop-Cowork-Test1" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returns ≥1
      - `grep -c "C--Users-rlasalle-OneDrive---ThermoTek--Inc-Documents-Projects-Training-With-Amit-2026-03-29" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returns ≥1
      - `grep -c "C--Users-rlasalle-OneDrive---ThermoTek--Inc-General-Current-Hotness-0246-2026-Management-Review-RL-Claude-Sandbox" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returns ≥1
      - `grep -c "C--Users-rlasalle-Projects-claude-code-cloud-sync-migration" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returns ≥1
    - Frontmatter contains `phase: 17-core-decoder-calibration`, `status: passed`, `requirements_verified: 2/2`
    - File contains `### Requirements Coverage` table with both `CORE-13` and `CORE-14` cited (verified via `grep -c "CORE-13" file` ≥1 and `grep -c "CORE-14" file` ≥1)
    - File does NOT reference a non-existent `14-REVIEW.md` (verified via `grep -c "14-REVIEW" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` returning `0`)
  </acceptance_criteria>
  <done>
    17-VERIFICATION.md exists with the full 16-VERIFICATION-shaped structure, the D-08 6-entry Path-Hash Diagnostic table, and CORE-13/CORE-14 cited under Requirements Coverage. CORE-14's "every no_candidates either resolves or has a documented reason" criterion has its concrete trail.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append WR-01 closure row to PROJECT.md and forward-pointer to v3.0.0-ROADMAP.md</name>
  <files>.planning/PROJECT.md, .planning/milestones/v3.0.0-ROADMAP.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 138-159 — Key Decisions table; new row goes at the end)
    - .planning/milestones/v3.0.0-ROADMAP.md (line 144 — `**999.6** — encode() regex calibration (WR-01)`; forward-pointer goes immediately after)
    - .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md (must exist from Task 1 — the back-pointer target)
    - .planning/phases/17-core-decoder-calibration/17-CONTEXT.md (D-09 outcome wording)
  </read_first>
  <action>
    **Sub-step A: Append a row to the PROJECT.md Key Decisions table.**

    Open `.planning/PROJECT.md`. Locate the Key Decisions table (header at line 138, schema `| Decision | Rationale | Outcome |`). The last existing row ends with `| Path-shape-only ...looksLikeProject` predicate ...` at line 159. Append exactly ONE new row immediately after that last data row (and before the blank line + `## Evolution` heading).

    Use this EXACT row format (preserving the table's `|` column delimiters):

    ```markdown
    | WR-01 (encode regex calibration) closed via Phase 17 — regex widened to cover seven CORE-13 char classes (apostrophe, ampersand, brackets, plus, equals, percent) | Defensive/forward-looking widening; 17/17 currently-extant path-hashes already round-trip and 6/6 `no_candidates` failures are documented as deleted source folders. Targeted, not catch-all (D-01). | ✓ Validated in v3.0.1 — see [`.planning/phases/17-core-decoder-calibration/17-VERIFICATION.md`](phases/17-core-decoder-calibration/17-VERIFICATION.md) |
    ```

    Then update the footer line at the bottom of PROJECT.md (currently `*Last updated: 2026-04-27 after Phase 16 ...*`). Replace it with:

    ```markdown
    *Last updated: <today's date in YYYY-MM-DD format> after Phase 17 (Core Decoder Calibration) complete — CORE-13 and CORE-14 satisfied; WR-01 closed and traced to 17-VERIFICATION.md; remaining v3.0.1 work covers Phases 18-20 (packaging polish, skill UAT, release pipeline validation).*
    ```

    Do NOT edit any other line in PROJECT.md.

    **Sub-step B: Append a forward-pointer line to v3.0.0-ROADMAP.md.**

    Open `.planning/milestones/v3.0.0-ROADMAP.md`. Locate line 144:

    ```markdown
    - **999.6** — encode() regex calibration (WR-01)
    ```

    Replace that line with:

    ```markdown
    - **999.6** — encode() regex calibration (WR-01) — **Resolved by Phase 17** — see [`.planning/phases/17-core-decoder-calibration/17-VERIFICATION.md`](../phases/17-core-decoder-calibration/17-VERIFICATION.md)
    ```

    The `..` in the relative path is correct because v3.0.0-ROADMAP.md is at `.planning/milestones/v3.0.0-ROADMAP.md` and the verification report is at `.planning/phases/17-core-decoder-calibration/17-VERIFICATION.md`.

    Do NOT edit any other line in v3.0.0-ROADMAP.md.

    **Note:** `git add -f` is required for any commit because `.planning/` is gitignored but tracked (per project memory `feedback_planning_gitignore.md`). The execute-plan workflow handles commits; the plan author flags this for the executor only.
  </action>
  <verify>
    <automated>grep -c "WR-01 (encode regex calibration) closed via Phase 17" .planning/PROJECT.md && grep -c "Resolved by Phase 17" .planning/milestones/v3.0.0-ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains exactly 1 occurrence of the literal string `WR-01 (encode regex calibration) closed via Phase 17` (verified via `grep -c "WR-01 (encode regex calibration) closed via Phase 17" .planning/PROJECT.md` returning `1`)
    - PROJECT.md row links to 17-VERIFICATION.md (verified via `grep -c "phases/17-core-decoder-calibration/17-VERIFICATION.md" .planning/PROJECT.md` returning ≥1)
    - PROJECT.md footer cites Phase 17 (verified via `grep -c "after Phase 17" .planning/PROJECT.md` returning `1`)
    - PROJECT.md row count grows by exactly 1 (verified via `grep -c "^| " .planning/PROJECT.md` returning `20` — was `19` before this plan, comprising 1 header row + 18 pre-existing data rows + 1 new WR-01 row)
    - `.planning/milestones/v3.0.0-ROADMAP.md` line ~144 contains `Resolved by Phase 17` (verified via `grep -c "Resolved by Phase 17" .planning/milestones/v3.0.0-ROADMAP.md` returning `1`)
    - The forward-pointer is on the same line as `**999.6**` (verified via `grep "999.6" .planning/milestones/v3.0.0-ROADMAP.md | grep -c "Resolved by Phase 17"` returning `1`)
    - Forward-pointer link target resolves to a real file (verified via `test -f .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` exit 0)
    - No other lines in PROJECT.md or v3.0.0-ROADMAP.md changed (verified by reading the files and confirming only the Key Decisions table append + footer update + line 144 replacement)
  </acceptance_criteria>
  <done>
    PROJECT.md has one new Key Decisions row marking WR-01 closed and linking to 17-VERIFICATION.md; v3.0.0-ROADMAP.md line ~144 has the "Resolved by Phase 17" forward-pointer; footer cites Phase 17.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files → repo readers | All edits are markdown files inside `.planning/` (project documentation, not runtime). No code surface area. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Information Disclosure | 17-VERIFICATION.md path-hash table contains user folder paths | accept | Paths are local user folder names already disclosed across `.planning/` history (e.g., 17-CONTEXT.md `<domain>` references the same diagnostic). The deleted-source paths are a snapshot of the user's own machine state used as a verification trail; no third-party PII, credentials, or attacker-relevant data. The repo is public per project memory `project_repo.md`, and this exposure level matches existing project documentation. |
| T-17-06 | Tampering | Forward-pointer link in v3.0.0-ROADMAP.md could rot if 17-VERIFICATION.md is renamed | mitigate | Acceptance criterion verifies `test -f` resolves the link target at the time of plan execution. Future renames are a maintenance concern, not a Phase 17 verification gap. |
| T-17-07 | Repudiation | PROJECT.md row could be edited later to misrepresent WR-01 closure | accept | Git history preserves all edits. The verification trail is in 17-VERIFICATION.md (this file) + the diagnostic in `project_diagnostic_23_paths.md` MEMORY.md (signed by the originSessionId field) + the regex change in commit history. Three-source corroboration. |
</threat_model>

<verification>
1. **17-VERIFICATION.md exists:** `test -f .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` → exit 0
2. **6-entry table present:** `grep -c "Deleted source folder" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` → exactly 6
3. **CORE-14 closure heading:** `grep -c "Path-Hash Diagnostic — CORE-14 Closure" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` → exactly 1
4. **Both REQ IDs cited:** `grep -c "CORE-13" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` ≥1 AND `grep -c "CORE-14" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` ≥1
5. **PROJECT.md WR-01 row:** `grep -c "WR-01 (encode regex calibration) closed via Phase 17" .planning/PROJECT.md` → exactly 1
6. **PROJECT.md back-pointer:** `grep -c "phases/17-core-decoder-calibration/17-VERIFICATION.md" .planning/PROJECT.md` ≥1
7. **v3.0.0-ROADMAP.md forward-pointer:** `grep -c "Resolved by Phase 17" .planning/milestones/v3.0.0-ROADMAP.md` → exactly 1
8. **No phantom 14-REVIEW.md reference:** `grep -c "14-REVIEW" .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md` → exactly 0
</verification>

<success_criteria>
- 17-VERIFICATION.md exists with the full 16-VERIFICATION-shaped structure
- 17-VERIFICATION.md contains the `### Path-Hash Diagnostic — CORE-14 Closure` section with exactly 6 deleted-source path-hash entries listed verbatim from `project_diagnostic_23_paths.md`
- 17-VERIFICATION.md `### Requirements Coverage` table cites both CORE-13 and CORE-14 with `✓ SATISFIED` status
- PROJECT.md `## Key Decisions` table has one new row marking WR-01 closed with a markdown link to 17-VERIFICATION.md
- PROJECT.md footer cites Phase 17 completion
- v3.0.0-ROADMAP.md line ~144 (the `**999.6**` entry) contains the `Resolved by Phase 17` annotation with a relative link to 17-VERIFICATION.md
- No code files touched; no test commands invoked beyond verification greps
- CORE-14 satisfied (every `no_candidates` from the original 23-path-hash sample either resolved (17 successes) or documented (6 deleted-source entries))
- WR-01 traceable from PROJECT.md → 17-VERIFICATION.md and from v3.0.0-ROADMAP.md backlog provenance → 17-VERIFICATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/17-core-decoder-calibration/17-02-SUMMARY.md` per the GSD summary template. Include: the exact line numbers of the PROJECT.md row append and v3.0.0-ROADMAP.md line replacement, the 6-entry table row count from 17-VERIFICATION.md, and a confirmation that the forward-pointer relative path resolves to a real file.

After this plan completes, Phase 17 is fully verified — CORE-13 (Wave 1) + CORE-14 (Wave 2) both satisfied, WR-01 closed, ready for `/gsd-transition` to Phase 18.
</output>
