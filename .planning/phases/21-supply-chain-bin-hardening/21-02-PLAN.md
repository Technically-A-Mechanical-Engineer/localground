---
phase: 21-supply-chain-bin-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp/src/index.ts
  - packages/mcp/test/smoke.test.ts
autonomous: true
requirements: [CLI-06]
must_haves:
  truths:
    - "D-12: The mcp bin recognizes --version, --version=..., and the -v / -V aliases as version requests; on match it writes ${SERVER_VERSION}\\n to stdout and process.exit(0) BEFORE new StdioServerTransport() (a stdio transport hangs forever waiting for a JSON-RPC client otherwise)"
    - "D-13: Long-form flags are case-sensitive — --Version and --VERSION are intentionally NOT version requests and fall through to normal server startup (defined, testable behavior, not an oversight)"
    - "D-14: NO argument-parser dependency is added (no commander/yargs) — a hand-rolled predicate only; the exact-string contract scripts/verify-tarball.mjs depends on (versionResult.stdout.trim() === expectedVersion AND exit code 0) is preserved for the bare --version case"
  artifacts:
    - path: "packages/mcp/src/index.ts"
      provides: "Robust pre-transport --version predicate inside main()"
      contains: "process.exit(0)"
    - path: "packages/mcp/test/smoke.test.ts"
      provides: "Tests asserting -v/-V/--version= print version+exit0 and --Version falls through to server startup"
      contains: "--Version"
  key_links:
    - from: "packages/mcp/src/index.ts"
      to: "stdout + process.exit(0)"
      via: "version-request predicate before StdioServerTransport"
      pattern: "process\\.exit\\(0\\)"
    - from: "scripts/verify-tarball.mjs"
      to: "packages/mcp/src/index.ts bin --version"
      via: "spawn bin with --version, assert stdout.trim() === version + exit 0"
      pattern: "--version"
---

<objective>
Replace the brittle one-flag check `process.argv.includes('--version')` in the mcp bin with a robust hand-rolled version-request predicate that recognizes `--version`, `--version=…`, and the `-v`/`-V` aliases, prints `${SERVER_VERSION}\n` to stdout, and `process.exit(0)`s BEFORE the stdio transport boots. `--Version`/`--VERSION` are case-sensitively NOT version requests and fall through to normal startup. No argument-parser dependency is added.

Purpose: CLI-06. A stdio transport blocks forever waiting for a JSON-RPC client, so a missed version flag would hang the bin instead of printing a version. The widening must stay additive — the bare `--version` path (exact stdout string + exit 0) is the contract `scripts/verify-tarball.mjs` exercises as a CI gate and must not break.
Output: A robust pre-transport predicate in `packages/mcp/src/index.ts` plus smoke tests locking both the match and the fall-through behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md
@.planning/phases/21-supply-chain-bin-hardening/21-PATTERNS.md

<interfaces>
<!-- Exact existing block. Executor widens ONLY the predicate; body + position stay as-is. -->

packages/mcp/src/index.ts:831-844 (the spot to extend):
```typescript
async function main(): Promise<void> {
  // Smoke-check escape hatch: respond to --version without booting the MCP server.
  // Used by scripts/verify-tarball.mjs to confirm the published tarball's bin
  // entry executes end-to-end. Must run BEFORE StdioServerTransport setup —
  // a transport on stdio would block forever waiting for a JSON-RPC client.
  if (process.argv.includes('--version')) {
    process.stdout.write(`${SERVER_VERSION}\n`);
    process.exit(0);
  }

  const transport = new StdioServerTransport();
  await server.connect(transport);
  console.error(`${SERVER_NAME} MCP server v${SERVER_VERSION} running on stdio`);
}
```

Version source (DO NOT change how it is derived) — packages/mcp/src/index.ts:18-21:
```typescript
const SERVER_NAME = 'localground';
const SERVER_VERSION = JSON.parse(
  readFileSync(new URL('../package.json', import.meta.url), 'utf8'),
).version as string;
```

The contract that MUST stay green — scripts/verify-tarball.mjs:~203-213 (runs as the `verify:tarball` CI step):
```javascript
    const versionResult = await run(process.execPath, [binEntry, '--version']);
    if (versionResult.exitCode !== 0) { /* throw */ }
    const actualVersion = versionResult.stdout.trim();
    if (actualVersion !== expectedVersion) { /* throw: version drift */ }
```

Existing smoke-test spawn pattern to reuse — packages/mcp/test/smoke.test.ts:14-21 (child process against dist/, process.execPath + array args, never shell):
```typescript
const DIST_PATH = path.resolve(__dirname, '..', 'dist', 'index.js');
function spawnServer() {
  return spawn(process.execPath, [DIST_PATH], { stdio: ['pipe', 'pipe', 'pipe'] });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the version-request predicate in main() (D-12, D-13, D-14)</name>
  <read_first>
    - packages/mcp/src/index.ts (the file being modified — read the main() block at :831-844 and the SERVER_VERSION source at :18-21)
    - scripts/verify-tarball.mjs (the contract at ~203-213 — the bare --version path must keep printing exactly ${SERVER_VERSION}\n and exiting 0)
    - .planning/phases/21-supply-chain-bin-hardening/21-PATTERNS.md (section "packages/mcp/src/index.ts" — the D-12..D-14 transform; "Pre-transport short-circuit" shared pattern)
    - .planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <files>packages/mcp/src/index.ts</files>
  <behavior>
    - `node dist/index.js --version` -> prints `${SERVER_VERSION}\n` to stdout, exits 0 (bare case, the verify-tarball contract)
    - `node dist/index.js --version=anything` -> prints version, exits 0 (prefix/equals form)
    - `node dist/index.js -v` -> prints version, exits 0
    - `node dist/index.js -V` -> prints version, exits 0
    - `node dist/index.js --Version` -> does NOT print-and-exit; falls through to StdioServerTransport startup (case-sensitive, D-13)
    - `node dist/index.js --VERSION` -> does NOT print-and-exit; falls through to startup (D-13)
    - In all match cases the write + exit happen BEFORE `new StdioServerTransport()`
  </behavior>
  <action>
Widen ONLY the predicate condition `process.argv.includes('--version')` at packages/mcp/src/index.ts:836. The body (`process.stdout.write(`${SERVER_VERSION}\n`); process.exit(0);`) and its position (before `new StdioServerTransport()`) stay EXACTLY as-is. Do NOT touch the SERVER_VERSION derivation (:18-21).

Implement a hand-rolled predicate (D-14 — no commander/yargs, no new dependency) recognizing, over `process.argv.slice(2)`:
  - exact `--version`
  - prefix `--version=` (i.e. an arg that starts with `--version=`)
  - exact `-v`
  - exact `-V`
Case-sensitive (D-13): `--Version`, `--VERSION`, `-W`, etc. must NOT match and must fall through to normal startup. Match only the four forms above, byte-exact for the long form.

Your discretion (D-12..D-14 hold either way): inline the check, or extract a small module-scope helper `function isVersionRequest(argv: string[]): boolean { ... }` and call `if (isVersionRequest(process.argv.slice(2)))`. A helper is recommended because the smoke tests can import and unit-test it directly, but a spawn-based test (Task 2) covers it regardless. Example helper:
```typescript
function isVersionRequest(argv: string[]): boolean {
  return argv.some(
    (arg) => arg === '--version' || arg.startsWith('--version=') || arg === '-v' || arg === '-V',
  );
}
```
If you use a helper, export it (or place it where the test can import it from the built dist or the source) so Task 2 can unit-test it without a child-process spawn — but the spawn-based behavioral test is the authoritative gate.

The widened predicate MUST keep the bare `--version` case printing exactly `${SERVER_VERSION}\n` and exiting 0 (the scripts/verify-tarball.mjs contract). Do not change the stdout string or the exit code.
  </action>
  <verify>
    <automated>cd "C:/Users/rlasalle/Projects/localground" && npm run build -w @localground/mcp && node packages/mcp/dist/index.js --version && node packages/mcp/dist/index.js -v && node packages/mcp/dist/index.js -V && node packages/mcp/dist/index.js --version=x && echo PRINTS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp/src/index.ts` no longer contains the bare expression `process.argv.includes('--version')` as the version predicate (it is widened)
    - `packages/mcp/src/index.ts` recognizes `--version`, `--version=`, `-v`, and `-V` (grep for all four match forms or for an `isVersionRequest` helper containing them)
    - `packages/mcp/src/index.ts` does NOT add an import of `commander` or `yargs` (grep: no `from 'commander'`, no `from 'yargs'`)
    - The write + exit remain physically before `new StdioServerTransport()` in `main()`
    - After build: `node packages/mcp/dist/index.js --version` prints the version and exits 0; same for `-v`, `-V`, `--version=x`
    - After build: `node packages/mcp/dist/index.js --Version` does NOT print-and-exit (it boots the server / hangs on stdio) — confirmed in Task 2's test
  </acceptance_criteria>
  <done>The predicate matches --version, --version=…, -v, -V (case-sensitive); --Version/--VERSION fall through; the write-to-stdout + exit(0) stay before transport boot; no parser dependency added; the bare --version contract string + exit code are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add smoke tests for the version predicate match + case-sensitive fall-through (D-12, D-13, D-14)</name>
  <read_first>
    - packages/mcp/test/smoke.test.ts (the file being modified — reuse the spawn(process.execPath, [DIST_PATH]) pattern, the afterEach child reaper, and the readResponse helper)
    - packages/mcp/src/index.ts (the widened predicate from Task 1 — the behavior under test)
    - .planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md (D-12, D-13)
  </read_first>
  <files>packages/mcp/test/smoke.test.ts</files>
  <behavior>
    - Spawning the built bin with `--version`, `--version=x`, `-v`, `-V` each: stdout equals `${pkgVersion}\n` (trimmed === package.json version) AND exit code 0 (process exits, no hang)
    - Spawning with `--Version` (and `--VERSION`): the process does NOT exit 0 with a version print — it boots the stdio server (verify by confirming it does NOT exit on its own within a short window, or that a JSON-RPC initialize handshake succeeds), proving the case-sensitive fall-through (D-13)
  </behavior>
  <action>
Add a new `describe('MCP bin --version predicate', ...)` block to packages/mcp/test/smoke.test.ts. Reuse the existing module constants/helpers (`DIST_PATH`, `process.execPath` + array-arg spawn, the `afterEach` child reaper that kills tracked children — add the version-flag children to the same tracking so a hung `--Version` child is cleaned up).

Read the expected version from `packages/mcp/package.json` at test time (e.g. `JSON.parse(readFileSync(path.resolve(__dirname, '..', 'package.json'), 'utf8')).version`) so the assertion tracks the real version, not a hardcoded literal.

Match-and-exit tests (D-12) — parametrize over `['--version', '--version=x', '-v', '-V']`. For each flag, spawn `process.execPath` with `[DIST_PATH, flag]`, capture stdout, await the `exit` event, and assert:
  - exit code === 0
  - `stdout.trim() === expectedVersion`

Fall-through test (D-13) — spawn with `[DIST_PATH, '--Version']` (and a second case `--VERSION`). Assert the process does NOT print-and-exit-0 as a version request. Implement this robustly without a flaky long sleep: prefer the positive proof — run the existing `handshake(child)` (initialize + notifications/initialized) and assert it completes (the server is up, proving fall-through). If you instead assert non-exit, use a bounded window (e.g. the process has NOT exited within ~1500ms) and ensure the child is killed in `afterEach`. The positive-handshake approach is preferred because it is deterministic.

Keep all spawns using `process.execPath` + array args; never `shell: true` (project spawn discipline). Add the new children to the tracked-children array so the `afterEach` reaper cleans up the deliberately-hung `--Version` server.
  </action>
  <verify>
    <automated>cd "C:/Users/rlasalle/Projects/localground" && npm run build -w @localground/mcp && npm test -w @localground/mcp</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp/test/smoke.test.ts` contains a test asserting `--version`, `--version=…`, `-v`, and `-V` each print `package.json` version (trimmed) and exit 0
    - `packages/mcp/test/smoke.test.ts` contains a test asserting `--Version` (and `--VERSION`) fall through to server startup (handshake succeeds, or process does not version-exit within a bounded window)
    - The test reads the expected version from `packages/mcp/package.json` (no hardcoded version literal)
    - All spawns use `process.execPath` + array args (no `shell: true`)
    - `npm test -w @localground/mcp` exits 0 with the new tests passing
  </acceptance_criteria>
  <done>smoke.test.ts proves all four match forms print-and-exit-0 with the package version and that --Version/--VERSION fall through to server startup; the full mcp test suite passes; deliberately-hung children are reaped in afterEach.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the verify-tarball CLI-06 contract still passes end-to-end</name>
  <read_first>
    - scripts/verify-tarball.mjs (the contract at ~203-213 + the resolveNpmCliJs/run spawn helpers at ~47-99 — the Windows-safe spawn discipline)
    - packages/mcp/src/index.ts (the widened predicate — what the contract exercises)
    - .planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md (D-14 — exact-string contract preserved)
  </read_first>
  <files>scripts/verify-tarball.mjs</files>
  <action>
Do NOT edit scripts/verify-tarball.mjs unless a defect is found — this task is a regression confirmation that the widened predicate kept the bare `--version` contract green. The script packs each tarball, installs clean, runs the bin with `--version`, and asserts `versionResult.stdout.trim() === expectedVersion` AND exit code 0 (D-14 contract).

Run `npm run verify:tarball` from the repo root. It must exit 0. On Windows, the script already uses the `process.execPath` + `npm-cli.js` resolution (resolveNpmCliJs / NPM_CLI_JS, ~:47-63) to avoid the Node 20+ spawnSync EINVAL on `npm.cmd` — do not reintroduce a raw `npm`/`npm.cmd` spawn.

If `verify:tarball` fails because the bare `--version` stdout changed (extra chars, wrong exit), that means Task 1 broke the contract — fix Task 1's predicate so the bare `--version` case prints exactly `${SERVER_VERSION}\n` and exits 0, then re-run. Only touch verify-tarball.mjs if the script itself has a genuine defect (it should not — CLI-06 changes are additive).
  </action>
  <verify>
    <automated>cd "C:/Users/rlasalle/Projects/localground" && npm run verify:tarball</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:tarball` exits 0
    - `scripts/verify-tarball.mjs` is unchanged (no diff) unless a genuine script defect was found and fixed
    - The bin `--version` path asserted by the script (`stdout.trim() === package.json version`, exit 0) still holds after the predicate widening
  </acceptance_criteria>
  <done>npm run verify:tarball passes with the widened predicate; the exact-string + exit-0 contract verify-tarball.mjs depends on is intact; the script was not modified (or only a genuine defect was fixed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invoker -> mcp bin process | Untrusted argv crosses into `main()`. A version request must short-circuit before the bin commits to a long-lived stdio transport. |
| mcp bin -> stdio JSON-RPC transport | Once `new StdioServerTransport()` is created, the process blocks waiting for a JSON-RPC client; a wrong control-flow decision here is an availability bug. |
| Published tarball -> verify-tarball CI gate | The bin's `--version` exact-string + exit-0 output is a machine-checked contract; any drift fails CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Denial of Service | `main()` reaching `new StdioServerTransport()` on an intended version request (e.g. `-v`) the old predicate missed | mitigate | The widened predicate matches `--version`, `--version=…`, `-v`, `-V` and `process.exit(0)`s before transport boot (D-12, Task 1). A stdio transport hangs forever waiting for a client — short-circuiting first removes the hang. |
| T-21-09 | Tampering (control-flow ambiguity) | An over-broad predicate matching `--Version`/`--VERSION` and exiting when the user meant to start the server | mitigate | Case-sensitive byte-exact long-form match (D-13); `--Version`/`--VERSION` deliberately fall through. Locked by a smoke test asserting the handshake succeeds for `--Version` (Task 2). |
| T-21-10 | Supply-chain (dependency surface) | Adding commander/yargs to parse one flag expands the mcp package's dependency/attack surface | mitigate | Hand-rolled predicate only; no parser dependency added (D-14, Task 1). Verified by grep that no `commander`/`yargs` import exists. |
| T-21-11 | Repudiation / Integrity | Predicate change silently alters the bin's `--version` output, breaking the published-tarball contract undetected | mitigate | The bare `--version` stdout string + exit 0 are preserved (D-14); `scripts/verify-tarball.mjs` re-run as a regression gate (Task 3) and runs as the `verify:tarball` CI step on every push/PR. |

No HIGH-severity threat is left unmitigated. This change reduces an existing availability risk (transport hang on a missed version flag).
</threat_model>

<verification>
- The predicate matches `--version`, `--version=…`, `-v`, `-V` and exits 0 with `${SERVER_VERSION}\n` before transport boot; `--Version`/`--VERSION` fall through to server startup.
- No `commander`/`yargs` (or any new) dependency added to the mcp package.
- New smoke tests lock both the match and the fall-through behaviors; `npm test -w @localground/mcp` passes.
- `npm run verify:tarball` exits 0 — the exact-string + exit-0 contract is intact.
</verification>

<success_criteria>
- SC #5 (CLI-06): the mcp bin robustly recognizes a `--version` request (`--version`, `--version=…`, `-v`/`-V`), prints the version to stdout, exits 0, and NEVER boots the stdio transport on a match; `--Version`/`--VERSION` are case-sensitively NOT version requests and fall through; the pre-transport short-circuit + `process.exit(0)` + the exact-string contract `verify-tarball.mjs` depends on are preserved; no argument-parser dependency is added to the mcp package.
</success_criteria>

<output>
After completion, create `.planning/phases/21-supply-chain-bin-hardening/21-02-SUMMARY.md`.
</output>
