---
phase: 21-supply-chain-bin-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .github/dependabot.yml
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "D-01: Every external uses: in BOTH ci.yml and release.yml is pinned to a full 40-char SHA + '# vX.Y.Z' comment (actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0, actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0); ci.yml test matrix stays on Node 20.x"
    - "D-02: ci.yml runs EXACTLY 'pinact run --verify --check' (non-mutating live tag->SHA resolution) in a dedicated ubuntu-latest verify-pins job (NOT in the 3-OS test matrix), fail-closed (no continue-on-error/|| true/skip if:), with the verifier exact-pinned to pinact@v4.1.0 (never @latest), failing the build on any unpinned/mismatched/wrong-tag pin"
    - "D-03: zizmor is NOT added this phase (deferred; pinact --check alone closes SC #1 since only first-party actions are in play)"
    - "D-04: release.yml publish flow exact-pins npm to 'npm@11.18.0' and no longer contains the range 'npm@^11.5.1'"
    - "D-05: release.yml publish job pins Node to 'node-version: '22.14.0'' (a floor >= 22.14.0; no longer the floating '22.x')"
    - "D-06: release.yml has a runtime floor-assert step that FAILS the release (echo '::error::...'; exit 1) if npm --version < 11.5.1 OR Node < 22.14.0 using a NUMERIC/semver-correct comparison (never lexical string operators) — a VALUE assert, not shape-only"
    - "D-07: release.yml carries a visible manual-bump note documenting that the npm@11.18.0 run: literal is NOT covered by Dependabot's github-actions ecosystem and must be bumped by hand"
    - "D-08: .github/dependabot.yml exists with package-ecosystem github-actions, directory '/', schedule weekly, updates grouped into a single PR"
    - "D-09: release.yml OIDC posture is preserved untouched — 'id-token: write', '--provenance' on all four publish invocations (>=4 occurrences), 'package-manager-cache: false', no stored npm token"
    - "D-10: release.yml keeps the SINGLE-JOB release structure (no publish-job split — that refactor is deferred to protect the OIDC/provenance flow)"
    - "D-11: SC #4's surviving SLSA-provenance attestation is flagged as a release-closure verification obligation (read back on the next real tagged release), NOT a CI-on-push step"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "SHA-pinned actions + dedicated ubuntu-only verify-pins job running pinact --verify --check (pinact@v4.1.0, fail-closed)"
      contains: "pinact run --verify --check"
    - path: ".github/workflows/release.yml"
      provides: "SHA-pinned actions, exact npm pin, Node floor, numeric runtime floor-assert, manual-bump note, untouched OIDC/provenance"
      contains: "npm@11.18.0"
    - path: ".github/dependabot.yml"
      provides: "github-actions weekly grouped Dependabot config"
      contains: "package-ecosystem"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub API (live tag->SHA resolution)"
      via: "dedicated ubuntu-latest verify-pins job running pinact run --verify --check"
      pattern: "pinact run --verify --check"
    - from: ".github/workflows/release.yml"
      to: "OIDC trusted publishing"
      via: "id-token: write + --provenance (preserved)"
      pattern: "--provenance"
---

<objective>
Pin the supply chain for both GitHub Actions workflows and make the OIDC publish job's npm/Node floors verifiable at runtime. SHA-pin every external `uses:` in `ci.yml` and `release.yml`, add a non-mutating `pinact run --verify --check` gate to CI as a dedicated ubuntu-only `verify-pins` job (the pinact verifier itself exact-pinned and fail-closed), exact-pin the runner npm and raise the Node floor in the publish job with a numeric fail-closed runtime floor-assert, add a `github-actions` Dependabot config, and preserve the existing OIDC/provenance posture untouched.

Purpose: SEC-01. The `id-token: write` publish job is the repo's highest-privilege surface. A floating action ref can be repointed at a fork/wrong commit; a shape-only version declaration shipped an immutable bad version in v3.0.1. This plan asserts the VALUE (live tag->SHA resolution, numeric runtime floor-assert, fail-closed pin gate), not the shape.
Output: Pinned + verified `ci.yml` and `release.yml`, a new `.github/dependabot.yml`, with OIDC/provenance flow intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md
@.planning/phases/21-supply-chain-bin-hardening/21-PATTERNS.md

<interfaces>
<!-- Exact current state of the two workflows. Executor edits these in place; no exploration needed. -->

ci.yml current matrix + unpinned refs (ci.yml:16-34):
```yaml
jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        node-version: ['20.x']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
```
ci.yml test matrix (ci.yml:24) — DO NOT CHANGE: `node-version: ['20.x']`. ci.yml runs a 3-OS matrix (windows-latest, macos-latest, ubuntu-latest) — the pinact gate must NOT run inside this matrix (BLOCKER 3).

release.yml current unpinned refs + floating npm (release.yml:18-30):
```yaml
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 22.x
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'
          package-manager-cache: false

      - name: Upgrade npm to an OIDC-capable version (>=11.5.1)
        run: |
          npm install -g npm@^11.5.1
          echo "npm version now: $(npm --version)"
```

release.yml canonical fail-closed VALUE-assert analog (release.yml:41-51) — copy this idiom for D-06:
```yaml
      - name: Preflight — assert tag matches package versions
        run: |
          MCP_VERSION=$(node -p "require('./packages/mcp/package.json').version")
          ...
          if [ "$GITHUB_REF_NAME" != "v$MCP_VERSION" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match mcp version v$MCP_VERSION"; exit 1
          fi
```

release.yml OIDC posture to PRESERVE (release.yml:8-10, :25, :53-62) — do not trim:
```yaml
permissions:
  contents: read
  id-token: write
...
          package-manager-cache: false
...
          npm publish -w @localground/mcp --provenance --access public --dry-run
          npm publish -w @localground/cli --provenance --access public --dry-run
          npm publish -w @localground/mcp --provenance --access public
          npm publish -w @localground/cli --provenance --access public
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SHA-pin both workflows and add the fail-closed pinact verify gate as a dedicated ubuntu-only job (D-01, D-02, D-03)</name>
  <read_first>
    - .github/workflows/ci.yml (the file being modified — note the 3-OS matrix at ci.yml:23)
    - .github/workflows/release.yml (the file being modified — SHA-pin only in this task)
    - .planning/phases/21-supply-chain-bin-hardening/21-PATTERNS.md (sections "ci.yml" and "release.yml" — exact transform blocks)
    - .planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml</files>
  <action>
D-01 — In BOTH workflows, replace the two floating `uses:` refs with the Codex-confirmed 40-char SHA + version comment (copy these literals verbatim — do NOT paraphrase the SHA or tag):
  - `uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0`
  - `uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0`

In ci.yml, replace `actions/checkout@v4` (ci.yml:28) and `actions/setup-node@v4` (ci.yml:31). Keep the `cache: 'npm'` line and `node-version: ${{ matrix.node-version }}` unchanged. DO NOT change the test matrix at ci.yml:24 — it stays `node-version: ['20.x']` (setup-node v6.4.0 supports Node 20.x; the v4->v6 bump is incidental to pinning).

In release.yml, replace `actions/checkout@v4` (release.yml:19) and `actions/setup-node@v6` (release.yml:22). PRESERVE `package-manager-cache: false` on the setup-node step (D-09). (The Node floor and npm pin are Task 2 — leave them as-is in this task.)

D-02 — Add the pinact gate as a DEDICATED ubuntu-only `verify-pins` job (BLOCKER 3 — do NOT add it as a step inside the 3-OS `test` matrix). `ci.yml` runs `os: [windows-latest, macos-latest, ubuntu-latest]` (ci.yml:23). The POSIX/Go pinact command block is a parse error on the Windows leg (PowerShell default shell: `The '--' operator works only on variables or on properties`) and would either break CI or silently no-op on 2 of 3 legs. Pin verification is OS-independent and only needs to run once per push — give it its own `ubuntu-latest` job that runs in EXACTLY one Ubuntu context. Add this as a sibling job to `test:` in ci.yml's `jobs:` block, running EXACTLY:
```yaml
  verify-pins:
    name: Verify action pins resolve to their tags
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Verify action pins (pinact run --verify --check)
        shell: bash
        run: |
          go install github.com/suzuki-shunsuke/pinact/v4/cmd/pinact@v4.1.0
          "$(go env GOPATH)/bin/pinact" run --verify --check
        env:
          GITHUB_TOKEN: ${{ github.token }}
```
Hard requirements on this gate:
  - The executed command MUST be EXACTLY `pinact run --verify --check` (check mode = non-mutating, fails on mismatch). Do NOT use raw `pinact run --verify` — pinact v4 auto-corrects comments without `--check`, silently mutating instead of failing the build. `--verify` triggers live tag->SHA resolution via the GitHub API (needs GITHUB_TOKEN), which is what proves each SHA resolves to its commented tag (SC #1's "verify the VALUE").
  - The verifier install MUST be exact-pinned to `pinact@v4.1.0` (current latest, Codex-verified 2026-06-29). NEVER `@latest` — the tool that detects unpinned refs must not itself float (self-contradictory with D-01/D-02).
  - The gate MUST be fail-closed: NO `continue-on-error: true` on the step; the `run:` block does NOT append `|| true` / `|| :` / `; true`, is NOT preceded by `set +e`, and has NO `if:` that skips it on push/PR.
  - The gate MUST run in exactly ONE Ubuntu context and MUST NOT execute on the Windows/macOS legs (it is not in the matrix at all — it is a separate ubuntu-latest job).
  - The checkout in this job uses the SAME locked SHA pin as the rest of D-01 (`actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0`).
If a pre-built install path is simpler on the Ubuntu runner, any install method is fine PROVIDED the install is exact-pinned (not `@latest`), the executed command is exactly `pinact run --verify --check`, the gate is fail-closed, and it runs only on Ubuntu.

Prove fail-closed by reproduction: in a SCRATCH copy of a workflow (never commit it), flip one hex digit of a pinned SHA and run the pinact command against it; paste the non-zero exit proving the gate fails on a bad pin.

D-03 — Do NOT add zizmor or any second workflow-security action. `pinact --check` alone closes SC #1 for this phase (only first-party actions in play).
  </action>
  <verify>
    <automated>cd "C:/Users/rlasalle/Projects/localground" && grep -q "actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0" .github/workflows/ci.yml && grep -q "actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0" .github/workflows/ci.yml && grep -q "actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0" .github/workflows/release.yml && grep -q "actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0" .github/workflows/release.yml && grep -q "pinact run --verify --check" .github/workflows/ci.yml && grep -q "verify-pins:" .github/workflows/ci.yml && grep -q "pinact/v4/cmd/pinact@v4.1.0" .github/workflows/ci.yml && ! grep -q "pinact@latest" .github/workflows/ci.yml && ! grep -q "continue-on-error" .github/workflows/ci.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `ci.yml` contains `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0`
    - `ci.yml` contains `actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0`
    - `ci.yml` still contains `node-version: ['20.x']` (test matrix unchanged)
    - `ci.yml` contains the exact string `pinact run --verify --check`
    - `ci.yml` does NOT contain `actions/checkout@v4` or `actions/setup-node@v4`
    - The pinact gate is a DEDICATED `verify-pins:` job on `runs-on: ubuntu-latest` — it is NOT a step inside the `test` matrix (BLOCKER 3). The pinact step runs in exactly one Ubuntu context and does NOT execute on the Windows/macOS legs (it is not in the matrix at all).
    - The pinact step has `shell: bash` (or is in a job whose default shell is bash) so the POSIX/Go command block parses.
    - The verifier install is exact-pinned `pinact@v4.1.0` — `ci.yml` does NOT contain `pinact@latest` (forbidden for the verifier tool, BLOCKER 2).
    - The pinact step is fail-closed: `ci.yml` has NO `continue-on-error: true` on the pinact step; its `run:` block does NOT contain `|| true`, `|| :`, `; true`, and is NOT preceded by `set +e`; the step has NO `if:` that skips it on push/PR.
    - Fail-closed proven by EXECUTION (not grep): the executor ran pinact against a deliberately-broken pin (one SHA hex digit flipped) in a scratch copy and pasted the non-zero exit code. (Clean repo state: pinact exits 0; broken-pin scratch: pinact exits non-zero.)
    - `release.yml` contains `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0` and `actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0`
    - `release.yml` does NOT contain `actions/checkout@v4` or `actions/setup-node@v6`
    - `release.yml` still contains `package-manager-cache: false`
    - Neither workflow references `zizmor`
  </acceptance_criteria>
  <done>Both workflows pin both external actions to the locked SHAs with `# vX.Y.Z` comments; ci.yml runs `pinact run --verify --check` in a dedicated ubuntu-only `verify-pins` job (pinact exact-pinned to v4.1.0, fail-closed, not in the 3-OS matrix), proven to fail on a broken pin by execution; ci.yml matrix stays on 20.x; release.yml's package-manager-cache:false preserved; no zizmor added.</done>
</task>

<task type="auto">
  <name>Task 2: Exact-pin npm, raise Node floor, add NUMERIC runtime floor-assert + manual-bump note in release.yml (D-04, D-05, D-06, D-07, D-09, D-10)</name>
  <read_first>
    - .github/workflows/release.yml (the file being modified)
    - .planning/phases/21-supply-chain-bin-hardening/21-PATTERNS.md (section "release.yml" — D-04..D-07 transforms + the fail-closed VALUE-assert pattern)
    - .planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md (D-04, D-05, D-06, D-07, D-09, D-10)
  </read_first>
  <files>.github/workflows/release.yml</files>
  <action>
D-05 — Raise the Node floor. Replace `node-version: '22.x'` (release.yml:24) with the exact key `node-version: '22.14.0'` (the paired OIDC floor). The old floating `'22.x'` MUST be fully gone from the file — do NOT leave it in a comment. Keep `package-manager-cache: false` on the same step (D-09).

D-04 — Exact-pin npm. In the "Upgrade npm to an OIDC-capable version" step (release.yml:27-30), replace the range `npm install -g npm@^11.5.1` with the EXACT literal `npm install -g npm@11.18.0` (Codex-confirmed as `latest` and >= the 11.5.1 OIDC floor). Keep the `echo "npm version now: $(npm --version)"` line.

D-07 — Add a visible manual-bump note in that same npm-upgrade step. The `npm@11.18.0` `run:` literal is NOT covered by Dependabot's github-actions ecosystem (Dependabot scans `uses:` refs, not `run:` literals), so it ages silently. Add a YAML comment immediately above the `npm install` line, e.g.:
```yaml
      - name: Upgrade npm to an OIDC-capable version (exact-pinned)
        run: |
          # MANUAL-BUMP: Dependabot does NOT track this run: literal (it scans uses: refs only).
          # Bump npm@11.18.0 by hand when a newer OIDC-capable npm ships. Floor-assert below guards correctness.
          npm install -g npm@11.18.0
          echo "npm version now: $(npm --version)"
```
Exact wording/placement is your discretion provided the note is visible in release.yml and names the manual-bump obligation.

D-06 — Add a runtime floor-assert step AFTER the npm-upgrade step (so it checks the upgraded npm) and before the publish steps. Mirror the fail-closed `echo "::error::..."; exit 1` idiom from the existing Preflight step (release.yml:41-51). Fail the release if `npm --version` < 11.5.1 OR Node < 22.14.0.

CRITICAL — the comparison MUST be numeric/semver-correct, NOT lexical (BLOCKER 1). A lexical string compare is WRONG: `[ "22.9.0" \< "22.14.0" ]` returns FALSE (sub-floor Node 22.9.0 would be wrongly treated as >= floor and PUBLISH), and `[ "11.18.0" \< "11.5.1" ]` returns TRUE (in-floor npm wrongly fails). FORBIDDEN: `[ "$V" \< "$FLOOR" ]`, the `<` / `>` string operators, and naive `==` on dotted strings. There is NO `semver` package in this repo (root or packages/mcp) — do NOT `require('semver')`; it throws `Cannot find module 'semver'`.

Use a numeric field-split comparison (canonical idiom) or a `sort -V` min-check. Field-split example:
```yaml
      - name: Assert OIDC floors at runtime (npm >= 11.5.1, Node >= 22.14.0)
        run: |
          NPM_VERSION=$(npm --version)
          NODE_VERSION=$(node -p "process.versions.node")
          echo "npm: $NPM_VERSION  node: $NODE_VERSION"
          ge() { # ge VERSION FLOOR -> exit 0 if VERSION >= FLOOR (numeric, dotted)
            [ "$(printf '%s\n%s\n' "$2" "$1" | sort -V | head -n1)" = "$2" ]
          }
          ge "$NPM_VERSION" "11.5.1"  || { echo "::error::npm $NPM_VERSION is below the OIDC floor 11.5.1"; exit 1; }
          ge "$NODE_VERSION" "22.14.0" || { echo "::error::Node $NODE_VERSION is below the OIDC floor 22.14.0"; exit 1; }
          echo "OIDC floors satisfied."
```
(`sort -V` is version-sort: the lower of {floor, actual} sorts first; if the floor sorts first, actual >= floor.) A pure-bash field-split (`IFS=. read a b c <<<"$V"` then numeric `-gt`/`-ge` per field) is equally acceptable. The assert MUST compare the actual runtime VALUE (`npm --version`, runtime Node version), not a declared literal — this is the value-assert closing the v3.0.1 "asserted shape, shipped a bad version" lesson. The locked requirement: numeric/semver-correct, fail-closed below either floor.

PROVE correctness by EXECUTION (not grep). Run the comparison logic locally against these test vectors and paste the output:
  - {Node 22.9.0, npm 11.4.0} -> exit 1 (BOTH below floor; assert fails the release)
  - {Node 22.20.0, npm 11.18.0} -> exit 0 (both >= floor; assert passes)
  - Lexical-trap rejection: Node 22.9.0 FAILS (sub-floor), Node 22.100.0 PASSES (>= floor). A lexical compare would get both backwards — your numeric compare must get them right.

D-09 / D-10 — PRESERVE EVERYTHING ELSE UNTOUCHED. Do NOT remove or alter: `permissions: id-token: write` (release.yml:8-10), `package-manager-cache: false`, the dry-run publish step, `--provenance` on all four publish invocations, `--access public`, and the single-job structure. Do NOT split into separate build/publish jobs (the job-split is deferred — it risks the OIDC/provenance flow SC #4 must preserve).
  </action>
  <verify>
    <automated>cd "C:/Users/rlasalle/Projects/localground" && grep -q "npm@11.18.0" .github/workflows/release.yml && ! grep -q "npm@\^11.5.1" .github/workflows/release.yml && grep -q "node-version: '22.14.0'" .github/workflows/release.yml && ! grep -q "node-version: '22.x'" .github/workflows/release.yml && grep -q "id-token: write" .github/workflows/release.yml && [ "$(grep -c -- "--provenance" .github/workflows/release.yml)" -ge 4 ] && grep -q "package-manager-cache: false" .github/workflows/release.yml && grep -qi "MANUAL-BUMP\|bump.*by hand\|bump.*manually" .github/workflows/release.yml && grep -q "::error::" .github/workflows/release.yml && ! grep -qE '\[ +"\$[A-Z_]+" +\\?< ' .github/workflows/release.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `release.yml` contains `npm@11.18.0` AND does NOT contain `npm@^11.5.1`
    - `release.yml` contains `node-version: '22.14.0'` (anchored key) AND does NOT contain `node-version: '22.x'` anywhere (including comments) — belt-and-suspenders, mirroring the npm pin (WARNING 1)
    - `release.yml` contains a runtime floor-assert step that emits `::error::` and `exit 1` when npm < 11.5.1 or Node < 22.14.0, comparing actual `npm --version` / runtime Node version (VALUE assert)
    - The floor-assert uses a NUMERIC/semver-correct comparison (field-split or `sort -V`); it does NOT use lexical string operators. `release.yml` contains NO `[ "$V" \< "$FLOOR" ]`-style compare, no `<`/`>` string operators on versions, and no `require('semver')` (BLOCKER 1)
    - Comparison correctness PROVEN by execution (pasted output): exit 1 for {Node 22.9.0, npm 11.4.0}; exit 0 for {Node 22.20.0, npm 11.18.0}; lexical-trap rejection — Node 22.9.0 FAILS and Node 22.100.0 PASSES
    - `release.yml` contains a visible manual-bump note (case-insensitive match on `MANUAL-BUMP` / `bump ... by hand` / `bump ... manually`) in or near the npm-upgrade step
    - `release.yml` still contains `id-token: write`, `package-manager-cache: false`, and `--provenance` appears at LEAST 4 times — enforced by `[ "$(grep -c -- "--provenance" .github/workflows/release.yml)" -ge 4 ]` (2 dry-run + 2 real publish) (WARNING 2)
    - `release.yml` still has a single `release:` job (no second publish job introduced)
  </acceptance_criteria>
  <done>release.yml exact-pins npm to 11.18.0, raises the Node floor to the anchored `node-version: '22.14.0'` (floating '22.x' fully removed), fails the release on a runtime npm/Node value below floor using a NUMERIC comparison proven correct against the lexical-trap vectors, carries a visible manual-bump note, and leaves the OIDC/provenance/single-job posture entirely intact (--provenance >= 4).</done>
</task>

<task type="auto">
  <name>Task 3: Add the github-actions Dependabot config (D-08, D-07 note, D-11 closure flag)</name>
  <read_first>
    - .planning/phases/21-supply-chain-bin-hardening/21-PATTERNS.md (section ".github/dependabot.yml" — canonical schema to author)
    - .planning/phases/21-supply-chain-bin-hardening/21-CONTEXT.md (D-08, D-07, D-11)
  </read_first>
  <files>.github/dependabot.yml</files>
  <action>
D-08 — Create `.github/dependabot.yml` (NEW file; no in-repo analog — do NOT copy the vendored `node_modules/fast-uri/.github/dependabot.yml`). Use the canonical Dependabot v2 github-actions schema, with updates grouped into a single PR:
```yaml
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"
```
This keeps the SHA pins updatable: Dependabot opens one grouped weekly PR proposing newer SHAs (with refreshed `# vX.Y.Z` comments) for the pinned `uses:` refs in ci.yml and release.yml.

D-07 / D-08 accepted gaps (document only, do NOT add config to "fix" them this phase):
  - Dependabot's github-actions ecosystem scans `uses:` refs only — it does NOT track the `npm@11.18.0` `run:` literal in release.yml (that's the manual-bump note from Task 2). Do not attempt to make Dependabot cover it.
  - Grouped updates default to version-updates; repo-level Dependabot SECURITY alerts still fire independently. Do NOT add a separate `applies-to: security-updates` group this phase.
Capture both as a short YAML comment block at the top of the file so the accepted gaps are visible in-repo.

D-11 — In the plan SUMMARY (not in any file), flag the closure obligation for the verifier/release-closure: SC #4's "surviving SLSA-provenance attestation" cannot be proven by the dry-run publish step; it must be read back on the NEXT actual tagged release. This is NOT a CI-on-push step — do not add a CI step for it.
  </action>
  <verify>
    <automated>cd "C:/Users/rlasalle/Projects/localground" && test -f .github/dependabot.yml && grep -q "package-ecosystem" .github/dependabot.yml && grep -qi "github-actions" .github/dependabot.yml && grep -q "interval:" .github/dependabot.yml && grep -qi "weekly" .github/dependabot.yml && grep -q "groups:" .github/dependabot.yml && grep -q "version: 2" .github/dependabot.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/dependabot.yml` exists
    - Contains `version: 2`
    - Contains `package-ecosystem` with `github-actions`
    - Contains `directory: "/"`
    - Contains `schedule:` with `interval:` `weekly`
    - Contains a `groups:` block (single grouped PR)
    - Contains a visible comment documenting the two accepted gaps (run-literal not tracked; security-updates not separately grouped)
  </acceptance_criteria>
  <done>.github/dependabot.yml exists with the canonical github-actions weekly grouped schema and a visible comment recording the two accepted gaps; D-11 closure obligation flagged in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> external action source | A floating `uses:` tag is a mutable ref resolved at run time; the action's owner (or a tag repoint) controls what code executes in the workflow. |
| Release runner -> npm registry (OIDC publish) | The `id-token: write` job mints a short-lived OIDC token and publishes with provenance — the repo's highest-privilege surface. |
| Runtime tool versions -> OIDC eligibility | npm < 11.5.1 or Node < 22.14.0 silently disables OIDC/provenance, degrading the publish to an unattested one. |
| Pin-verifier tool itself -> CI integrity | An unpinned (`@latest`) or fail-open pinact undermines the very gate meant to detect unpinned/mismatched refs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | `uses: actions/checkout@v4` / `setup-node@v4` floating tags in ci.yml + release.yml | mitigate | SHA-pin both refs to 40-char commit SHAs with `# vX.Y.Z` comments (D-01, Task 1). A mutable tag can be repointed at malicious code; an immutable SHA cannot. |
| T-21-02 | Spoofing / Tampering | A pinned SHA that points at a fork/wrong commit (the SHA is right shape but wrong value) | mitigate | `pinact run --verify --check` does live tag->SHA resolution via the GitHub API and fails the build if any SHA does not resolve to its commented tag (D-02, Task 1). VALUE assert, not shape. Proven fail-closed by a broken-pin reproduction. |
| T-21-03 | Elevation of Privilege | Impostor-commit in the action history (a commit attributed to a maintainer that they did not author) | accept | Deferred (D-03). Only first-party GitHub actions (checkout, setup-node) are in play — lowest impostor-commit risk. zizmor's impostor-commit audit recorded as deferred defense-in-depth; residual risk accepted for this phase. |
| T-21-04 | Tampering / Repudiation | OIDC publish silently downgraded to unattested because the runner npm/Node dropped below the OIDC floor | mitigate | Exact-pin npm@11.18.0 + anchored Node floor `node-version: '22.14.0'` (D-04, D-05) AND a fail-closed NUMERIC runtime floor-assert that exits 1 if the live `npm --version`/Node version is below floor (D-06, Task 2). Lexical compare explicitly forbidden — proven correct against the lexical-trap vectors. |
| T-21-05 | Information Disclosure / EoP | OIDC posture accidentally trimmed by the pinning refactor (token added, `id-token: write` dropped, `--provenance` removed) | mitigate | D-09/D-10 preservation asserts: `id-token: write`, `--provenance` >= 4 (enforced by a `-ge 4` count gate, not bare grep -c), `package-manager-cache: false`, single-job structure all verified present post-edit (Task 2 acceptance criteria). |
| T-21-06 | Tampering | Pins go stale and miss security patches in the actions themselves | mitigate | `.github/dependabot.yml` github-actions weekly grouped config opens PRs proposing updated SHAs (D-08, Task 3). Repo-level security alerts fire independently (accepted gap, documented). |
| T-21-07 | Repudiation | The `npm@11.18.0` run: literal ages silently (Dependabot does not track run: literals) | accept | Documented manual-bump note in release.yml (D-07); the NUMERIC runtime floor-assert (T-21-04) guards correctness so staleness never drops below the OIDC floor. |
| T-21-12 | Tampering / EoP | The pinact verifier itself floats (`@latest`) or the gate is neutered (`|| true`, `continue-on-error`, false `if:`) — shape-only gate recreating the v3.0.1 class | mitigate | pinact exact-pinned to `@v4.1.0` (never `@latest`); fail-open syntax forbidden; gate proven fail-closed by a deliberately-broken-pin reproduction (D-02, Task 1). |
| T-21-13 | Denial of Service (CI breakage) | The POSIX/Go pinact step run unmodified across the Windows/macOS matrix legs (PowerShell parse error or silent no-op on 2 of 3 legs) | mitigate | pinact runs in a dedicated `ubuntu-latest` `verify-pins` job (not the 3-OS matrix) with `shell: bash`, in exactly one Ubuntu context (BLOCKER 3, Task 1). |

No HIGH-severity threat is left unmitigated. T-21-03 and T-21-07 are explicitly accepted with documented rationale per D-03/D-07.
</threat_model>

<verification>
- `ci.yml` and `release.yml` both pin both external actions to the locked SHAs with version comments; ci.yml runs `pinact run --verify --check` in a dedicated ubuntu-only `verify-pins` job (pinact exact-pinned v4.1.0, fail-closed, not in the 3-OS matrix); ci.yml matrix stays on Node 20.x.
- `release.yml` exact-pins npm@11.18.0, raises the Node floor to the anchored `node-version: '22.14.0'` (floating '22.x' fully removed), fails the release on a sub-floor runtime npm/Node value via a NUMERIC comparison, and carries a visible manual-bump note.
- OIDC posture (`id-token: write`, `--provenance` >= 4, `package-manager-cache: false`, single-job) is byte-for-byte preserved aside from the locked floor edits.
- `.github/dependabot.yml` exists with the canonical github-actions weekly grouped schema.
- D-11 (surviving SLSA-provenance attestation) flagged in the SUMMARY as a next-real-release closure obligation, not a CI step.
</verification>

<success_criteria>
- SC #1: every third-party `uses:` in both workflows is SHA-pinned with a `# vX.Y.Z` comment AND verified to resolve to its tag via a fail-closed `pinact run --verify --check` (pinact exact-pinned, ubuntu-only job).
- SC #2: publish job exact-pins npm >= 11.5.1 (11.18.0) AND runs on Node >= 22.14.0 (anchored `'22.14.0'`), asserting both at runtime with a numeric fail-closed compare.
- SC #3: a `github-actions` Dependabot config is present.
- SC #4: the release still publishes via OIDC with no stored token, retains `id-token: write`, and produces a surviving SLSA-provenance attestation (preservation verified; live attestation read back next real release per D-11).
</success_criteria>

<output>
After completion, create `.planning/phases/21-supply-chain-bin-hardening/21-01-SUMMARY.md`. Include the D-11 closure obligation explicitly: the surviving SLSA-provenance attestation must be read back on the next actual tagged release (it cannot be proven by the dry-run publish step or any CI-on-push step).
</output>
</content>
</invoke>
