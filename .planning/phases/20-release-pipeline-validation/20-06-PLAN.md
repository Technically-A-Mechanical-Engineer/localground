---
phase: 20-release-pipeline-validation
plan: 06
type: execute
wave: 5
depends_on: [20-05]
files_modified: []
autonomous: false
requirements: [PIPE-02, DOC-03]
must_haves:
  truths:
    - "SC3: both @localground/mcp@3.0.1 and @localground/cli@3.0.1 show the provenance attestation 'Provenance' badge on their npmjs.com package pages"
    - "SC4 / DOC-03: both npmjs.com package pages render the per-package README content (packages/mcp/README.md and packages/cli/README.md) — not the empty-state placeholder shown for v3.0.0"
    - "SC5: a clean machine running npx -y @localground/cli@3.0.1 detect resolves the published artifact and executes successfully (exit 0, real detect output)"
    - "D-12: validation scope is exactly SC5 + DOC-03 + ONE manual MCP-registration check — run the documented command once post-publish: claude mcp add --transport stdio localground -- cmd /c npx -y @localground/mcp@3.0.1 ; NO bundled .mcp.json is shipped (that reverses Phase 19 C-1 and is deferred to v3.1.0)"
  artifacts:
    - path: ".planning/phases/20-release-pipeline-validation/20-PIPELINE-LOG.md"
      provides: "provenance-badge evidence, README-render evidence, npx-smoke output, MCP-add-check result"
      min_lines: 20
  key_links:
    - from: "release.yml --provenance publish"
      to: "npmjs.com Provenance badge on both package pages"
      via: "OIDC provenance attestation rendered by npm registry"
      pattern: "Provenance"
    - from: "packages/{mcp,cli}/README.md in the published tarball"
      to: "rendered README on the npmjs.com package page"
      via: "npm rendering the tarball README (files: [dist] preserves README per npm rules)"
      pattern: "@localground"
---

<objective>
Close the milestone by validating the published artifacts on a clean surface: confirm the provenance badge on both npmjs.com pages (SC3), confirm both pages render their per-package READMEs (SC4 / DOC-03), run the npx smoke against the live CLI (SC5), and run the single documented MCP-registration check (D-12). No code changes — this is pure post-publish verification.

This plan is NON-AUTONOMOUS (`autonomous: false`): SC3/SC4 require reading rendered npmjs.com web pages, and SC5/D-12 run commands that resolve the freshly-published registry artifacts (clean-machine semantics, npx cache-clean). It depends on 20-05 having published both packages at 3.0.1.

Purpose: the success criteria that prove the release actually shipped usable artifacts — provenance visible, docs visible, install works end-to-end. D-12 explicitly bounds scope: validate the documented MCP-registration command ONCE; do NOT add a bundled .mcp.json (that reverses Phase 19 C-1, auto-starts a competing server, and adds product code to a validation phase — deferred to v3.1.0).
Output: A completed 20-PIPELINE-LOG.md with badge/README/npx/MCP evidence, closing PIPE-02, DOC-03, and the v3.0.1 milestone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-release-pipeline-validation/20-CONTEXT.md
@packages/mcp/README.md
@packages/cli/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify provenance badge + README rendering on both npmjs.com pages (SC3, SC4, DOC-03)</name>
  <read_first>
    - packages/mcp/README.md (the content that should render on the mcp page)
    - packages/cli/README.md (the content that should render on the cli page)
    - .planning/phases/20-release-pipeline-validation/20-CONTEXT.md (SC3/SC4/DOC-03; DOC-03 replaces the v3.0.0 empty-state placeholder)
  </read_first>
  <action>Open both npmjs.com package pages (mcp + cli at /v/3.0.1) and confirm each shows a Provenance badge (SC3) and renders its real README content rather than the v3.0.0 empty-state placeholder (SC4/DOC-03); corroborate provenance via `npm view @localground/{mcp,cli}@3.0.1 --json` dist.attestations being non-null. Record all four confirmations with page URLs in 20-PIPELINE-LOG.md. Full step detail in how-to-verify.</action>
  <what-built>Plan 20-05 published both packages at 3.0.1 with --provenance. npm renders the provenance attestation as a "Provenance" badge and renders the tarball's README on the package page.</what-built>
  <how-to-verify>
    PROVENANCE (SC3) — open both pages in a browser:
      https://www.npmjs.com/package/@localground/mcp/v/3.0.1
      https://www.npmjs.com/package/@localground/cli/v/3.0.1
    On each page confirm a "Provenance" section/badge is present (links to the GitHub Actions build + the source commit). Optionally cross-check from the terminal:
      `npm view @localground/mcp@3.0.1 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const o=JSON.parse(s);console.log('dist.attestations:',JSON.stringify(o.dist&&o.dist.attestations||null))})"`
    A non-null dist.attestations (provenance) corroborates the badge.

    README RENDER (SC4 / DOC-03) — on each package page, confirm the rendered README shows the real content (NOT the v3.0.0 empty-state placeholder):
      - @localground/mcp page renders the "@localground/mcp" heading, the Install section incl. the Windows `cmd /c npx` command, and the Available Tools table.
      - @localground/cli page renders the "@localground/cli" heading, the npx install command, and the Commands section.

    Record for each package in 20-PIPELINE-LOG.md: provenance badge present (yes/no) + README renders real content (yes/no), with the page URL. All four must be yes.
  </how-to-verify>
  <acceptance_criteria>
    - npmjs.com @localground/mcp@3.0.1 page shows a Provenance badge AND renders the README (heading + cmd /c npx + tools table)
    - npmjs.com @localground/cli@3.0.1 page shows a Provenance badge AND renders the README (heading + npx install + Commands)
    - `npm view @localground/mcp@3.0.1 --json` dist.attestations is non-null (provenance corroboration); same for cli
    - 20-PIPELINE-LOG.md records all four confirmations with page URLs
  </acceptance_criteria>
  <resume-signal>Type "pages-verified" once both pages show the Provenance badge and render their READMEs; or describe what's missing.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Clean-machine npx smoke (SC5) + documented MCP-registration check (D-12)</name>
  <read_first>
    - packages/cli/README.md (the npx command being validated)
    - packages/mcp/README.md (the documented MCP-add command incl. Windows cmd /c npx)
    - .planning/phases/20-release-pipeline-validation/20-CONTEXT.md (D-12 — SC5 + one MCP-add check; NO bundled .mcp.json)
  </read_first>
  <action>Clear the npm/npx cache (`npm cache clean --force` and ensure no local npm link), then run `npx -y @localground/cli@3.0.1 --version` (expect 3.0.1) and `npx -y @localground/cli@3.0.1 detect` (expect exit 0 + real detect output) for SC5; then run the documented MCP-registration command once (D-12) — `claude mcp add --transport stdio localground -- cmd /c npx -y @localground/mcp@3.0.1` — and confirm `claude mcp list` shows localground. Do NOT create or commit any .mcp.json. Record the npx result + MCP-add result in 20-PIPELINE-LOG.md. Full step detail in how-to-verify.</action>
  <what-built>Both packages are live at 3.0.1. SC5 validates the CLI resolves and runs from the registry on a clean surface; D-12 validates the documented MCP-registration command works once.</what-built>
  <how-to-verify>
    SC5 — clean-machine npx smoke. To approximate a clean machine, clear the npx/npm cache for these packages first so npx fetches the published artifact (not a local link). On the user's machine:
      `npm cache clean --force`   (or at minimum ensure no local `npm link` to @localground/cli is active)
      `npx -y @localground/cli@3.0.1 detect`
    Confirm: exit code 0 AND real detect output (platform/shell/cloud-service/projects/path-hash findings), NOT an error or a stale local version. Verify the resolved version is 3.0.1:
      `npx -y @localground/cli@3.0.1 --version`   → prints 3.0.1
    (Ideal: also run on a genuinely clean second machine or container if available — but the cache-cleared npx fetch is the minimum bar for SC5.)

    D-12 — documented MCP-registration check (run ONCE). Using the exact command from packages/mcp/README.md (Windows form, since the user is on Windows):
      `claude mcp add --transport stdio localground -- cmd /c npx -y @localground/mcp@3.0.1`
    Confirm the server registers without error (`claude mcp list` shows `localground`). This validates the published mcp artifact is launchable via the documented command. Do NOT create or commit any .mcp.json file (D-12 / Phase 19 C-1 — bundled config is OUT). After the check, you may remove the test registration (`claude mcp remove localground`) to leave the environment clean; record the result either way.

    Record in 20-PIPELINE-LOG.md: npx detect exit code + version, and the MCP-add registration result.
  </how-to-verify>
  <acceptance_criteria>
    - `npx -y @localground/cli@3.0.1 --version` prints 3.0.1
    - `npx -y @localground/cli@3.0.1 detect` exits 0 with real detect output (logged)
    - `claude mcp add --transport stdio localground -- cmd /c npx -y @localground/mcp@3.0.1` registers without error and `claude mcp list` shows localground (logged)
    - NO .mcp.json file was created or committed (D-12): `git status --porcelain` shows no new .mcp.json
    - 20-PIPELINE-LOG.md records the npx result + MCP-add result
  </acceptance_criteria>
  <resume-signal>Type "install-verified" once npx detect runs clean at 3.0.1 and the documented MCP-add registers; or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry artifact → end-user machine (npx) | the published tarball executes on the user's machine via npx; provenance is the integrity signal |
| documented MCP-add command → Claude Code session | registers a server that spawns the published mcp artifact; must launch cleanly without a bundled config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-20 | Tampering | published tarball integrity (supply chain) | mitigate | SC3 verifies the provenance attestation badge is present on both pages (npm-rendered OIDC provenance linking build → commit); `npm view --json dist.attestations` corroborates. |
| T-20-21 | Spoofing | npx resolving a stale/local version instead of the published 3.0.1 | mitigate | Task 2 clears the npx/npm cache and asserts `--version` == 3.0.1 before trusting the detect run. |
| T-20-22 | Elevation of Privilege | bundled .mcp.json auto-starting a server on plugin load | mitigate | D-12 forbids shipping .mcp.json; acceptance criteria assert no .mcp.json is created/committed; MCP registration stays explicit/documented (Phase 19 C-1 preserved). |
</threat_model>

<verification>
- Provenance badge present on both npmjs.com pages (SC3); dist.attestations non-null.
- Both pages render real README content (SC4 / DOC-03), not the empty-state placeholder.
- npx -y @localground/cli@3.0.1 detect exits 0 at version 3.0.1 (SC5).
- Documented MCP-add command registers cleanly (D-12); no .mcp.json shipped.
</verification>

<success_criteria>
- SC3 satisfied: provenance badge visible on both package pages.
- SC4 / DOC-03 satisfied: both pages render per-package READMEs (not v3.0.0 placeholder).
- SC5 satisfied: clean npx detect runs against the published 3.0.1 artifact.
- D-12 satisfied: one documented MCP-registration check passed; no bundled .mcp.json.
- PIPE-02 fully closed (publish + provenance + render + install all verified); v3.0.1 milestone closes.
</success_criteria>

<output>
After completion, create `.planning/phases/20-release-pipeline-validation/20-06-SUMMARY.md` (include all post-publish evidence: badge, README render, npx output, MCP-add result).
</output>
