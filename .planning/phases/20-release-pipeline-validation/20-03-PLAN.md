---
phase: 20-release-pipeline-validation
plan: 03
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified: []
autonomous: false
requirements: [PIPE-01]
must_haves:
  truths:
    - "D-10 (push gate): the pre-flight commit (D-04/D-05 manifests + D-13 docs from 20-01, and the D-02/D-07/D-08/D-09 release.yml hardening from 20-02) is committed and master is pushed to origin — firing the first ci.yml run that includes these fixes; a red CI run is free to retry, so push happens BEFORE the version bump and long before the tag"
    - "D-10 (no-tags push, review L1): the catch-up uses plain `git push origin master` and NEVER `git push --tags` or `git push --follow-tags` — a local-only v3.0.0 tag exists (never on remote), and pushing tags prematurely would fire release.yml against the wrong/old workflow before the deliberate v3.0.1 tag"
    - "PIPE-01 / SC1: the most recent ci.yml run on master shows GREEN on Windows, macOS, and Linux jobs at Node 20.x; any platform-specific failure is diagnosed and resolved with the linked CI run ID before proceeding"
    - "D-03: npmjs.com trusted-publisher config is set per-package BEFORE the tag — for BOTH @localground/mcp and @localground/cli: owner Technically-A-Mechanical-Engineer, repo localground, workflow release.yml (case-sensitive filename), AND Allowed actions: npm publish (review H1 — npm's required field; configs created after 2026-05-20 require explicitly selecting ≥1 action; a mis-set action causes a publish-time failure → the partial-publish state the phase guards against); config confirmed saved on both packages"
  artifacts:
    - path: ".planning/phases/20-release-pipeline-validation/20-PIPELINE-LOG.md"
      provides: "CI run IDs/URLs (3-OS green) + trusted-publisher confirmation evidence (incl. Allowed actions: npm publish per package)"
      min_lines: 10
  key_links:
    - from: "git push origin master"
      to: "ci.yml run on GitHub Actions"
      via: "push to master branch trigger"
      pattern: "branches:\\s*\\[master\\]"
    - from: "npmjs.com trusted-publisher config (both packages)"
      to: "release.yml OIDC publish on the v3.0.1 tag"
      via: "owner/repo/workflow-filename match + Allowed actions: npm publish enabling OIDC token exchange"
      pattern: "release.yml"
---

<objective>
Execute the first guarded operational gate of the release sequence: commit the Wave-1 pre-flight fixes, push master to fire ci.yml, confirm CI is GREEN across all 3 OSes (PIPE-01 / SC1), and configure the npmjs.com trusted-publisher per package (D-03) — all BEFORE the version bump and the tag.

This plan is NON-AUTONOMOUS (`autonomous: false`): it pushes to a public remote, waits on external CI, and configures an external web UI. The executor runs the git/gh commands, then pauses at human-verify checkpoints for the CI-green confirmation and the per-package trusted-publisher web-UI setup (which only the npm-account owner can perform).

REPO-STATE NOTE (verified 2026-06-28, supersedes CONTEXT's "first push ever" prose): `origin/master` ALREADY EXISTS at commit 8b1eea2; local master is ~77 commits ahead. So "push master" is a catch-up push to an existing upstream branch via `git push origin master` (NOT a brand-new branch push). Remote already carries tags v1.2.0 and v2.0.0 but NOT v3.0.0 (the v3.0.0 tag is local-only — it never fired release.yml because v3.0.0 was published manually per npm/cli#8544). The push still fires ci.yml on master; the v3.0.1 tag will be the first tag to ever fire release.yml. `.planning/` history is published as-is per D-10 (owner-accepted disclosure — leave the push mechanics alone). The push must NEVER carry tags (review L1) — the local-only v3.0.0 tag must not reach the remote.

Purpose: D-10's first guard. A red CI run here is free to retry; getting CI green on the fixed code BEFORE the irreversible publish is the whole point of incremental sequencing.
Output: A green 3-OS CI run with logged run IDs and a saved per-package trusted-publisher config (incl. Allowed actions: npm publish).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-release-pipeline-validation/20-CONTEXT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the Wave-1 pre-flight fixes and push master (D-10 push gate, PIPE-01 trigger)</name>
  <read_first>
    - .planning/phases/20-release-pipeline-validation/20-CONTEXT.md (D-10 sequencing — push before bump before tag)
    - .github/workflows/ci.yml (confirm push-to-master trigger)
    - packages/mcp/package.json, packages/cli/package.json, .github/workflows/release.yml (confirm 20-01 + 20-02 edits are present and version is still 3.0.0)
  </read_first>
  <files></files>
  <action>
    Pre-check: confirm the working tree carries the 20-01 + 20-02 edits and that NO version bump has happened yet (versions still 3.0.0 — the bump is Plan 20-04, a separate post-CI-green commit per D-10). Run:
      git status --porcelain
      node -p "require('./packages/mcp/package.json').version"   # expect 3.0.0
      node -p "require('./packages/cli/package.json').version"   # expect 3.0.0

    Stage and commit the source-tree changes (the two manifests + release.yml; PROJECT.md lives under gitignored .planning/ and is NOT part of this public source commit — it's tracked separately with `git add -f` if you choose to capture it, but keep the public commit to source files only):
      git add packages/mcp/package.json packages/cli/package.json .github/workflows/release.yml
      git commit -m "build(20): release pipeline pre-flight — repository+license metadata (D-04/D-05), release.yml OIDC hardening (D-02/D-07/D-08/D-09)"

    Push the catch-up to the existing upstream with a PLAIN branch push — NEVER push tags here (review L1):
      git push origin master

    HARD CONSTRAINT (review L1): do NOT use `git push --tags`, `git push --follow-tags`, or `git push origin master --tags`. A local-only `v3.0.0` tag exists and must NOT reach the remote — pushing it would fire release.yml against the old/wrong workflow state before the deliberate v3.0.1 tag in 20-05. Only the bare `git push origin master` is permitted in this plan. Optionally confirm no tags were pushed: `git ls-remote --tags origin` should NOT list v3.0.0.

    Then surface the CI run for the human-verify checkpoint:
      gh run list --workflow=ci.yml --branch=master --limit=1
      gh run watch $(gh run list --workflow=ci.yml --branch=master --limit=1 --json databaseId -q '.[0].databaseId')   # optional live watch

    Record the run ID/URL into .planning/phases/20-release-pipeline-validation/20-PIPELINE-LOG.md (create it; remember `.planning/` needs `git add -f` if committed). Do NOT bump versions and do NOT create any tag in this plan.
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process'); const out=cp.execSync('git rev-parse --abbrev-ref HEAD').toString().trim(); if(out!=='master') throw new Error('not on master: '+out); const mcp=require('./packages/mcp/package.json').version; if(mcp!=='3.0.0') throw new Error('version already bumped — should still be 3.0.0 at push gate, got '+mcp); const remoteTags=cp.execSync('git ls-remote --tags origin').toString(); if(/refs\/tags\/v3\.0\.0(\^\{\})?\s*$|refs\/tags\/v3\.0\.0\b/m.test(remoteTags)) throw new Error('v3.0.0 tag leaked to remote — L1 violated (never push --tags)'); console.log('OK on master, version still 3.0.0, no v3.0.0 tag on remote (L1)')"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` shows the pre-flight commit message (build(20): release pipeline pre-flight ...)
    - `git rev-parse HEAD` equals `git rev-parse origin/master` after push (local master pushed to upstream)
    - the push used plain `git push origin master` — NO `--tags` / `--follow-tags` (review L1); `git ls-remote --tags origin` does NOT list v3.0.0
    - mcp + cli package versions are still 3.0.0 (no bump in this plan)
    - `gh run list --workflow=ci.yml --branch=master --limit=1` returns a run triggered by this push (run ID captured)
  </acceptance_criteria>
  <done>Pre-flight commit pushed to origin/master via a plain branch push (no tags, L1); a ci.yml run is triggered and its ID is logged; versions still 3.0.0; no tag created or pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm ci.yml is GREEN across Windows, macOS, Linux (PIPE-01 / SC1)</name>
  <read_first>
    - .planning/phases/20-release-pipeline-validation/20-CONTEXT.md (D-11 — known Windows risks already mitigated; D-10 — red is retryable)
    - .planning/phases/20-release-pipeline-validation/20-PIPELINE-LOG.md (the run ID logged in Task 1)
  </read_first>
  <action>Verify the most recent ci.yml run on master is green on all three OS jobs using the gh commands in how-to-verify; if any job is red, diagnose with `gh run view <RUN_ID> --log-failed`, fix → recommit → repush (plain `git push origin master`, never --tags per L1) → re-watch (free to retry per D-10), and proceed only once the most recent run is green on Windows + macOS + Linux. Record the green run ID + URL in 20-PIPELINE-LOG.md.</action>
  <what-built>Task 1 pushed master and triggered a ci.yml run across the 3-OS matrix (windows-latest, macos-latest, ubuntu-latest) at Node 20.x.</what-built>
  <how-to-verify>
    1. Check the run status from the terminal:
       `gh run list --workflow=ci.yml --branch=master --limit=1`
       then `gh run view <RUN_ID>` (substitute the run ID logged in 20-PIPELINE-LOG.md).
    2. Confirm ALL THREE OS jobs show conclusion `success`: Test (windows-latest), Test (macos-latest), Test (ubuntu-latest).
    3. Expected: every job green. If any job is RED, diagnose from `gh run view <RUN_ID> --log-failed`, fix the cause (a red CI run is free to retry per D-10 — fix, recommit, repush with plain `git push origin master`, re-watch), and only proceed once the MOST RECENT run is green on all three OSes. Per D-11 no Windows fixes are pre-emptively expected; the two known risks (decode.test.ts realpath canonicalization, verify-tarball.mjs spawn) are already mitigated — but `fail-fast: false` means each OS reports independently if a residual surprise appears.
    4. Record the GREEN run ID + URL in 20-PIPELINE-LOG.md with the linked run ID (SC1 requires linked CI run IDs for any platform-specific failure diagnosed/resolved).
  </how-to-verify>
  <acceptance_criteria>
    - `gh run view <RUN_ID> --json jobs -q '.jobs[].conclusion'` returns `success` for all three OS jobs
    - 20-PIPELINE-LOG.md records the green run ID + URL (and any earlier red-run diagnosis with its run ID per SC1)
  </acceptance_criteria>
  <resume-signal>Type "ci-green" once all three OS jobs show success on the most recent master run, or describe the failure to diagnose.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Configure npmjs.com trusted publisher per package (D-03 — owner-only web UI)</name>
  <read_first>
    - .planning/phases/20-release-pipeline-validation/20-CONTEXT.md (D-03 exact field values + per-package requirement; D-01 OIDC primary)
  </read_first>
  <action>In the npmjs.com web UI (owner login), add a GitHub Actions trusted publisher to BOTH @localground/mcp and @localground/cli with the exact case-sensitive values in how-to-verify (user Technically-A-Mechanical-Engineer, repo localground, workflow release.yml, environment blank) AND set the required "Allowed actions" field to `npm publish` on BOTH packages (review H1); confirm both configs are saved — including Allowed actions: npm publish — before resume and record the confirmation in 20-PIPELINE-LOG.md. This is owner-only web-UI work with no CLI/API path.</action>
  <what-built>release.yml is hardened for pure-OIDC publishing (Plan 20-02). For OIDC to authenticate at tag time, each package on npmjs.com must have a trusted-publisher config pointing at this repo's workflow AND explicitly allowing the `npm publish` action. This step has NO CLI/API path the executor can drive — it is set in the npmjs.com web UI by the package owner (the user).</what-built>
  <how-to-verify>
    For BOTH packages, in the npmjs.com web UI (logged in as the owner):
    1. Go to https://www.npmjs.com/package/@localground/mcp → Settings → "Trusted Publisher" (Publishing access).
    2. Add a GitHub Actions trusted publisher with these EXACT values (case-sensitive):
       - Organization or user: `Technically-A-Mechanical-Engineer`
       - Repository: `localground`
       - Workflow filename: `release.yml`  (the filename only, case-sensitive — NOT a path)
       - Environment: leave blank (release.yml declares no GitHub environment)
       - Allowed actions (required): select `npm publish`  (review H1 — npm requires explicitly selecting ≥1 action for configs created after 2026-05-20; choose `npm publish`, NOT stage-only. A mis-set/omitted action means the cli or mcp publish silently lacks authorization and fails at REAL publish time — exactly the partial-publish state the phase guards against.)
    3. Save. Repeat identically for https://www.npmjs.com/package/@localground/cli (same owner/repo/workflow AND Allowed actions: npm publish).
    4. Confirm the config is SAVED and visible on BOTH package settings pages — including that Allowed actions shows `npm publish` on each — before signaling resume. Both packages already exist on npm (v3.0.0 published manually), so post-hoc trusted-publisher config is possible.
    5. Record "trusted publisher configured: mcp / cli — Allowed actions: npm publish (both)" with the date in 20-PIPELINE-LOG.md.
    Note (D-01 fallback): if the v3.0.1 tag's OIDC auth later fails, the documented fallback is a granular automation token via NODE_AUTH_TOKEN + --provenance — but do NOT wire that now; OIDC is primary. (The token path also requires a release.yml patch — see 20-05's recovery runbook.)
  </how-to-verify>
  <acceptance_criteria>
    - npmjs.com @localground/mcp settings show a trusted publisher: user Technically-A-Mechanical-Engineer, repo localground, workflow release.yml
    - npmjs.com @localground/mcp settings show Allowed actions: npm publish (review H1 — hard criterion)
    - npmjs.com @localground/cli settings show the identical trusted publisher config
    - npmjs.com @localground/cli settings show Allowed actions: npm publish (review H1 — hard criterion)
    - 20-PIPELINE-LOG.md records both confirmations (owner/repo/workflow + Allowed actions: npm publish per package) with a date
  </acceptance_criteria>
  <resume-signal>Type "publisher-configured" once BOTH @localground/mcp and @localground/cli show the saved trusted-publisher config AND Allowed actions: npm publish on each.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → public GitHub master | the catch-up push exposes ~77 source commits publicly; `.planning/` history is published as-is per D-10 (owner-accepted disclosure) |
| local tag store → remote | a local-only v3.0.0 tag must not leak to the remote via a tags-push (L1) — it would fire release.yml prematurely |
| npmjs.com trusted-publisher config → OIDC token exchange | a wrong owner/repo/workflow value OR an unset/mis-set Allowed action silently disables OIDC for that package, forcing a publish-time failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Spoofing | trusted-publisher owner/repo/workflow mismatch OR missing/mis-set Allowed action | mitigate | D-03 specifies exact case-sensitive values AND Allowed actions: npm publish per package (review H1); Task 3 acceptance criteria require visual confirmation of all fields incl. Allowed actions on BOTH package pages before the tag. A mismatch or wrong action fails OIDC at REAL publish time for that package — the recovery matrix is in 20-05 (H2). |
| T-20-10 | Information Disclosure | catch-up push exposes commit + `.planning/` history publicly | accept | Intentionally public open-source repo; owner verified the identity/employer exposure has been public since v3.0.0 (incl. shipped npm source) and accepts publishing the GSD process docs (D-10). No secrets in source per project security model. |
| T-20-11 | Tampering | red CI masking a real cross-OS defect | mitigate | `fail-fast: false` reports all 3 OSes independently; Task 2 blocks on all-three-green and requires linked run IDs (SC1) for any diagnosed failure. |
| T-20-20 | Tampering / premature trigger | local-only v3.0.0 tag pushed to remote, firing release.yml early | mitigate | L1 hard constraint: only plain `git push origin master`, never `--tags`/`--follow-tags`; Task 1 verify + acceptance assert `git ls-remote --tags origin` does NOT list v3.0.0. |
</threat_model>

<verification>
- A ci.yml run exists on master from the pre-flight push; its most recent run is green on all 3 OSes (logged run ID).
- The push used plain `git push origin master` — no tags reached the remote (L1); v3.0.0 absent from `git ls-remote --tags origin`.
- Versions still 3.0.0 (bump deferred to 20-04).
- Both npm packages show the saved trusted-publisher config WITH Allowed actions: npm publish (H1).
- No tag created in this plan.
</verification>

<success_criteria>
- PIPE-01 / SC1 satisfied: most recent ci.yml run on master green on Windows + macOS + Linux at Node 20.x, with linked run IDs.
- D-10 push gate satisfied: pre-flight fixes pushed before bump/tag, via a plain branch push with no tags (L1).
- D-03 satisfied: per-package trusted-publisher config saved on both packages before the tag, INCLUDING Allowed actions: npm publish (H1).
</success_criteria>

<output>
After completion, create `.planning/phases/20-release-pipeline-validation/20-03-SUMMARY.md` (include the green CI run ID/URL and the trusted-publisher confirmation incl. Allowed actions: npm publish per package).
</output>
