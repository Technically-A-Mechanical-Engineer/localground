---
phase: 19-skill-runtime-uat
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - .planning/phases/19-skill-runtime-uat/19-UAT.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/reap.md
autonomous: false
requirements:
  - UAT-03
must_haves:
  truths:
    - "Per UAT-03 / SC3: /localground:reap invokes both localground_health_check AND localground_verify (or just health_check, which itself includes seed_markers via verify per packages/mcp/src/index.ts:637-650), produces natural-language report mapping findings to recommendations"
    - "Per D-15 (natural product flow on chained fixture): UAT-03 reaps the destination project from UAT-02 (`C:/Users/rlasalle/Projects/lg-uat-19-dest/lg-uat-fixture-19`); manifest survives migration per L-6 + 19-RESEARCH § D-15/D-16"
    - "Per skill spec (localground-reap.md step 4): traffic-light table renders 6 health checks (git integrity / placeholder files / cloud sync / path-hash / seed markers / source-target alignment) with PASS/WARN/FAIL/N/A status per check"
    - "19-UAT.md row 3 (UAT-03 → SC3) status updated to VERIFIED with evidence pointer to reap.md transcript"
  artifacts:
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/reap.md"
      provides: "UAT-03 transcript — section-anchored capture of /localground:reap run against migrated destination"
      contains: "## Tool call: localground_health_check"
  key_links:
    - from: "/localground:reap skill"
      to: "localground_health_check MCP tool"
      via: "skill invocation routes through MCP stdio JSON-RPC; tool runs 6 checks per packages/mcp/src/index.ts:529-685"
      pattern: "transcript contains '## Tool call: localground_health_check' AND 'checks' array with 6 entries in response"
    - from: "localground_health_check seed_markers check"
      to: "<dest>/lg-uat-fixture-19/.localground-seed-manifest.json"
      via: "health_check check 5 calls verify() with destination path; manifest survived migration"
      pattern: "transcript shows seed_markers status PASS with N markers verified"
---

<objective>
Execute UAT-03 — `/localground:reap` end-to-end against the destination project from UAT-02. Validates that the migrate skill's output (a complete copy + surviving seed manifest at the destination) flows correctly into the reap skill's 6-check health assessment, producing a traffic-light report.

This is the **integration check** between UAT-02 (migrate) and UAT-03 (reap) — if migrate corrupted the manifest, reap's seed_markers check catches it. Per CONTEXT D-15, this is the natural product flow.

Output: `19-transcripts/reap.md` with section anchors; updated 19-UAT.md row 3 status VERIFIED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-skill-runtime-uat/19-CONTEXT.md
@.planning/phases/19-skill-runtime-uat/19-RESEARCH.md
@.planning/phases/19-skill-runtime-uat/19-02-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-UAT.md
@.claude/skills/localground-reap.md

<interfaces>
<!-- Key contracts the executor needs. -->

From `.claude/skills/localground-reap.md`:
- Step 1: calls `localground_detect`
- Step 2: calls `localground_health_check` with `projectPath` (required) + optional `sourcePath` + optional `manifestPath`
- Step 3 (optional): calls `localground_verify` if seed_markers status is N/A or FAIL
- Step 4: presents traffic-light table per the example format
- Step 5: natural-language interpretation (GREEN / YELLOW / RED)

From `packages/mcp/src/index.ts:529-685` (health_check tool — 6 checks):
1. git integrity
2. placeholder files
3. cloud sync status
4. path-hash validity
5. seed markers (calls verify() internally)
6. source/target alignment (only if sourcePath provided)

Status values per check: `PASS`, `WARN`, `FAIL`, `N/A`.

Overall status: `GREEN` (all PASS), `YELLOW` (any WARN), `RED` (any FAIL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute /localground:reap on UAT-02 destination project + capture transcript</name>
  <read_first>
    - .claude/skills/localground-reap.md (full skill spec)
    - .planning/phases/19-skill-runtime-uat/19-02-SUMMARY.md (the destination path UAT-02 produced)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "D-15/D-16" (manifest-survives-migration property)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-transcripts/reap.md (NEW)
  </files>
  <action>
    **Required shell: Git Bash (MINGW64)** — uses `$(node -e ...)`, `2>/dev/null`, and `&&`/`||` chaining. PowerShell users should switch to Git Bash for the shell snippets in this task. The skill invocations themselves run inside Claude Code, not the shell.

    1. The destination project from UAT-02 is at `C:/Users/rlasalle/Projects/lg-uat-19-dest/lg-uat-fixture-19/`. Confirm it exists and the seed manifest survived the migrate:
       ```bash
       test -d "C:/Users/rlasalle/Projects/lg-uat-19-dest/lg-uat-fixture-19" && echo "DEST OK" || echo "MISSING — UAT-02 issue"
       test -f "C:/Users/rlasalle/Projects/lg-uat-19-dest/lg-uat-fixture-19/.localground-seed-manifest.json" && echo "MANIFEST OK" || echo "MISSING — migrate did not preserve manifest"
       ```
       Both should print `OK`. If `MANIFEST OK` fails, this is itself a UAT-finding — capture in the transcript and note as a defect; close UAT-03 as `diagnosed` rather than continuing.

    2. Open Claude Code (any cwd; MCP server is `--scope user`). Type the following PROMPT verbatim:
       ```
       Run /localground:reap against the project at "C:/Users/rlasalle/Projects/lg-uat-19-dest/lg-uat-fixture-19". The original source path was "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19" — pass that as sourcePath so the source/target alignment check (check #6) runs.
       ```
       (Adjust paths if 19-01/19-02 used different paths — read those plans' SUMMARYs.)

       Observe:
       - Skill calls `localground_detect`, summarizes environment, confirms project path
       - Skill calls `localground_health_check` with `projectPath` + `sourcePath` (the source path enables check #6)
       - Skill receives a `checks` array of 6 entries (each with `check`, `status`, `detail`)
       - Skill renders the traffic-light table per the spec's example format
       - Skill provides natural-language interpretation: GREEN / YELLOW / RED with per-finding recommendations

    3. Capture the transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/reap.md` with these EXACT section anchors:
       ```markdown
       # UAT-03 Transcript: /localground:reap

       **Captured:** <ISO 8601 date>
       **Project under reap:** C:/Users/rlasalle/Projects/lg-uat-19-dest/lg-uat-fixture-19 (UAT-02 destination)
       **Source path passed:** C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19 (UAT-01 fixture)
       **MCP runtime:** local-dist (D-04 inner loop)

       ## Pre-run state confirmation
       <paste the two `test -f` outputs from step 1 (DEST OK / MANIFEST OK)>

       ## Skill invocation prompt
       <paste the prompt the maintainer typed>

       ## Tool call: localground_detect
       <paste tool call envelope + response>

       ## Tool call: localground_health_check
       <paste tool call envelope + response — preserve the full checks array verbatim, all 6 entries with status + detail>

       ## Skill output to user — traffic-light table
       <paste the natural-language table the skill rendered (e.g., the format from reap.md step 4 example)>

       ## Skill interpretation (GREEN/YELLOW/RED + recommendations)
       <paste the post-table natural-language interpretation>

       ## Manifest cross-check (post-run)
       <paste output of: cat "C:/Users/rlasalle/Projects/lg-uat-19-dest/lg-uat-fixture-19/.localground-seed-manifest.json" | jq '.markers | length'>
       <expected: integer >= 2 (test-file + git-tag markers)>
       ```

    4. Verify SC3 acceptance — the report MUST map findings to recommendations. From the captured transcript, confirm:
       - The `## Skill output to user — traffic-light table` section contains all 6 check rows
       - The `## Skill interpretation` section explicitly maps each non-PASS finding (if any) to an actionable next step OR confirms "GREEN — no action needed" if all pass

       If overall status is YELLOW or RED, this is **expected behavior, not a UAT failure** — the toolkit's purpose is to detect issues. The UAT criterion is "report MAPS findings to recommendations," not "all checks pass." Document whatever the actual status is in the transcript.
  </action>
  <verify>
    <automated>test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/reap.md" && grep -c "## Tool call: localground_health_check" ".planning/phases/19-skill-runtime-uat/19-transcripts/reap.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-skill-runtime-uat/19-transcripts/reap.md` exists
    - reap.md contains `## Tool call: localground_detect`, `## Tool call: localground_health_check`, `## Skill output to user — traffic-light table`, `## Skill interpretation (GREEN/YELLOW/RED + recommendations)` (grep count >=4 distinct anchors)
    - reap.md `## Tool call: localground_health_check` section captures the full response with 6 check entries (count grep for `"check":` or equivalent JSON shape inside the response yields 6)
    - reap.md `## Skill interpretation` section either reads "GREEN — no action needed" OR maps each WARN/FAIL finding to a recommendation in natural language
    - reap.md `## Manifest cross-check (post-run)` section confirms manifest markers count >= 2 (proves manifest survived migration per L-6)
  </acceptance_criteria>
  <done>UAT-03 executed end-to-end on the UAT-02 destination project. Transcript captured with section anchors. Manifest-survives-migration property confirmed via post-run cat + jq check. SC3 ("/localground:reap invokes both localground_verify and localground_health_check and produces a natural-language report mapping findings to recommendations") satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Update 19-UAT.md row 3 (UAT-03 → SC3) with VERIFIED status</name>
  <read_first>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (current state — row 3 status PENDING)
    - .planning/phases/19-skill-runtime-uat/19-transcripts/reap.md (the evidence to reference)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (UPDATE row 3 + frontmatter score)
  </files>
  <action>
    1. Use the Edit tool (NOT Write — preserve everything else) on 19-UAT.md to make these targeted changes:

    2. Update frontmatter:
       - Change `score: 2/5 truths verified` → `score: 3/5 truths verified`
       - Change `requirements_verified: 2/5` → `requirements_verified: 3/5`

    3. Update Observable Truths table row 3 (SC3). Replace the PENDING row's Status + Evidence cells:
       ```markdown
       | SC3 | <SC3 verbatim from ROADMAP — already in cell> | VERIFIED   | `19-transcripts/reap.md § Tool call: localground_health_check` (6-check response array); `§ Skill output to user — traffic-light table` (rendered natural-language report); `§ Skill interpretation` (findings mapped to recommendations); `§ Manifest cross-check (post-run)` (confirms manifest survived UAT-02 migrate per L-6) |
       ```

    4. Update Score below the table: `**Score:** 3/5 truths verified`.

    5. Append to Required Artifacts table:
       ```markdown
       | `19-transcripts/reap.md`                                                  | UAT-03 transcript — health_check tool call + 6-check response + traffic-light table + interpretation | VERIFIED   | Authored by plan 19-03 Task 1                                                                 |
       ```

    6. Append to Behavioral Spot-Checks table:
       ```markdown
       | UAT-02 manifest survived migration                       | `cat <dest>/lg-uat-fixture-19/.localground-seed-manifest.json | jq '.markers | length'` | >=2                                                 | PASS     |
       | health_check returned 6-check array                      | reap.md § Tool call: localground_health_check (response checks count) | 6                                                   | PASS     |
       | Skill produced natural-language report mapping findings  | reap.md § Skill interpretation                                          | non-empty natural language with recommendations     | PASS     |
       ```

    7. Update Requirements Coverage table row UAT-03:
       - Status: `SATISFIED`
       - Evidence: `19-transcripts/reap.md § Tool call: localground_health_check + § Skill interpretation; manifest survived UAT-02 migrate (post-run cross-check)`

    8. Update **Coverage so far:** `3/5 requirement IDs satisfied; 2 pending.`

    9. Save the file.
  </action>
  <verify>
    <automated>grep -c "score: 3/5 truths verified" ".planning/phases/19-skill-runtime-uat/19-UAT.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - 19-UAT.md frontmatter contains literal `score: 3/5 truths verified`
    - 19-UAT.md frontmatter contains literal `requirements_verified: 3/5`
    - SC3 row in Observable Truths table reads `VERIFIED` (not `PENDING`)
    - SC3 row evidence cell references `19-transcripts/reap.md` and contains at least 4 section-anchor citations
    - Required Artifacts table has new row for `19-transcripts/reap.md`
    - Requirements Coverage UAT-03 row shows `SATISFIED`
    - 19-UAT.md does NOT contain a `## Verifier Cross-Check` section (gsd-verifier territory only)
  </acceptance_criteria>
  <done>19-UAT.md row 3 updated with VERIFIED + reap.md evidence; frontmatter score 3/5. UAT-03 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reap skill → real filesystem (destination project) | read-only operations: git status, file scan, manifest read |
| `localground_health_check` → optional source path comparison | source path provided enables check #6 (source/target alignment); read-only on source as well |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Information Disclosure | reap report contains full destination path + git commit hash | accept | Per CONTEXT D-08, project paths and git metadata are non-sensitive. Transcript captures these intentionally. |
| T-19-08 | Tampering | reap is read-only — no write surface | accept | health_check + verify are read-only by spec; no mitigation needed. |
</threat_model>

<verification>
After Task 2, 19-UAT.md must show `score: 3/5 truths verified` and `requirements_verified: 3/5`. The reap.md transcript proves SC3's two-tool interaction (`localground_health_check` carries the seed_markers check internally per index.ts:637-650, satisfying the "invokes both" wording in SC3) and the natural-language report mapping.
</verification>

<success_criteria>
- reap.md exists with section anchors for both tool calls + traffic-light table + interpretation
- Manifest cross-check confirms UAT-02 manifest preserved through migration
- 19-UAT.md row 3 status VERIFIED with reap.md evidence pointer
- Frontmatter score: 3/5 truths verified
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-runtime-uat/19-03-SUMMARY.md` per the template.
</output>
