---
phase: 19-skill-runtime-uat
plan: 06
type: execute
wave: 6
depends_on: [19-05]
files_modified:
  - .planning/phases/19-skill-runtime-uat/19-UAT.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-seed.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-1.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-2.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-reap.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-cleanup.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-verify.md
autonomous: false
requirements:
  - UAT-01
  - UAT-02
  - UAT-03
  - UAT-04
  - UAT-05
must_haves:
  truths:
    - "Per D-04 (verbatim gating pass): npm pack the mcp tarball, install into a fresh os.tmpdir() install dir with --ignore-scripts, swap MCP server registration to point at the tarball-installed dist/index.js (with --scope user per L-1), then REPLAY the full UAT-01..UAT-05 suite against the tarball-installed runtime to verify the published tarball shape works end-to-end."
    - "UAT-02 tarball replay covers Run 1 (happy path) only — Runs 2 (idempotency) and 3 (missing-state-file fallback) already proved out under local-dist (plan 19-02) and depend on skill logic, not the runtime path. Run 1 alone exercises the two-session boundary on tarball runtime, which is the load-bearing thing the swap validates."
    - "Per L-5 (seed refuses on already-seeded fixture): tarball replay uses a FRESH fixture chain (`lg-uat-fixture-19-tarball/` under OneDrive + `lg-uat-19-dest-tarball/` under Projects) — the local-dist chain from 19-01..19-03 is already seeded and migrated. UAT-04 builds its own synthetic tmpdir fixture (per D-11/D-14); UAT-05 needs no fixture. This is the 'one extra fixture cycle' the user accepted in the BLOCKER framing."
    - "Per L-1 mitigation: tarball registration uses `--scope user` (not local). Per L-2: remove → add swap procedure for both registration directions (local→tarball at start, tarball→local at end)."
    - "Post-replay: re-register the MCP server back to local-dist (`packages/mcp/dist/index.js`) so future iterative work uses the inner-loop runtime (D-04)."
    - "19-UAT.md SC1..SC5 evidence cells updated to distinguish local-dist evidence from tarball-runtime evidence (format: `local: 19-transcripts/<skill>.md § X / tarball: 19-transcripts/tarball-<skill>.md § Y`); Required Artifacts table gets 6 new tarball transcript rows; Behavioral Spot-Checks table gets pack/install/register/replay-cycle rows."
  artifacts:
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-seed.md"
      provides: "UAT-01 tarball replay — /localground:seed against tarball-installed runtime + fresh OneDrive fixture"
      contains: "## Tool call: localground_seed (tarball runtime)"
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-1.md"
      provides: "UAT-02 Run 1 Session 1 tarball replay — detect + copy + verify + state-write against tarball-installed runtime"
      contains: "## Tool call: localground_copy (tarball runtime)"
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-2.md"
      provides: "UAT-02 Run 1 Session 2 tarball replay — state-read + settings + state-update against tarball-installed runtime"
      contains: "## State file (post-Session-2, tarball runtime)"
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-reap.md"
      provides: "UAT-03 tarball replay — /localground:reap against tarball-installed runtime + tarball-chain destination"
      contains: "## Tool call: localground_health_check (tarball runtime)"
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-cleanup.md"
      provides: "UAT-04 tarball replay — /localground:cleanup yes/no/skip-all against tarball-installed runtime + synthetic fixture"
      contains: "## Tool call: localground_cleanup_scan (tarball runtime)"
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-verify.md"
      provides: "UAT-05 tarball replay — /localground:verify environment audit against tarball-installed runtime"
      contains: "## Tool call: localground_audit (tarball runtime)"
  key_links:
    - from: "npm pack <packages/mcp> → dist tarball"
      to: "fresh os.tmpdir() install dir"
      via: "npm install <tarball> --ignore-scripts (mirrors verify-tarball.mjs install-dir lifecycle per CONTEXT § code_context)"
      pattern: "tarball file count = 5 (per Phase 18 evidence); install dir contains node_modules/@localground/mcp/dist/index.js"
    - from: "claude mcp add localground --scope user -- node <tarball-install-dir>/node_modules/@localground/mcp/dist/index.js"
      to: "registered MCP server at tarball-install path"
      via: "remove → add swap procedure (per L-2)"
      pattern: "claude mcp list shows localground entry pointing at the tarball-install path"
    - from: "All 5 /localground:* skill invocations against tarball runtime"
      to: "6 tarball-* transcript files with per-skill section anchors"
      via: "skills route through stdio JSON-RPC to tarball-installed binary; same dialogue as local-dist runs but binary path is the tarball-install"
      pattern: "each tarball-<skill>.md transcript contains the same load-bearing section anchors as its local-dist counterpart"
---

<objective>
Execute the **D-04 gating pass** verbatim — `npm pack` the mcp package, install the tarball into a fresh `os.tmpdir()` install dir with `--ignore-scripts`, swap MCP server registration to point at the tarball-installed binary (with `--scope user` per L-1), and **replay the full UAT-01..UAT-05 suite** against the tarball-installed runtime to verify the published tarball shape works end-to-end.

This is the "full UAT suite once" gating pass per CONTEXT D-04. Per the BLOCKER resolution: UAT-02 tarball replay covers **Run 1 (happy path) only** — Runs 2 (idempotency) and 3 (missing-state-file fallback) already proved out under local-dist in plan 19-02 and depend on skill logic, not the runtime path. The two-session continuation-token boundary on tarball runtime is fully exercised by Run 1.

**Fixture story (load-bearing inference per L-5):** the local-dist chain from 19-01..19-03 left `lg-uat-fixture-19/` seeded and `lg-uat-19-dest/` migrated. `/localground:seed` refuses on an already-seeded fixture (L-5). The tarball replay therefore uses a **fresh fixture chain** under different paths:
- UAT-01 source: `C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball/` (NEW)
- UAT-02 destination: `C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/` (NEW)
- UAT-03 reap target: the tarball destination above
- UAT-04 fixture: NEW synthetic tmpdir per D-11/D-14 (cannot reuse the local-dist UAT-04 fixture; cleanup may have edited it or it was deleted)
- UAT-05: no fixture (environment-wide audit)

This is the "one extra fixture cycle" the user accepted in the BLOCKER framing.

Steps:
1. `npm pack` mcp + install tarball into `os.tmpdir()` install dir with `--ignore-scripts`
2. Swap MCP server registration from local-dist → tarball-install (--scope user per L-1; remove→add swap per L-2)
3. Replay UAT-01 (seed) on fresh tarball fixture
4. Replay UAT-02 Run 1 (migrate Session 1 + Session 2) on tarball fixture chain
5. Replay UAT-03 (reap) on tarball destination
6. Replay UAT-04 (cleanup) on fresh synthetic tmpdir fixture
7. Replay UAT-05 (verify) environment-wide
8. Restore MCP server registration to local-dist + cleanup tarball install dir
9. Update `19-UAT.md` with tarball-runtime evidence pointers (local/tarball split per SC row)

Output: 6 `tarball-*.md` transcripts; updated 19-UAT.md with full per-SC tarball evidence; MCP registration restored to local-dist for any post-UAT iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-skill-runtime-uat/19-CONTEXT.md
@.planning/phases/19-skill-runtime-uat/19-RESEARCH.md
@.planning/phases/18-packaging-polish/18-VERIFICATION.md
@.planning/phases/19-skill-runtime-uat/19-UAT.md
@.planning/phases/19-skill-runtime-uat/19-01-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-02-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-03-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-04-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-05-SUMMARY.md
@scripts/verify-tarball.mjs
@.claude/skills/localground-seed.md
@.claude/skills/localground-migrate.md
@.claude/skills/localground-reap.md
@.claude/skills/localground-cleanup.md
@.claude/skills/localground-verify.md

<interfaces>
From `scripts/verify-tarball.mjs` (Phase 18 reference impl per CONTEXT § code_context):
- Pattern: `npm pack` → mkdtemp under `os.tmpdir()` → `npm install <tarball> --ignore-scripts` → invoke binary
- Note: verify-tarball.mjs uses `process.execPath + npm-cli.js` resolution to dodge the Windows EINVAL bug (memory `feedback_windows_npm_spawn.md`). The maintainer's interactive shell (`npm` typed manually) is a different code path and does not hit EINVAL.

From `claude mcp add --help` (per L-1, L-2):
- `-s, --scope <scope>` defaults to `local`; MUST use `user`
- No `--force`/`--update` flag — must `remove` before `add` (per L-2)

UAT scenarios re-applied against tarball runtime:
- UAT-01 (seed): identical to plan 19-01 Task 2, but on fresh `lg-uat-fixture-19-tarball/` fixture
- UAT-02 Run 1 only (migrate): identical to plan 19-02 Task 1 (Session 1 + Session 2), but source = `lg-uat-fixture-19-tarball`, destination = `lg-uat-19-dest-tarball`
- UAT-03 (reap): identical to plan 19-03 Task 1, but project = `lg-uat-19-dest-tarball/lg-uat-fixture-19-tarball`
- UAT-04 (cleanup): identical to plan 19-04 Tasks 1+2, but fresh synthetic tmpdir fixture (`lg-uat-cleanup-fixture-19-tarball/`)
- UAT-05 (verify): identical to plan 19-05 Task 1 — environment-wide audit, no fixture
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: npm pack mcp tarball + install into fresh tmpdir + swap MCP server registration to tarball-install path</name>
  <read_first>
    - .planning/phases/18-packaging-polish/18-VERIFICATION.md (tarball file shape — 5 files; --version short-circuit baseline)
    - scripts/verify-tarball.mjs (install-dir lifecycle pattern reference)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "L-1", § "L-2" (--scope user, remove-then-add swap)
  </read_first>
  <files>
    - <os.tmpdir>/lg-uat-19-tarball-install/ (NEW — install dir for tarball; cleaned up in Task 4)
  </files>
  <action>
    **Required shell: Git Bash (MINGW64)** — uses `$(node -e ...)` command substitution, `2>/dev/null` redirection, and forward-slash path arguments. PowerShell users should switch to Git Bash for this plan; equivalent PowerShell commands are not provided because evidence integrity here is exit-status + file-existence checks, not shell-specific output formatting.

    1. From the repo root, build mcp + pack tarball:
       ```bash
       cd "C:/Users/rlasalle/Projects/localground"
       npm run build -w @localground/mcp
       cd packages/mcp
       npm pack
       ls -la *.tgz
       ```
       Expected: `localground-mcp-3.0.0.tgz` (or current version) created in `packages/mcp/`. Note the absolute path of the tgz file.

       Note: `cd` is used here because the operations are user-shell, not Node-spawn — this is fine for manual UAT (the spawn-discipline rules apply to Node code, not interactive maintainer commands).

    2. Create a fresh install dir under tmpdir + install the tarball:
       ```bash
       INSTALL_DIR="$(node -e 'console.log(require(\"os\").tmpdir())')/lg-uat-19-tarball-install"
       rm -rf "$INSTALL_DIR" 2>/dev/null
       mkdir -p "$INSTALL_DIR"
       cd "$INSTALL_DIR"
       npm init -y
       npm install --ignore-scripts "C:/Users/rlasalle/Projects/localground/packages/mcp/localground-mcp-3.0.0.tgz"
       ls "$INSTALL_DIR/node_modules/@localground/mcp/dist/index.js"
       ```
       (Adjust tgz filename if version differs.) Expected: `dist/index.js` exists under the installed package. The `--ignore-scripts` flag mirrors verify-tarball.mjs's T-18-05 mitigation.

    3. Sanity check the tarball-installed binary boots clean on `--version`:
       ```bash
       node "$INSTALL_DIR/node_modules/@localground/mcp/dist/index.js" --version
       ```
       Expected: semver to stdout, exit 0, no "running on stdio" stderr (per Phase 18 SC4 baseline).

    4. Swap MCP server registration from local-dist to tarball-install path (per L-1: `--scope user`; per L-2: remove → add):
       ```bash
       INSTALL_DIR="$(node -e 'console.log(require(\"os\").tmpdir())')/lg-uat-19-tarball-install"
       claude mcp remove localground 2>/dev/null
       claude mcp add localground --scope user -- node "$INSTALL_DIR/node_modules/@localground/mcp/dist/index.js"
       claude mcp list
       ```
       Expected: `claude mcp list` shows `localground` pointing at the tarball-install path. If it still shows the local-dist path, the swap didn't apply — re-run remove+add.

    5. Verify cross-cwd visibility (L-1 acceptance):
       ```bash
       cd "C:/Users/rlasalle"
       claude mcp list
       cd "C:/Users/rlasalle/Projects/localground"
       ```
       Expected: localground appears in BOTH listings, both pointing at the tarball-install path.

    6. Note both paths for the cleanup step at end of Task 4:
       - Local-dist path: `C:/Users/rlasalle/Projects/localground/packages/mcp/dist/index.js`
       - Tarball-install path: `<tmpdir>/lg-uat-19-tarball-install/node_modules/@localground/mcp/dist/index.js`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),os=require('os'),p=require('path');const f=p.join(os.tmpdir(),'lg-uat-19-tarball-install','node_modules','@localground','mcp','dist','index.js');console.log(fs.existsSync(f)?'OK':'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - Tarball file `packages/mcp/localground-mcp-*.tgz` exists after `npm pack`
    - Install dir at `<tmpdir>/lg-uat-19-tarball-install/node_modules/@localground/mcp/dist/index.js` exists
    - `node <install-dir>/node_modules/@localground/mcp/dist/index.js --version` exits 0 with semver stdout
    - `claude mcp list` shows `localground` pointing at the tarball-install path (NOT the local-dist path)
    - `claude mcp list` from a cwd outside the repo still shows localground (--scope user verification — same as 19-01)
  </acceptance_criteria>
  <done>Tarball packed, installed under tmpdir, MCP server registration swapped to tarball-install path with --scope user. Ready for full UAT-01..UAT-05 replay against tarball runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Tarball replay UAT-01 (seed) + UAT-02 Run 1 (migrate Session 1 + Session 2) on fresh fixture chain</name>
  <read_first>
    - .planning/phases/19-skill-runtime-uat/19-01-PLAN.md Task 2 (UAT-01 procedure — replay verbatim against tarball runtime)
    - .planning/phases/19-skill-runtime-uat/19-02-PLAN.md Task 1 (UAT-02 Run 1 procedure — replay verbatim; SKIP Runs 2 + 3 per BLOCKER framing)
    - .claude/skills/localground-seed.md
    - .claude/skills/localground-migrate.md
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "L-5" (seed refuses on already-seeded fixture — fresh fixture required)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "L-7" (state file at destination BASE path)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-seed.md (NEW)
    - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-1.md (NEW)
    - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-2.md (NEW)
  </files>
  <action>
    **Required shell: Git Bash (MINGW64)** — uses `git`, `for i in $(seq ...)`, `jq`, `cat`, `&&`/`||` chaining, `2>/dev/null`. The skill invocations themselves run inside Claude Code, not the shell, but the fixture creation + on-disk evidence capture commands require Git Bash.

    1. **UAT-01 tarball replay — Build fresh OneDrive fixture (DIFFERENT path from 19-01 per L-5):**
       ```bash
       FIXTURE_TB="C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball"
       mkdir -p "$FIXTURE_TB"
       cd "$FIXTURE_TB"
       git init
       echo "# UAT-19 tarball-runtime fixture" > README.md
       mkdir -p src docs
       for i in $(seq 1 12); do echo "file $i" > "src/file-$i.txt"; done
       for i in $(seq 1 5); do echo "doc $i" > "docs/doc-$i.md"; done
       git add .
       git commit -m "initial commit"
       echo "second" >> README.md
       git add README.md
       git commit -m "second commit"
       echo "third" >> README.md
       git add README.md
       git commit -m "third commit"
       ls .localground-seed-test 2>/dev/null && echo "ALREADY SEEDED — abort, fixture path collision" || echo "FRESH OK"
       ```
       Expected last line: `FRESH OK`. If `ALREADY SEEDED`, choose a different fixture name (e.g., `lg-uat-fixture-19-tarball-2`) and retry.

    2. **UAT-01 tarball replay — Run /localground:seed.** Open Claude Code from any cwd. Type verbatim:
       ```
       Run /localground:seed against the project at "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball".
       ```
       Observe: skill calls `localground_detect` then `localground_seed`; manifest written to fixture root; git tag created.

    3. Capture transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-seed.md`:
       ```markdown
       # UAT-01 Tarball Replay: /localground:seed (D-04 gating pass)

       **Captured:** <ISO 8601 date>
       **Fixture:** C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball
       **MCP runtime:** TARBALL-INSTALL — `<tmpdir>/lg-uat-19-tarball-install/node_modules/@localground/mcp/dist/index.js`
       **Registered with:** `claude mcp add localground --scope user -- node <tarball-install-path>`

       ## Pre-run: confirm tarball binary is registered
       ```bash
       claude mcp list
       node <tarball-install-path> --version
       ```
       <paste outputs>

       ## Skill invocation prompt
       <paste the prompt>

       ## Tool call: localground_detect (tarball runtime)
       <paste tool call envelope + response>

       ## Tool call: localground_seed (tarball runtime)
       <paste tool call envelope + response>

       ## Skill output to user
       <paste natural-language summary>

       ## On-disk evidence (post-run)
       <paste output of: cat "<fixture>/.localground-seed-manifest.json">
       <paste output of: git tag --list 'lg-seed-*' from inside fixture>
       ```

    4. **UAT-02 Run 1 tarball replay — Session 1.** Per BLOCKER framing, only Run 1 happy path is replayed (Runs 2/3 skipped — already proved under local-dist in plan 19-02). Set up destination:
       ```bash
       mkdir -p "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball"
       ```
       Open Claude Code (any cwd; tarball-runtime registration persists via --scope user). Type verbatim:
       ```
       Run /localground:migrate. The project to migrate is "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball" (the fresh tarball-runtime fixture seeded in this plan). Migrate it to "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball".
       ```
       Confirm at each prompt — same flow as plan 19-02 Task 1 step 2:
       - localground_detect → project list → confirm `lg-uat-fixture-19-tarball`
       - destination → confirm `lg-uat-19-dest-tarball`
       - copy → yes
       - localground_copy continuation-token loop runs
       - localground_verify confirms PASS
       - State file written to `C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/localground-migrate-state.json` (BASE path per L-7)
       - Skill prints handoff message

    5. Capture Session 1 transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-1.md` with the same EXACT section anchors as plan 19-02's `migrate-session-1.md`:
       ```markdown
       # UAT-02 Run 1 Session 1 Tarball Replay: /localground:migrate (D-04 gating pass)

       **Captured:** <ISO 8601 date>
       **Source:** C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball
       **Destination:** C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball
       **MCP runtime:** TARBALL-INSTALL

       ## Skill invocation prompt
       <paste>

       ## Tool call: localground_detect (tarball runtime)
       <paste>

       ## Project / destination confirmation dialogue
       <paste>

       ## Tool call: localground_copy (chunk 1) (tarball runtime)
       <paste>

       ## Tool call: localground_copy (chunk N) (tarball runtime)
       <repeat per chunk if multi-chunk>

       ## Tool call: localground_verify (tarball runtime)
       <paste>

       ## State file (post-Session-1, tarball runtime)
       <paste output of: cat "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/localground-migrate-state.json">

       ## Skill output to user (handoff message)
       <paste>
       ```

    6. Verify state file at BASE path (L-7):
       ```bash
       test -f "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/localground-migrate-state.json" && echo "STATE FILE OK at base path" || echo "MISSING — defect"
       jq -r '.session' "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/localground-migrate-state.json"
       ```
       Expected: `STATE FILE OK at base path` and `1`. Append to transcript.

    7. **UAT-02 Run 1 tarball replay — Session 2.** Restart Claude Code at destination:
       ```bash
       cd "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball"
       claude
       ```
       At new prompt, type:
       ```
       Run /localground:migrate
       ```
       Skill detects state.session === 1 → enters Session 2; reads state, completes settings migration, updates state to session: 2 + completedTimestamp.

    8. Capture Session 2 transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-2.md`:
       ```markdown
       # UAT-02 Run 1 Session 2 Tarball Replay: /localground:migrate (D-04 gating pass)

       **Captured:** <ISO 8601 date>
       **CWD:** C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball
       **MCP runtime:** TARBALL-INSTALL

       ## Skill invocation prompt
       <paste>

       ## State file read confirmation (tarball runtime)
       <paste>

       ## Migration summary report
       <paste>

       ## Settings migration scan
       <paste>

       ## State file (post-Session-2, tarball runtime)
       <paste output of: cat "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/localground-migrate-state.json">

       ## Skill output to user (post-migration reminders)
       <paste>
       ```

    9. Verify state file post-Session-2:
       ```bash
       jq -r '.session, .completedTimestamp' "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/localground-migrate-state.json"
       ```
       Expected: `2` and a non-null ISO 8601 timestamp.
  </action>
  <verify>
    <automated>test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-seed.md" && test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-1.md" && test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-2.md" && grep -c "## Tool call: localground_seed (tarball runtime)" ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-seed.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-seed.md` exists with `## Tool call: localground_seed (tarball runtime)` and `## On-disk evidence (post-run)` anchors
    - File `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-1.md` exists with `## Tool call: localground_copy (chunk 1) (tarball runtime)`, `## Tool call: localground_verify (tarball runtime)`, `## State file (post-Session-1, tarball runtime)` anchors
    - File `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-migrate-session-2.md` exists with `## State file (post-Session-2, tarball runtime)` anchor
    - On disk: `<fixture-tarball>/.localground-seed-manifest.json` exists with all 6 schema keys (UAT-01 evidence)
    - On disk: `C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/localground-migrate-state.json` exists; jq shows `.session === 2` and `.completedTimestamp` non-null (UAT-02 evidence)
    - On disk: `C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/lg-uat-fixture-19-tarball/` exists with copied content (smoke check that copy actually happened on tarball runtime)
    - All tool call envelope responses captured with `isError: false` (or recorded as defect if any failed)
  </acceptance_criteria>
  <done>UAT-01 + UAT-02 Run 1 replayed against tarball runtime. Seed + migrate (full two-session boundary) work end-to-end on the published tarball shape. Three transcripts captured.</done>
</task>

<task type="auto">
  <name>Task 3: Tarball replay UAT-03 (reap) + UAT-04 (cleanup) + UAT-05 (verify)</name>
  <read_first>
    - .planning/phases/19-skill-runtime-uat/19-03-PLAN.md Task 1 (UAT-03 procedure — replay verbatim against tarball runtime)
    - .planning/phases/19-skill-runtime-uat/19-04-PLAN.md Tasks 1+2 (UAT-04 procedure — fresh synthetic fixture per D-11/D-14)
    - .planning/phases/19-skill-runtime-uat/19-05-PLAN.md Task 1 (UAT-05 procedure — environment-wide audit)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "L-8", § "L-11"
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-reap.md (NEW)
    - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-cleanup.md (NEW)
    - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-verify.md (NEW)
  </files>
  <action>
    **Required shell: Git Bash (MINGW64)** — uses `$(node -e ...)`, `md5sum`, `printf`, `cat | jq`, `2>/dev/null`. The skill invocations run in Claude Code; only fixture setup + on-disk evidence capture require the shell.

    1. **UAT-03 tarball replay — /localground:reap on tarball destination project.** Confirm preconditions:
       ```bash
       test -d "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/lg-uat-fixture-19-tarball" && echo "DEST OK" || echo "MISSING — UAT-02 tarball replay issue"
       test -f "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/lg-uat-fixture-19-tarball/.localground-seed-manifest.json" && echo "MANIFEST OK" || echo "MISSING — migrate did not preserve manifest under tarball runtime"
       ```
       Both should print `OK`. If `MANIFEST OK` fails, that itself is a tarball-gate finding — capture and mark UAT-03 tarball replay as `diagnosed`.

    2. Open Claude Code (any cwd). Type verbatim:
       ```
       Run /localground:reap against the project at "C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/lg-uat-fixture-19-tarball". The original source path was "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball" — pass that as sourcePath.
       ```
       Observe: localground_detect → localground_health_check (6-check array) → traffic-light table → interpretation.

    3. Capture transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-reap.md`:
       ```markdown
       # UAT-03 Tarball Replay: /localground:reap (D-04 gating pass)

       **Captured:** <ISO 8601 date>
       **Project under reap:** C:/Users/rlasalle/Projects/lg-uat-19-dest-tarball/lg-uat-fixture-19-tarball
       **Source path passed:** C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19-tarball
       **MCP runtime:** TARBALL-INSTALL

       ## Pre-run state confirmation
       <paste DEST OK / MANIFEST OK outputs>

       ## Skill invocation prompt
       <paste>

       ## Tool call: localground_detect (tarball runtime)
       <paste>

       ## Tool call: localground_health_check (tarball runtime)
       <paste — preserve full 6-check response array>

       ## Skill output to user — traffic-light table
       <paste>

       ## Skill interpretation (GREEN/YELLOW/RED + recommendations)
       <paste>

       ## Manifest cross-check (post-run, tarball runtime)
       <paste output of: cat "<dest-tarball>/lg-uat-fixture-19-tarball/.localground-seed-manifest.json" | jq '.markers | length'>
       <expected: integer >= 2>
       ```

    4. **UAT-04 tarball replay — Build fresh synthetic fixture under tmpdir (D-11/D-14):**
       ```bash
       FIXTURE_C="$(node -e 'console.log(require(\"os\").tmpdir())')/lg-uat-cleanup-fixture-19-tarball"
       mkdir -p "$FIXTURE_C/.claude/memory"
       printf '# Synthetic UAT-04 tarball-replay fixture\nActive project path: C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Projects/stale-project-1\nThis is the line cleanup-scan should match.\n' > "$FIXTURE_C/CLAUDE.md"
       printf 'legacy_path=C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Projects/stale-project-2\n' > "$FIXTURE_C/.clauderc"
       printf 'Old project location: C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Projects/stale-project-3\n' > "$FIXTURE_C/.claude/memory/note.md"
       md5sum "$FIXTURE_C/CLAUDE.md" "$FIXTURE_C/.clauderc" "$FIXTURE_C/.claude/memory/note.md" > "$FIXTURE_C/pre-run-checksums.txt"
       cat "$FIXTURE_C/pre-run-checksums.txt"
       echo "$FIXTURE_C"
       ```
       Note the fixture path — paste it into the Claude Code prompt below.

    5. **UAT-04 tarball replay — Run /localground:cleanup with mixed yes/no/skip-all.** Open Claude Code; type verbatim (substitute the actual fixture path):
       ```
       Run /localground:cleanup against the directory "<absolute-path-to-tarball-cleanup-fixture>". I will respond to each candidate one at a time.
       ```
       Respond: `yes` to first candidate, `no` to second, `skip all` to third (verbatim per L-8).

    6. Capture transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-cleanup.md`:
       ```markdown
       # UAT-04 Tarball Replay: /localground:cleanup (D-04 gating pass)

       **Captured:** <ISO 8601 date>
       **Fixture:** <tmpdir>/lg-uat-cleanup-fixture-19-tarball
       **MCP runtime:** TARBALL-INSTALL

       ## Pre-run state (3 files with cloud-path refs)
       <paste pre-run-checksums.txt>

       ## Skill invocation prompt
       <paste>

       ## Tool call: localground_cleanup_scan (tarball runtime)
       <paste>

       ## Candidate 1 dialogue (response: yes) (tarball runtime)
       <paste>

       ## Candidate 2 dialogue (response: no) (tarball runtime)
       <paste>

       ## Candidate 3 dialogue (response: skip all) (tarball runtime)
       <paste — verbatim "skip all" per L-8>

       ## Skill output to user — Step 4 summary
       <paste>

       ## Post-run state verification (tarball runtime)
       ```bash
       md5sum "$FIXTURE_C/CLAUDE.md" "$FIXTURE_C/.clauderc" "$FIXTURE_C/.claude/memory/note.md" > "$FIXTURE_C/post-run-checksums.txt"
       diff "$FIXTURE_C/pre-run-checksums.txt" "$FIXTURE_C/post-run-checksums.txt"
       ```
       <paste outputs — expected: exactly 1 line different (the YES file)>
       ```

    7. Run the post-run checksum diff:
       ```bash
       FIXTURE_C="$(node -e 'console.log(require(\"os\").tmpdir())')/lg-uat-cleanup-fixture-19-tarball"
       md5sum "$FIXTURE_C/CLAUDE.md" "$FIXTURE_C/.clauderc" "$FIXTURE_C/.claude/memory/note.md" > "$FIXTURE_C/post-run-checksums.txt"
       diff "$FIXTURE_C/pre-run-checksums.txt" "$FIXTURE_C/post-run-checksums.txt"
       ```
       Expected: exactly 1 line different. If more, that's a tarball-gate SC4 failure → mark `diagnosed`.

    8. **UAT-05 tarball replay — /localground:verify environment audit.** Open Claude Code (any cwd). Type verbatim:
       ```
       Run /localground:verify to audit my entire Claude Code environment.
       ```
       Skill calls localground_audit (no projectPaths arg), renders traffic-light table per project + overall summary + recommendations.

    9. Capture transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/tarball-verify.md`:
       ```markdown
       # UAT-05 Tarball Replay: /localground:verify (D-04 gating pass)

       **Captured:** <ISO 8601 date>
       **Mode:** auto-discovery (no projectPaths argument)
       **MCP runtime:** TARBALL-INSTALL

       ## Skill invocation prompt
       <paste>

       ## Tool call: localground_audit (tarball runtime)
       <paste — preserve summary + projects array>

       ## Skill output to user — traffic-light table per project
       <paste>

       ## Skill output to user — overall summary
       <paste>

       ## Skill recommendations
       <paste — each WARN/FAIL mapped to next step OR GREEN no-action>

       ## Auto-discovery filter check (L-11 known reading, tarball runtime)
       <note any expected paths excluded by looksLikeProject; correct behavior, not a defect>
       ```
  </action>
  <verify>
    <automated>test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-reap.md" && test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-cleanup.md" && test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-verify.md" && grep -c "## Tool call: localground_health_check (tarball runtime)" ".planning/phases/19-skill-runtime-uat/19-transcripts/tarball-reap.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - File `tarball-reap.md` exists with `## Tool call: localground_health_check (tarball runtime)` + `## Skill interpretation (GREEN/YELLOW/RED + recommendations)` + `## Manifest cross-check (post-run, tarball runtime)` anchors
    - tarball-reap.md `## Tool call: localground_health_check` response shows 6-check array
    - File `tarball-cleanup.md` exists with `## Tool call: localground_cleanup_scan (tarball runtime)` + 3 candidate dialogue anchors (yes / no / skip all) + `## Post-run state verification (tarball runtime)` anchor
    - tarball-cleanup.md `## Candidate 3` section captures literal `skip all` (verbatim per L-8)
    - tarball-cleanup.md post-run checksum diff shows exactly 1 line different (the YES file)
    - File `tarball-verify.md` exists with `## Tool call: localground_audit (tarball runtime)` + `## Skill recommendations` + `## Auto-discovery filter check` anchors
    - tarball-verify.md `## Tool call: localground_audit` response shows `summary` + `projects` keys
    - All tool call envelopes captured with `isError: false` (or recorded as defect)
  </acceptance_criteria>
  <done>UAT-03 + UAT-04 + UAT-05 all replayed against tarball runtime. Three transcripts captured. The published tarball shape is verified end-to-end across all 5 skills, including the load-bearing two-session boundary (UAT-02 Run 1) and the safety-critical per-item confirmation gate (UAT-04 yes/no/skip-all).</done>
</task>

<task type="auto">
  <name>Task 4: Restore MCP server registration to local-dist + cleanup tarball install dir + update 19-UAT.md with local/tarball evidence split</name>
  <read_first>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (current state — all 5 SC rows VERIFIED with local-dist evidence; status TBD)
    - .planning/phases/19-skill-runtime-uat/19-transcripts/tarball-*.md (the 6 evidence files from Tasks 2 + 3)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (UPDATE — append local/tarball split to SC1..SC5 evidence cells; append Required Artifacts rows; append Behavioral Spot-Checks rows)
  </files>
  <action>
    **Required shell: Git Bash (MINGW64)** for the cleanup commands; the Edit-tool operations on 19-UAT.md are shell-agnostic.

    1. Restore the MCP server registration to local-dist (D-04 inner loop default for ongoing iteration):
       ```bash
       claude mcp remove localground 2>/dev/null
       claude mcp add localground --scope user -- node "C:/Users/rlasalle/Projects/localground/packages/mcp/dist/index.js"
       claude mcp list
       ```
       Expected: `claude mcp list` shows localground pointing at local-dist path.

    2. Optional cleanup of tarball install dir + tarball replay fixtures (saves space; auto-cleaned at reboot otherwise):
       ```bash
       rm -rf "$(node -e 'console.log(require(\"os\").tmpdir())')/lg-uat-19-tarball-install"
       rm -rf "$(node -e 'console.log(require(\"os\").tmpdir())')/lg-uat-cleanup-fixture-19-tarball"
       rm -f "C:/Users/rlasalle/Projects/localground/packages/mcp/"localground-mcp-*.tgz
       ```
       Note: leave the tarball replay fixture chain (`lg-uat-fixture-19-tarball/` + `lg-uat-19-dest-tarball/`) for post-UAT inspection if any defect was diagnosed; otherwise the maintainer can remove via the same post-UAT housekeeping operation that handles 19-01..19-03's local-dist chain (D-13 territory).

    3. Use Edit tool on 19-UAT.md to append tarball-runtime evidence to each SC1..SC5 evidence cell. Format per BLOCKER instructions: `local: <existing evidence> / tarball: 19-transcripts/tarball-<skill>.md § <anchor>`.

       Example for SC1 (UAT-01 → seed):
       - Before: `19-transcripts/seed.md § Tool call: localground_seed and § On-disk evidence (post-run)`
       - After: `local: 19-transcripts/seed.md § Tool call: localground_seed and § On-disk evidence (post-run) / tarball: 19-transcripts/tarball-seed.md § Tool call: localground_seed (tarball runtime) and § On-disk evidence (post-run)`

       Apply same pattern to SC2 (migrate), SC3 (reap), SC4 (cleanup), SC5 (verify). For SC2, the tarball portion references both `tarball-migrate-session-1.md` and `tarball-migrate-session-2.md` (Run 1 only, per BLOCKER framing). Add a short note in the SC2 cell: `(tarball replay covers Run 1 happy path only; Runs 2+3 idempotency/missing-state already proved under local-dist per plan 19-02 — depend on skill logic, not runtime path)`.

    4. Append a new section before `### Gaps Summary`:
       ```markdown
       ### Tarball-Runtime Replay (D-04 gating pass — full UAT-01..UAT-05 against tarball-installed runtime)

       Plan 19-06 packed `packages/mcp` via `npm pack`, installed the tarball into `<os.tmpdir>/lg-uat-19-tarball-install/` with `--ignore-scripts`, swapped MCP server registration to point at the tarball-installed binary (with `--scope user` per L-1), and replayed the **full UAT suite** against the tarball runtime per CONTEXT D-04. UAT-02 tarball replay covers Run 1 (happy path) only — Runs 2+3 already proved under local-dist per plan 19-02.

       | UAT     | Tarball replay scope                                                  | Transcript                                                                            | Status                |
       |---------|------------------------------------------------------------------------|---------------------------------------------------------------------------------------|-----------------------|
       | UAT-01  | /localground:seed on fresh `lg-uat-fixture-19-tarball`                 | `19-transcripts/tarball-seed.md`                                                       | <SATISFIED/diagnosed> |
       | UAT-02  | /localground:migrate Run 1 happy path on fresh tarball fixture chain   | `tarball-migrate-session-1.md` + `tarball-migrate-session-2.md`                        | <SATISFIED/diagnosed> |
       | UAT-03  | /localground:reap on `lg-uat-19-dest-tarball/lg-uat-fixture-19-tarball`| `19-transcripts/tarball-reap.md`                                                       | <SATISFIED/diagnosed> |
       | UAT-04  | /localground:cleanup yes/no/skip-all on synthetic tmpdir tarball fixture| `19-transcripts/tarball-cleanup.md`                                                   | <SATISFIED/diagnosed> |
       | UAT-05  | /localground:verify environment-wide audit                              | `19-transcripts/tarball-verify.md`                                                     | <SATISFIED/diagnosed> |

       **Verdict:** D-04 gating pass <verified / diagnosed at UAT-NN>. Phase 18's `verify-tarball.mjs` covers `--version` short-circuit + tarball file shape; this replay covers the runtime path verify-tarball.mjs cannot exercise (stdio JSON-RPC handshake under skill invocation across all 5 skills, including the load-bearing two-session boundary in UAT-02).

       **Post-replay state:** MCP server registration restored to local-dist (`packages/mcp/dist/index.js`) for ongoing iteration; tarball install dir under tmpdir cleaned up. Tarball replay fixture chain (`lg-uat-fixture-19-tarball/` source + `lg-uat-19-dest-tarball/` destination) left in place for post-UAT inspection; cleanup deferred to D-13 housekeeping.
       ```

    5. Append to Required Artifacts table (6 new rows for the tarball transcripts):
       ```markdown
       | `19-transcripts/tarball-seed.md`                                          | UAT-01 tarball replay — seed against tarball-installed runtime + fresh fixture                  | VERIFIED   | Authored by plan 19-06 Task 2                                                                 |
       | `19-transcripts/tarball-migrate-session-1.md`                             | UAT-02 Run 1 Session 1 tarball replay — copy + verify + state-write against tarball runtime    | VERIFIED   | Authored by plan 19-06 Task 2                                                                 |
       | `19-transcripts/tarball-migrate-session-2.md`                             | UAT-02 Run 1 Session 2 tarball replay — settings migration + state-update against tarball runtime| VERIFIED   | Authored by plan 19-06 Task 2                                                                 |
       | `19-transcripts/tarball-reap.md`                                          | UAT-03 tarball replay — health_check + traffic-light report against tarball runtime             | VERIFIED   | Authored by plan 19-06 Task 3                                                                 |
       | `19-transcripts/tarball-cleanup.md`                                       | UAT-04 tarball replay — cleanup_scan + 3-candidate dialogues + checksum diff against tarball runtime | VERIFIED   | Authored by plan 19-06 Task 3                                                                 |
       | `19-transcripts/tarball-verify.md`                                        | UAT-05 tarball replay — audit auto-discovery + traffic-light + recommendations against tarball runtime| VERIFIED   | Authored by plan 19-06 Task 3                                                                 |
       ```

    6. Append to Behavioral Spot-Checks table:
       ```markdown
       | Tarball pack + install lifecycle (D-04 gating pass)        | `npm pack -w @localground/mcp` + install in tmpdir with `--ignore-scripts`        | tarball file exists; install dir contains dist/index.js | PASS     |
       | Tarball binary --version short-circuits cleanly             | `node <tarball-install>/node_modules/@localground/mcp/dist/index.js --version`    | exit 0; semver to stdout                            | PASS     |
       | Registration swap to tarball-install path with --scope user | `claude mcp remove`+`claude mcp add localground --scope user -- node <tarball>`    | claude mcp list shows tarball-install path; cross-cwd visible | PASS     |
       | UAT-01 replay: seed against tarball runtime                | tarball-seed.md § Tool call: localground_seed (tarball runtime)                   | manifest written; isError=false                     | <PASS/diagnosed> |
       | UAT-02 Run 1 replay: full two-session boundary on tarball  | tarball-migrate-session-1.md + tarball-migrate-session-2.md                       | state.session=2 + completedTimestamp non-null       | <PASS/diagnosed> |
       | UAT-03 replay: reap against tarball runtime                | tarball-reap.md § Tool call: localground_health_check (tarball runtime)           | 6-check array; manifest survived                    | <PASS/diagnosed> |
       | UAT-04 replay: cleanup yes/no/skip-all against tarball     | tarball-cleanup.md § Post-run state verification (tarball runtime)                | exactly 1 file changed (the YES file)               | <PASS/diagnosed> |
       | UAT-05 replay: audit against tarball runtime                | tarball-verify.md § Tool call: localground_audit (tarball runtime)                | summary+projects keys; recommendations mapped       | <PASS/diagnosed> |
       | Registration restored to local-dist post-replay             | `claude mcp list` post-restore                                                     | localground points at packages/mcp/dist/index.js    | PASS     |
       ```

    7. The Requirements Coverage table — for each UAT-NN row, the Source Plan(s) cell already lists `19-XX, 19-06` (set up by plan 19-01). Confirm this is correct; if not, edit each cell so it reads `19-XX, 19-06`. Status remains `SATISFIED` (set by plans 19-01..19-05 from local-dist evidence) — the tarball replay augments rather than replaces those statuses.

    8. Save the file.
  </action>
  <verify>
    <automated>grep -c "Tarball-Runtime Replay (D-04 gating pass" ".planning/phases/19-skill-runtime-uat/19-UAT.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - 19-UAT.md contains a new `### Tarball-Runtime Replay (D-04 gating pass — full UAT-01..UAT-05 against tarball-installed runtime)` section header
    - Tarball-Runtime Replay section's per-UAT table has 5 rows (one per skill) with transcript references
    - Each SC1..SC5 evidence cell uses the `local: ... / tarball: ...` split format pointing to both local-dist and tarball-runtime transcripts
    - SC2 evidence cell explicitly notes `(tarball replay covers Run 1 happy path only; Runs 2+3 already proved under local-dist)`
    - Required Artifacts table has 6 new rows (one per tarball-* transcript)
    - Behavioral Spot-Checks table has 9 new rows for the tarball pack/install/register/replay/restore lifecycle
    - `claude mcp list` (verify externally) shows localground registered at local-dist path post-restore
    - Frontmatter score: 5/5 truths verified (unchanged from 19-05); status: still TBD pending 19-07 finalization
  </acceptance_criteria>
  <done>D-04 gating pass complete: full UAT-01..UAT-05 replayed against tarball runtime; 6 tarball transcripts captured; 19-UAT.md has local/tarball evidence split per SC row + Tarball-Runtime Replay section + 6 new Required Artifacts rows + 9 new Behavioral Spot-Checks rows. MCP registration restored to local-dist. Phase 19 evidence is complete except for the final status-finalization step in 19-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `npm pack` → tarball file | tarball is the v3.0.1 shipping artifact shape; integrity already confirmed by Phase 18 |
| `npm install <tarball>` → tmpdir install dir | `--ignore-scripts` mitigates lifecycle-script tampering (T-18-05) |
| `claude mcp add ... --scope user` | scope=user persists across cwds; same boundary as 19-01 |
| Tarball-runtime skill invocations → real filesystem | same trust boundaries as local-dist runs (per-UAT threat models from 19-01..19-05 apply) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-14 | Tampering | tarball install lifecycle scripts | mitigate | `--ignore-scripts` flag on `npm install` (mirrors verify-tarball.mjs T-18-05 mitigation) |
| T-19-15 | DoS | tarball install dir + tarball fixture chain not cleaned up | accept | Tmpdir is OS-managed; auto-clean at reboot. Manual cleanup in Task 4 step 2 is best-effort. Tarball replay fixture chain stays in place pending D-13 housekeeping. |
| T-19-16 | Tampering | swap procedure leaves stale registration | mitigate | Task 4 step 1 explicitly restores to local-dist + verifies via `claude mcp list` post-restore |
| T-19-18 | Tampering | tarball replay fixture chain contaminates filesystem | accept | Fixtures left in place per D-13 territory; maintainer cleans via post-UAT housekeeping (same operation that removes 19-01..19-03 local-dist chain). Synthetic UAT-04 fixture optionally cleaned in Task 4 step 2. |
| T-19-19 | Tampering | UAT-02 Run 1 only on tarball replay leaves Runs 2+3 unverified on tarball | accept | Per BLOCKER framing: Runs 2+3 (idempotency, missing-state-file) depend on skill logic (state-file branch detection in localground-migrate.md:17-21), NOT on the runtime path. Local-dist proof in plan 19-02 transitively covers tarball runtime since the skill loads identically. The two-session boundary itself IS exercised on tarball runtime via Run 1. |
</threat_model>

<verification>
After Task 4, 19-UAT.md has the Tarball-Runtime Replay section + per-SC local/tarball evidence split + 6 new Required Artifacts rows + 9 new Behavioral Spot-Checks rows. The MCP server registration is restored to local-dist for any post-UAT iterative work. The frontmatter `status:` field still reads TBD — 19-07 finalizes it after rolling up the per-UAT outcomes plus the tarball-replay verdict.
</verification>

<success_criteria>
- Tarball packed, installed under tmpdir with `--ignore-scripts`, registered with `--scope user`
- Full UAT-01..UAT-05 replayed against tarball runtime (UAT-02 Run 1 only per BLOCKER framing)
- 6 tarball-* transcripts captured with same load-bearing section anchors as their local-dist counterparts
- All tool call responses are `isError: false` (or marked `diagnosed` if any failed)
- MCP server registration restored to local-dist post-replay
- 19-UAT.md has Tarball-Runtime Replay section + per-SC local/tarball evidence split
- Tarball install dir + synthetic UAT-04 fixture cleaned up (best-effort); tarball replay fixture chain left in place pending D-13 housekeeping
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-runtime-uat/19-06-SUMMARY.md` per the template.
</output>
