---
phase: 19-skill-runtime-uat
plan: 07
type: execute
wave: 7
depends_on: [19-06]
files_modified:
  - .planning/phases/19-skill-runtime-uat/19-UAT.md
autonomous: true
requirements:
  - UAT-01
  - UAT-02
  - UAT-03
  - UAT-04
  - UAT-05
must_haves:
  truths:
    - "19-UAT.md frontmatter `verified` field set to ISO 8601 timestamp of finalization (this is when ALL 5 local-dist UATs + all 5 tarball-runtime UAT replays reached terminal state)"
    - "19-UAT.md frontmatter `status` field finalized to one of: passed | partial | diagnosed | blocked. Computed by the rule below."
    - "If any local-dist UAT row OR any tarball-runtime UAT replay row reads `diagnosed` → status = `diagnosed` (a fix plan needs authoring under Phase 19 scope per D-18 before milestone advances)"
    - "If all 5 local-dist UATs read SATISFIED AND all 5 tarball-runtime UAT replays read SATISFIED → status = `passed`"
    - "Gaps Summary section populated with: known-readings (L-11 looksLikeProject filter), 999.7 backlog (Phase 17 trailing-edge defect — out of Phase 19 scope), Correction 1 reframe (cleanup-scan emits only ScanMatch records), and any UAT-discovered defects from either local-dist or tarball-runtime evidence"
  artifacts:
    - path: ".planning/phases/19-skill-runtime-uat/19-UAT.md"
      provides: "Phase 19 final UAT index — frontmatter finalized, Gaps Summary populated, ready for gsd-verifier cross-check augmentation per D-09"
      contains: "verified: 2026-"
  key_links:
    - from: "19-UAT.md frontmatter"
      to: "/gsd-progress + /gsd-verify-work routing logic"
      via: "frontmatter.status drives next-workflow-step routing per CONTEXT § code_context"
      pattern: "status field is one of {passed, partial, diagnosed, blocked}; verifier cross-check augments after"
---

<objective>
Finalize 19-UAT.md frontmatter — set `verified` to a real timestamp, set `status` based on the rollup of plans 19-01..19-06, populate the Gaps Summary section. This is a small consolidation plan that runs AFTER all evidence is captured; it does not run any UAT itself.

After this plan, `gsd-verifier` augments 19-UAT.md with a `## Verifier Cross-Check` section per D-09 / `feedback_plan_authored_verification.md`. The verifier MUST NOT overwrite plan-authored content; it appends.

Output: 19-UAT.md with finalized frontmatter + populated Gaps Summary, ready for verifier augmentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-skill-runtime-uat/19-CONTEXT.md
@.planning/phases/19-skill-runtime-uat/19-UAT.md
@.planning/phases/19-skill-runtime-uat/19-01-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-02-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-03-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-04-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-05-SUMMARY.md
@.planning/phases/19-skill-runtime-uat/19-06-SUMMARY.md

<interfaces>
Phase status decision rule (rolls up local-dist UAT rows + tarball-runtime replay rows):
- If any local-dist UAT row Status = `DIAGNOSED` OR any tarball-runtime UAT replay row Status = `diagnosed` → phase status = `diagnosed` (fix plan needed per D-17/D-18)
- If any row Status = `PARTIAL` → phase status = `partial`
- If any row Status = `BLOCKED` → phase status = `blocked`
- If ALL 5 local-dist UAT rows = SATISFIED AND ALL 5 tarball-runtime UAT replay rows = SATISFIED → phase status = `passed`

Frontmatter `verified` field: ISO 8601 timestamp of the moment 19-07 runs (e.g., `2026-04-29T12:34:00Z`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roll up per-UAT outcomes (local-dist + tarball-runtime) + finalize 19-UAT.md frontmatter status + verified timestamp</name>
  <read_first>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (current state — all 5 SC rows VERIFIED with local/tarball evidence split, Tarball-Runtime Replay section present, status TBD)
    - .planning/phases/19-skill-runtime-uat/19-01-SUMMARY.md through 19-06-SUMMARY.md (per-plan outcomes — were any defects diagnosed?)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (UPDATE frontmatter: verified + status fields)
  </files>
  <action>
    1. Read each per-plan SUMMARY (19-01..19-06) and the current 19-UAT.md state. Determine the rollup status:
       - Are all 5 Observable Truths rows VERIFIED (local-dist evidence)? (yes/no)
       - Are all 5 Requirements Coverage rows SATISFIED? (yes/no)
       - Are all 5 tarball-runtime UAT replay rows in the Tarball-Runtime Replay section SATISFIED? (yes/no)
       - Were any UAT-discovered defects flagged as `diagnosed` in any 19-NN-SUMMARY (local-dist or tarball runtime)? (yes/no)

    2. Apply the decision rule:
       - If any was DIAGNOSED (local-dist or tarball-runtime) → status = `diagnosed`
       - If any was PARTIAL → status = `partial`
       - If any was BLOCKED → status = `blocked`
       - If all 5 local-dist SATISFIED + all 5 tarball-runtime SATISFIED + no diagnosed defects → status = `passed`

    3. Use Edit tool on 19-UAT.md to update frontmatter:
       - Set `verified:` to the current ISO 8601 timestamp (use `date -u +"%Y-%m-%dT%H:%M:%SZ"` for the value; PowerShell equivalent: `Get-Date -AsUTC -Format "yyyy-MM-ddTHH:mm:ssZ"`)
       - Set `status:` to the computed value (passed | partial | diagnosed | blocked)

    4. If status is `diagnosed`: add a top-of-document note before the Observable Truths table:
       ```markdown
       > **Status: DIAGNOSED.** UAT-NN identified a defect in <skill> (<local-dist | tarball-runtime>). See <transcript section> for evidence. A fix plan (19-NN-PLAN.md) closes this defect under Phase 19 scope per D-17/D-18, after which the affected UAT re-runs to confirm `passed`.
       ```

    5. Save the file.
  </action>
  <verify>
    <automated>grep -E "^status:\s*(passed|partial|diagnosed|blocked)$" ".planning/phases/19-skill-runtime-uat/19-UAT.md" | wc -l | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - 19-UAT.md frontmatter `verified` field contains an ISO 8601 timestamp (NOT `TBD`)
    - 19-UAT.md frontmatter `status` field is one of: `passed` | `partial` | `diagnosed` | `blocked` (NOT `TBD`)
    - The status value matches the decision rule applied to BOTH local-dist UAT outcomes AND tarball-runtime UAT replay outcomes
    - If status is `diagnosed`: top-of-document note present pointing to the defect transcript section + indicates whether the defect is local-dist or tarball-runtime
  </acceptance_criteria>
  <done>Frontmatter finalized. The status drives the next workflow step: `passed` → milestone advances to Phase 20 close; `diagnosed` → fix plan authored under Phase 19 scope per D-17/D-18.</done>
</task>

<task type="auto">
  <name>Task 2: Populate Gaps Summary section + final consistency check</name>
  <read_first>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (Gaps Summary section currently placeholder from 19-01)
    - .planning/phases/19-skill-runtime-uat/19-CONTEXT.md § "Deferred Ideas" (D-codes scoped out of Phase 19)
    - .planning/PROJECT.md (999.7 backlog reference)
    - Per-plan SUMMARYs for any UAT-discovered findings (local-dist or tarball-runtime)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (UPDATE Gaps Summary section)
  </files>
  <action>
    1. Edit 19-UAT.md to populate the `### Gaps Summary` section. Replace the placeholder with:
       ```markdown
       ### Gaps Summary

       Phase 19 is observational UAT. Per CONTEXT D-17, no skill behavior changes were introduced. The following items are deliberately outside Phase 19's scope:

       **Deferred to v3.1.0** (per CONTEXT D-02, D-03):
       - Crash-resume mid-loop test (D-02) — race-prone in manual UAT; belongs in deterministic Vitest against `chunk()` + `copy()`
       - Multi-project migration batching (D-03) — fixture overhead disproportionate; ROADMAP SC #2 specifies single-project handoff

       **Deferred to Phase 20** (per CONTEXT D-06):
       - `npx -y @localground/cli@3.0.1 detect` smoke against published tarball (Phase 20 SC #5)
       - First green CI run on master (PIPE-01 / Phase 20 SC #1)
       - First OIDC + provenance publish (PIPE-02 / Phase 20 SC #2-3)
       - npmjs.com per-package README rendering (DOC-03 / Phase 20 SC #4)

       **Known limitations** (acknowledged, not gating):
       - 999.7 trailing-edge buildCandidates defect (Phase 17 backlog) — `encode("Trailing<X>")` strips trailing hyphen; CORE-13 char at trailing edge of intermediate component fails decode. Active-environment impact: zero per the 2026-04-27 23-path-hash diagnostic. Fix deferred to v3.1.0.
       - L-11 known reading: `localground_audit` filters via `looksLikeProject` (packages/mcp/src/index.ts:712-718) — filesystem-root + home-dir entries excluded. Correct behavior, not a defect.
       - Correction 1 reframe: `localground_cleanup_scan` emits only ScanMatch records (file references), NOT directory candidates. UAT-04 fixture aligned with actual code path; the cleanup skill's directory-candidate prose (localground-cleanup.md:38-46) is forward-looking spec, not currently exercised by the scan tool. Future enhancement (not Phase 19 scope per D-17).
       - UAT-02 tarball-runtime replay covers Run 1 (happy path) only; Runs 2 (idempotency) and 3 (missing-state-file fallback) proved out under local-dist (plan 19-02) and depend on skill logic (state-file branch detection in localground-migrate.md:17-21), NOT on the runtime path. Local-dist proof transitively covers tarball runtime since the skill loads identically.

       **Post-UAT housekeeping** (separate operation, NOT folded into Phase 19 per D-11/D-13):
       - Cleanup of 3 diagnosed debug entries on maintainer's machine (D-11)
       - Removal of Phase 14 fixture artifacts (OneDrive QMS folder + 2535-file local target) (D-13)
       - Removal of local-dist UAT chain artifacts (`lg-uat-fixture-19/` source + `lg-uat-19-dest/` destination) post-Phase-19
       - Removal of tarball-runtime UAT chain artifacts (`lg-uat-fixture-19-tarball/` source + `lg-uat-19-dest-tarball/` destination) post-Phase-19
       - These get cleaned via a deliberate post-UAT operation, NOT during UAT — to preserve the safety-test invariant ("the test that validates we don't accidentally delete stuff must not itself become an unsafe event")

       **UAT-discovered findings** (if any):
       <If any UAT-NN (local-dist or tarball-runtime) closed as `diagnosed`, list the defect summary + transcript reference + fix plan reference here. Distinguish local-dist defects from tarball-runtime defects.
       If all 10 evidence rows (5 local-dist + 5 tarball-runtime) closed as SATISFIED, write: "None — all 5 UATs (UAT-01 through UAT-05) closed under both local-dist runtime AND tarball-runtime replay (D-04 gating pass) without findings. Phase 19 advances milestone to Phase 20.">
       ```

    2. Final consistency check on the entire 19-UAT.md file. Use Read tool to load the file in full and verify:
       - Frontmatter has all fields per the Phase 18 5-field shape (phase, verified, status, score, overrides_applied, requirements_verified)
       - Observable Truths table has exactly 5 rows mapping SC1..SC5; each evidence cell uses the `local: ... / tarball: ...` split format
       - Each SC row's evidence cell references at least one local-dist transcript AND one tarball-runtime transcript in `19-transcripts/`
       - Required Artifacts table lists ALL transcript files: 8 local-dist (`seed.md`, `migrate-session-1.md`, `migrate-session-2.md`, `migrate-idempotency.md`, `migrate-missing-state.md`, `reap.md`, `cleanup.md`, `verify.md`) + 6 tarball-runtime (`tarball-seed.md`, `tarball-migrate-session-1.md`, `tarball-migrate-session-2.md`, `tarball-reap.md`, `tarball-cleanup.md`, `tarball-verify.md`) = 14 transcript rows minimum
       - Behavioral Spot-Checks table has rollup rows from all plans (19-01 through 19-06), including the 9 tarball-runtime lifecycle rows from plan 19-06 Task 4
       - Requirements Coverage table has 5 rows (UAT-01..UAT-05) all SATISFIED; source plans listing both per-UAT plan AND 19-06
       - Tarball-Runtime Replay section present with per-UAT replay table (5 rows: UAT-01..UAT-05) — NOT a "routing-handshake" table
       - Gaps Summary populated (NOT placeholder)
       - NO `## Verifier Cross-Check` section (gsd-verifier territory only — comes after this plan finishes)

    3. If any inconsistency found, fix with Edit. If everything is clean, save no changes (the file is final).
  </action>
  <verify>
    <automated>grep -c "### Gaps Summary" ".planning/phases/19-skill-runtime-uat/19-UAT.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - Gaps Summary section is NOT the original placeholder text — it's populated with the 5 categories above (Deferred v3.1.0 / Deferred Phase 20 / Known limitations / Post-UAT housekeeping / UAT-discovered findings)
    - Gaps Summary references 999.7 (Phase 17 backlog), L-11, Correction 1 reframe, and UAT-02 Run 2/3 tarball-replay scoping rationale
    - Gaps Summary "UAT-discovered findings" subsection either lists defects with transcript+fix-plan refs (distinguishing local-dist from tarball-runtime) OR explicitly says "None"
    - Final consistency check confirms all required sections present and no Verifier Cross-Check section
    - 19-UAT.md does not contain any `TBD` strings in frontmatter
    - Required Artifacts table has at minimum 14 transcript rows (8 local-dist + 6 tarball-runtime)
  </acceptance_criteria>
  <done>Phase 19 evidence index complete. 19-UAT.md is the durable contract `/gsd-progress` reads. Per D-09, the next step is gsd-verifier augmenting with `## Verifier Cross-Check` section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan-authored 19-UAT.md → gsd-verifier augmentation | D-09 / `feedback_plan_authored_verification.md` invariant — verifier appends, never overwrites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-17 | Tampering | gsd-verifier overwrites plan-authored 19-UAT.md (D-09 violation) | mitigate | Stored memory `feedback_plan_authored_verification.md` documents the invariant; orchestrator must spawn gsd-verifier with explicit "augment, don't overwrite" instructions. NOT in 19-07's scope to enforce — flagged for the orchestrator's verifier-spawn step. |
</threat_model>

<verification>
After Task 2, 19-UAT.md is the final plan-authored phase artifact. The orchestrator next spawns gsd-verifier with the augment-not-overwrite instruction (per D-09 / stored memory) to add the cross-check section. Phase 19 closure follows the same pattern Phase 17 used (verifier cross-check augmenting plan-authored verification).
</verification>

<success_criteria>
- 19-UAT.md frontmatter `verified` and `status` fields finalized (no TBD strings)
- Status decision rule correctly applied based on per-plan outcomes (local-dist + tarball-runtime)
- Gaps Summary populated with 5 categories + UAT-discovered findings or "None"
- Final consistency check passes — all sections present, no Verifier Cross-Check section
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-runtime-uat/19-07-SUMMARY.md` per the template. This is the last plan in Phase 19; the orchestrator's next step is verifier cross-check augmentation per D-09.
</output>
