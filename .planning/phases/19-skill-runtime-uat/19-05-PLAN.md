---
phase: 19-skill-runtime-uat
plan: 05
type: execute
wave: 5
depends_on: [19-04]
files_modified:
  - .planning/phases/19-skill-runtime-uat/19-UAT.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/verify.md
autonomous: false
requirements:
  - UAT-05
must_haves:
  truths:
    - "Per UAT-05 / SC5: /localground:verify invokes localground_audit, produces traffic-light report whose recommendations map to actionable next steps"
    - "Per D-15/D-16: UAT-05 runs LAST as final clean-state environment audit (post-chain). Auto-discovery via looksLikeProject filter (L-11) — root paths and home dir excluded; this is correct behavior, not a defect"
    - "19-UAT.md row 5 (UAT-05 → SC5) status updated to VERIFIED with evidence pointer to verify.md transcript"
  artifacts:
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/verify.md"
      provides: "UAT-05 transcript — section-anchored capture of /localground:verify environment audit"
      contains: "## Tool call: localground_audit"
  key_links:
    - from: "/localground:verify skill"
      to: "localground_audit MCP tool"
      via: "auto-discovery via .filter(looksLikeProject) per packages/mcp/src/index.ts:712-718; explicit projectPaths bypass filter"
      pattern: "transcript shows summary {projectsAudited, pass, warn, fail, overallStatus} + per-project checks array"
---

<objective>
Execute UAT-05 — `/localground:verify` end-to-end as the final environment-wide audit. Per D-15/D-16, this is the last UAT in the chain; it gives a clean-state snapshot of the maintainer's Claude Code project ecosystem after the seed → migrate → reap → cleanup chain ran.

Per L-11: `localground_audit`'s auto-discovery uses `.filter(looksLikeProject)`, so root paths and home directory entries are correctly excluded. Don't mis-diagnose missing paths as defects.

Output: `19-transcripts/verify.md` with section anchors; updated 19-UAT.md row 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-skill-runtime-uat/19-CONTEXT.md
@.planning/phases/19-skill-runtime-uat/19-RESEARCH.md
@.planning/phases/19-skill-runtime-uat/19-UAT.md
@.claude/skills/localground-verify.md

<interfaces>
From `.claude/skills/localground-verify.md`:
- Step 1: calls `localground_audit` with no arguments (auto-discovers all projects); optional `projectPaths` for explicit list
- Step 2: presents traffic-light table per project (PASS/WARN/FAIL per check) + overall summary
- Step 3: provides recommendations — for each WARN/FAIL, explains meaning + next step (run /localground:seed + /localground:migrate for cloud-synced; /localground:cleanup for stale refs; etc.)

From `packages/mcp/src/index.ts:712-718` (verified per L-11):
- `autoDiscovered = ...filter(looksLikeProject)` — filesystem root + home dir entries excluded
- Explicit `projectPaths` bypass the filter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /localground:verify environment-wide + capture transcript</name>
  <read_first>
    - .claude/skills/localground-verify.md (full skill spec)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "L-11" (looksLikeProject filter — known reading, not a defect)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-transcripts/verify.md (NEW)
  </files>
  <action>
    1. Open Claude Code (any cwd; MCP server is `--scope user`). Type the following PROMPT verbatim:
       ```
       Run /localground:verify to audit my entire Claude Code environment.
       ```
       (No arguments — auto-discovery mode. Per L-11, the audit will skip filesystem-root and home-dir entries via `looksLikeProject`.)

       Observe:
       - Skill calls `localground_audit` with no `projectPaths` arg (auto-discovery)
       - Response contains `summary: {projectsAudited, pass, warn, fail, overallStatus}` + `projects: [{projectPath, checks: [...]}]`
       - Skill renders the traffic-light table per project
       - Skill provides natural-language recommendations for any WARN or FAIL findings

    2. Capture transcript at `.planning/phases/19-skill-runtime-uat/19-transcripts/verify.md` with these section anchors:
       ```markdown
       # UAT-05 Transcript: /localground:verify

       **Captured:** <ISO 8601 date>
       **Mode:** auto-discovery (no projectPaths argument)
       **MCP runtime:** local-dist (D-04 inner loop)

       ## Skill invocation prompt
       <paste the prompt the maintainer typed>

       ## Tool call: localground_audit
       <paste tool call envelope + response — preserve summary + projects array verbatim>

       ## Skill output to user — traffic-light table per project
       <paste the natural-language tables, one per project>

       ## Skill output to user — overall summary
       <paste the OverallStatus + per-status counts>

       ## Skill recommendations
       <paste the natural-language interpretation: each WARN/FAIL mapped to actionable next step (e.g., "run /localground:seed + /localground:migrate" for cloud-synced finding, "run /localground:cleanup" for stale refs, "no action needed" for all-PASS)>

       ## Auto-discovery filter check (L-11 known reading)
       <note any expected paths NOT in the audit — e.g., filesystem roots, home dir, paths excluded by looksLikeProject. This is correct behavior, not a defect.>
       ```

    3. Verify SC5 acceptance — the recommendations MUST map to actionable next steps. Confirm:
       - Each WARN/FAIL finding (if any) has a corresponding recommendation in the `## Skill recommendations` section
       - Recommendations explicitly name next steps (skill commands, settings to update, etc.) — not just descriptive prose

       If overall status is GREEN with no findings, that's also acceptable — the recommendation reads "no action needed" per the skill's contract. Capture whatever the actual outcome is.
  </action>
  <verify>
    <automated>test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/verify.md" && grep -c "## Tool call: localground_audit" ".planning/phases/19-skill-runtime-uat/19-transcripts/verify.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-skill-runtime-uat/19-transcripts/verify.md` exists
    - verify.md contains `## Tool call: localground_audit`, `## Skill output to user — traffic-light table per project`, `## Skill output to user — overall summary`, `## Skill recommendations` (grep count >= 4 distinct anchors)
    - verify.md `## Tool call: localground_audit` section captures the response with `summary` + `projects` keys
    - verify.md `## Skill recommendations` section either reads "no action needed" (GREEN status) OR maps each WARN/FAIL finding to a named next step
    - verify.md includes the `## Auto-discovery filter check (L-11 known reading)` section so the verifier can confirm the maintainer is aware of the filter behavior (not a defect)
  </acceptance_criteria>
  <done>UAT-05 executed end-to-end. Auto-discovery audit produced traffic-light report. Recommendations mapped to actionable next steps (or GREEN clean-state). SC5 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Update 19-UAT.md row 5 (UAT-05 → SC5) with VERIFIED status</name>
  <read_first>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (current state — row 5 status PENDING)
    - .planning/phases/19-skill-runtime-uat/19-transcripts/verify.md (the evidence)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (UPDATE row 5 + frontmatter score)
  </files>
  <action>
    1. Use Edit tool on 19-UAT.md (preserve everything else):

    2. Update frontmatter:
       - Change `score: 4/5 truths verified` → `score: 5/5 truths verified`
       - Change `requirements_verified: 4/5` → `requirements_verified: 5/5`

    3. Update Observable Truths table row 5 (SC5):
       ```markdown
       | SC5 | <SC5 verbatim from ROADMAP> | VERIFIED   | `19-transcripts/verify.md § Tool call: localground_audit` (summary + projects array); `§ Skill output to user — traffic-light table per project`; `§ Skill recommendations` (each WARN/FAIL mapped to next step, OR "no action needed" for GREEN). L-11 known-reading documented in `§ Auto-discovery filter check`. |
       ```

    4. Update Score below table: `**Score:** 5/5 truths verified`.

    5. Append to Required Artifacts table:
       ```markdown
       | `19-transcripts/verify.md`                                                | UAT-05 transcript — audit auto-discovery + per-project traffic-light + recommendations           | VERIFIED   | Authored by plan 19-05 Task 1                                                                 |
       ```

    6. Append to Behavioral Spot-Checks table:
       ```markdown
       | localground_audit auto-discovery returns summary+projects | verify.md § Tool call: localground_audit response                       | summary keys present + projects array              | PASS     |
       | Recommendations map to actionable next steps (SC5)        | verify.md § Skill recommendations                                        | each WARN/FAIL has named next step OR GREEN no-op  | PASS     |
       | L-11 known-reading documented (looksLikeProject filter)   | verify.md § Auto-discovery filter check                                  | section present and acknowledges filter behavior   | PASS     |
       ```

    7. Update Requirements Coverage table row UAT-05:
       - Status: `SATISFIED`
       - Evidence: `19-transcripts/verify.md § Tool call: localground_audit + § Skill recommendations`

    8. Update **Coverage so far:** `5/5 requirement IDs satisfied; 0 pending.`

    9. Save the file. Note: frontmatter `status:` is still TBD until 19-07 finalizes — local-dist runtime score is now 5/5 but the tarball-gate replay (19-06) needs to land before the phase is fully `passed`.
  </action>
  <verify>
    <automated>grep -c "score: 5/5 truths verified" ".planning/phases/19-skill-runtime-uat/19-UAT.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - 19-UAT.md frontmatter contains literal `score: 5/5 truths verified`
    - 19-UAT.md frontmatter contains literal `requirements_verified: 5/5`
    - SC5 row in Observable Truths table reads `VERIFIED` (not `PENDING`)
    - SC5 row evidence cell references `19-transcripts/verify.md` and includes >= 3 section anchors
    - All 5 SC rows in Observable Truths table now read VERIFIED (no PENDING remains for local-dist runtime)
    - Required Artifacts table has new row for `19-transcripts/verify.md`
    - Requirements Coverage UAT-05 row shows `SATISFIED`
    - Frontmatter `status:` field still reads `TBD` (19-07 finalizes after 19-06 lands)
  </acceptance_criteria>
  <done>19-UAT.md row 5 updated with VERIFIED. All 5 SC rows verified for local-dist runtime. Frontmatter status pending tarball-gate replay (19-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit skill → real filesystem (environment-wide read) | read-only auto-discovery via `~/.claude/projects/` and registered project paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Information Disclosure | audit transcript captures full project paths environment-wide | accept | Per CONTEXT D-08, project paths under `C:/Users/rlasalle/...` are non-sensitive. Maintainer may redact specific project names if desired (D-08 leaves redaction at maintainer discretion). |
| T-19-13 | Tampering | audit is read-only — no write surface | accept | No mitigation needed; audit cannot modify state. |
</threat_model>

<verification>
After Task 2, all 5 Observable Truths rows VERIFIED for local-dist runtime; score 5/5. The phase status remains TBD pending the tarball-gate replay in 19-06; 19-07 finalizes the frontmatter.
</verification>

<success_criteria>
- verify.md exists with section anchors for audit tool call + traffic-light + recommendations + L-11 acknowledgment
- 19-UAT.md row 5 status VERIFIED
- All 5 SC rows now VERIFIED for local-dist runtime
- Frontmatter score: 5/5 truths verified
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-runtime-uat/19-05-SUMMARY.md` per the template.
</output>
