---
phase: 19-skill-runtime-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-skill-runtime-uat/19-UAT.md
  - .planning/phases/19-skill-runtime-uat/19-transcripts/seed.md
autonomous: false
requirements:
  - UAT-01
user_setup:
  - service: claude-cli
    why: "Register localground MCP server for runtime UAT (D-04 inner loop, --scope user per L-1)"
    dashboard_config:
      - task: "Verify claude mcp CLI is available"
        location: "Terminal — `claude mcp --help` should succeed"
must_haves:
  truths:
    - "Per D-04 (inner loop): localground MCP server registered with --scope user against packages/mcp/dist/index.js, visible from any cwd via `claude mcp list`"
    - "Per UAT-01 / SC1: /localground:seed produces a valid .localground-seed-manifest.json plus user-readable summary, with the underlying localground_seed MCP tool call visible in transcript"
    - "Per D-12: fresh disposable fixture exists under a real OneDrive path with git init + 2-3 commits + 10-50 files (NOT a Phase 14 reuse — D-13 keeps QMS fixture untouched)"
    - "Per D-07/D-09: 19-UAT.md plan-authored with frontmatter matching Phase 17/18 shape, row 1 (UAT-01 → SC1) populated with status + evidence pointer to 19-transcripts/seed.md"
  artifacts:
    - path: ".planning/phases/19-skill-runtime-uat/19-UAT.md"
      provides: "Plan-authored UAT index — frontmatter + 5-row Observable Truths skeleton + UAT-01 row populated"
      contains: "phase: 19-skill-runtime-uat, status:, score:, requirements_verified:, | SC1 |"
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/seed.md"
      provides: "UAT-01 transcript — section-anchored capture of /localground:seed run"
      contains: "## Tool call: localground_seed"
    - path: ".planning/phases/19-skill-runtime-uat/19-transcripts/.gitkeep"
      provides: "Transcript directory placeholder (created by mkdir; .gitkeep ensures dir survives git operations)"
  key_links:
    - from: "Claude Code skill loader"
      to: "registered localground MCP server"
      via: "claude mcp add localground --scope user -- node <abs path to packages/mcp/dist/index.js>"
      pattern: "claude mcp list shows localground entry with scope=user"
    - from: "/localground:seed skill"
      to: "localground_seed MCP tool"
      via: "skill invocation routes through MCP stdio JSON-RPC"
      pattern: "transcript contains '## Tool call: localground_seed' AND tool response 'isError': false"
    - from: "localground_seed tool response"
      to: "<fixture>/.localground-seed-manifest.json on disk"
      via: "seed.ts:147 fs.writeFile(path.join(projectPath, '.localground-seed-manifest.json'))"
      pattern: "cat <fixture>/.localground-seed-manifest.json shows {version, toolkitVersion, created, projectPath, projectName, markers}"
---

<objective>
Stand up the Phase 19 UAT runtime: register the localground MCP server with `--scope user` (per Correction 2 / L-1), create a fresh disposable git-initialized fixture under OneDrive (per D-12), execute UAT-01 (`/localground:seed`) end-to-end, capture the transcript with section anchors, and author the `19-UAT.md` index skeleton with row 1 populated.

Purpose: UAT-01 closure (`/localground:seed` runtime validation) plus the foundation every later plan in this phase depends on (registered MCP server + fresh fixture + 19-UAT.md index file).

Output: registered MCP server (visible to all subsequent plans), fresh fixture path the maintainer notes for 19-02, `19-transcripts/seed.md`, `19-UAT.md` with frontmatter + 5-row skeleton + UAT-01 row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-skill-runtime-uat/19-CONTEXT.md
@.planning/phases/19-skill-runtime-uat/19-RESEARCH.md
@.planning/phases/17-core-decoder-calibration/17-VERIFICATION.md
@.planning/phases/18-packaging-polish/18-VERIFICATION.md
@.claude/skills/SKILL.md
@.claude/skills/localground-seed.md
@packages/core/src/operations/seed.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + skill specs. -->

From `packages/core/src/operations/seed.ts`:
```typescript
const SEED_MANIFEST_FILE_NAME = '.localground-seed-manifest.json';  // line 30
const SEED_TEST_FILE_NAME = '.localground-seed-test';
// On success: writes manifest at path.join(projectPath, '.localground-seed-manifest.json') (line 147)
// Manifest schema (line 137-144):
//   { version, toolkitVersion, created, projectPath, projectName,
//     markers: [{ type: 'test-file', path, checksum }, { type: 'git-tag', tag, commitHash }] }
// Refuses re-seed if .localground-seed-test exists (line 71-81 — L-5 confirmation)
```

From `.claude/skills/localground-seed.md`:
- Tool calls in order: `localground_detect` then `localground_seed`
- Skill expects absolute `projectPath`
- Error: "project already seeded" if .localground-seed-test exists at projectPath

From `claude mcp add --help` (empirically verified per 19-RESEARCH L-1):
- `-s, --scope <scope>` defaults to `local` (per-project cwd)
- MUST use `--scope user` for cross-cwd visibility

Verification artifact frontmatter (Phase 18 — simpler shape; 19-UAT.md uses this):
```yaml
---
phase: 19-skill-runtime-uat
verified: <ISO 8601 timestamp — leave empty until UAT runs>
status: passed | partial | diagnosed | blocked
score: N/5 truths verified
overrides_applied: 0
requirements_verified: N/5
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build packages/mcp/dist and register localground MCP server with --scope user</name>
  <read_first>
    - .claude/skills/SKILL.md (registration prerequisite text)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "L-1", § "L-2", § "L-3" (scope default trap; non-idempotent add; skill-loading order)
    - .planning/phases/19-skill-runtime-uat/19-CONTEXT.md § "Runtime target" (D-04, D-05)
  </read_first>
  <files>
    - packages/mcp/dist/index.js (artifact-only output of `npm run build`; do NOT edit source)
  </files>
  <action>
    1. From the repo root (`C:/Users/rlasalle/Projects/localground`), build the MCP package:
       ```bash
       npm run build -w @localground/mcp
       ```
       This produces `packages/mcp/dist/index.js` (and `.d.ts`, `.js.map`) via tsup. Confirm `packages/mcp/dist/index.js` exists and is non-empty before proceeding.

    2. Verify the dist build can short-circuit on `--version` without booting transport (Phase 18 SC4 baseline; provides ground truth that the binary is loadable):
       ```bash
       node "C:/Users/rlasalle/Projects/localground/packages/mcp/dist/index.js" --version
       ```
       Expected: stdout prints a semver (`3.0.0`), exit 0, no "running on stdio" stderr message. If either fails, STOP — do not proceed; report to user as a Phase 18 regression.

    3. Register the MCP server with `--scope user` (per Correction 2 / L-1). Use the `remove`-then-`add` swap pattern (per L-2 — `claude mcp add` is not idempotent). Run this EXACT command (forward slashes inside double quotes survive PowerShell, cmd, and Git Bash per L-1):
       ```bash
       claude mcp remove localground 2>/dev/null; claude mcp add localground --scope user -- node "C:/Users/rlasalle/Projects/localground/packages/mcp/dist/index.js"
       ```
       The `2>/dev/null` swallows the "not found" error on first registration; the `;` (not `&&`) ensures `add` runs even if `remove` fails.

    4. Verify registration succeeded — `claude mcp list` should show `localground` with scope `user`:
       ```bash
       claude mcp list
       ```
       Expected: output contains a line for `localground` with the configured node command. If localground is missing OR scope shows `local`, STOP — re-run step 3.

    5. Verify cross-cwd visibility (the load-bearing property — L-1):
       ```bash
       cd "C:/Users/rlasalle"   # any cwd outside the repo
       claude mcp list
       cd "C:/Users/rlasalle/Projects/localground"   # return
       ```
       Expected: `localground` appears in BOTH listings. If it disappears from cwd outside the repo, scope is wrong — STOP and fix.
  </action>
  <verify>
    <automated>node "C:/Users/rlasalle/Projects/localground/packages/mcp/dist/index.js" --version</automated>
  </verify>
  <acceptance_criteria>
    - `packages/mcp/dist/index.js` exists and `node packages/mcp/dist/index.js --version` exits 0 with semver stdout
    - `claude mcp list` shows `localground` with scope=`user` (NOT `local`)
    - `claude mcp list` shows `localground` from at least one cwd outside the repo (cross-cwd visibility — L-1 acceptance)
    - The exact registration command used (with `--scope user` and the absolute forward-slash-quoted path) is recorded for re-use in 19-06 (tarball-install replay) — note it in scratch or memory; the exact form is the canonical D-04 inner-loop registration.
  </acceptance_criteria>
  <done>localground MCP server registered with --scope user, visible from any cwd, dist binary boots clean on --version. Subsequent plans (19-02 .. 19-05) can invoke /localground:* skills without re-registering.</done>
</task>

<task type="auto">
  <name>Task 2: Create fresh disposable fixture under OneDrive (per D-12) and run /localground:seed end-to-end (UAT-01)</name>
  <read_first>
    - .claude/skills/localground-seed.md (full skill spec — what the maintainer should see during execution)
    - .planning/phases/19-skill-runtime-uat/19-CONTEXT.md § "Test fixture strategy" (D-12, D-13)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § "L-5", § "L-6" (refuse-on-existing-test-file; manifest schema)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-transcripts/seed.md (NEW — UAT-01 transcript)
  </files>
  <action>
    **Required shell: Git Bash (MINGW64)** — uses bash-only constructs (`$(...)` command substitution, `2>/dev/null`/`2>&1` redirection, `&&`/`||` chaining, forward-slash paths inside double-quoted strings). PowerShell users should switch to Git Bash for this task. Equivalent PowerShell commands are not provided because evidence integrity here is exit-status + file-existence checks, not shell-specific output formatting.

    1. Create the fresh disposable fixture under OneDrive (real cloud-synced path per D-12 — UAT must exercise the cloud → local path; tmpdir doesn't reproduce that). Suggested location:
       ```
       C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19/
       ```
       Build it with 2-3 commits and 10-50 files. From a Git Bash or PowerShell terminal (NOT Claude Code yet — this is shell setup):
       ```bash
       mkdir -p "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19"
       cd "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19"
       git init
       # Create 10-50 small files — text content fine, e.g., a README and a few subdirectories with placeholder files
       echo "# UAT-19 fixture" > README.md
       mkdir -p src docs
       for i in $(seq 1 12); do echo "file $i" > "src/file-$i.txt"; done
       for i in $(seq 1 5); do echo "doc $i" > "docs/doc-$i.md"; done
       git add .
       git commit -m "initial commit"
       echo "second" >> README.md
       git add README.md
       git commit -m "second commit"
       echo "third" >> README.md
       git add README.md
       git commit -m "third commit"
       ```
       Confirm: `ls .localground-seed-test 2>/dev/null` returns nothing (otherwise L-5 will refuse to seed).

    2. Note the absolute fixture path. You will pass this to the skill in step 3 and reuse it in 19-02 as the migrate source.

    3. Open Claude Code from any cwd (the MCP server is `--scope user`, so any cwd works). Type the following PROMPT to Claude Code verbatim:
       ```
       Run /localground:seed against the project at "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19".
       ```
       (Adjust the path if you used a different fixture location.) Observe the dialogue:
       - The skill calls `localground_detect` first, summarizes environment
       - Asks/confirms `projectPath` (D-12 fixture absolute path)
       - Calls `localground_seed` with the confirmed projectPath
       - Reports the manifest summary (test file path + checksum, git tag + commit hash, manifest file path)

    4. Capture the transcript by copy-pasting the relevant Claude Code dialogue into a new file at:
       ```
       C:/Users/rlasalle/Projects/localground/.planning/phases/19-skill-runtime-uat/19-transcripts/seed.md
       ```
       Use these EXACT section headings (the section-heading anchor pattern per Claude's Discretion bullet 3, matching grep-cell evidence style of Phase 17/18):
       ```markdown
       # UAT-01 Transcript: /localground:seed

       **Captured:** <ISO 8601 date>
       **Fixture:** <absolute path to fixture>
       **MCP runtime:** local-dist (D-04 inner loop) — packages/mcp/dist/index.js, --scope user

       ## Skill invocation prompt
       <paste the prompt the maintainer typed>

       ## Tool call: localground_detect
       <paste the tool call envelope + response — preserve isError boolean and content array>

       ## Tool call: localground_seed
       <paste the tool call envelope + response — preserve isError boolean and content array>

       ## Skill output to user
       <paste the natural-language summary the skill rendered>

       ## On-disk evidence (post-run)
       <paste output of: cat "<fixture>/.localground-seed-manifest.json">
       ```

    5. After the skill run, capture L-6 evidence — the on-disk manifest:
       ```bash
       cat "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19/.localground-seed-manifest.json"
       ```
       Paste the JSON output verbatim into the `## On-disk evidence (post-run)` section. Verify it contains the keys `version`, `toolkitVersion`, `created`, `projectPath`, `projectName`, `markers` per L-6.

    6. Verify the git tag was created:
       ```bash
       cd "C:/Users/rlasalle/OneDrive - ThermoTek, Inc/Documents/lg-uat-fixture-19"
       git tag --list 'lg-seed-*'
       ```
       Expected: at least one tag matching `lg-seed-*`. Add this output to the `## On-disk evidence (post-run)` section.
  </action>
  <verify>
    <automated>test -f ".planning/phases/19-skill-runtime-uat/19-transcripts/seed.md" && grep -c "## Tool call: localground_seed" ".planning/phases/19-skill-runtime-uat/19-transcripts/seed.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - Fresh fixture exists at the chosen OneDrive path with `git log --oneline | wc -l` >= 3 commits and at least 10 files (other than `.git/`)
    - File `.planning/phases/19-skill-runtime-uat/19-transcripts/seed.md` exists
    - `seed.md` contains the literal section heading `## Tool call: localground_seed` (grep returns >=1 match)
    - `seed.md` contains the literal section heading `## Tool call: localground_detect`
    - `seed.md` contains the literal section heading `## On-disk evidence (post-run)` followed by JSON containing all 6 keys: `version`, `toolkitVersion`, `created`, `projectPath`, `projectName`, `markers`
    - `<fixture>/.localground-seed-manifest.json` exists on disk and parses as valid JSON
    - `<fixture>/.localground-seed-test` exists on disk
    - At least one git tag matching `lg-seed-*` exists on the fixture
  </acceptance_criteria>
  <done>UAT-01 executed end-to-end against the registered local-dist MCP server. Transcript captured with section anchors. On-disk manifest evidence confirms SC1 ("valid .localground-seed-manifest.json plus user-readable summary, with the underlying localground_seed MCP tool call visible in the transcript").</done>
</task>

<task type="auto">
  <name>Task 3: Author 19-UAT.md plan-authored index skeleton + populate UAT-01 row</name>
  <read_first>
    - .planning/phases/17-core-decoder-calibration/17-VERIFICATION.md (frontmatter + Observable Truths table shape — copy verbatim)
    - .planning/phases/18-packaging-polish/18-VERIFICATION.md (alternate simpler frontmatter shape — use this one)
    - .planning/phases/19-skill-runtime-uat/19-RESEARCH.md § 3 "Verification Artifact Field Set" (lifted-verbatim shapes)
    - .planning/ROADMAP.md (Phase 19 success criteria SC1-5 — copy verbatim)
  </read_first>
  <files>
    - .planning/phases/19-skill-runtime-uat/19-UAT.md (NEW — plan-authored index, augmented later by gsd-verifier per D-09)
  </files>
  <action>
    1. Create `.planning/phases/19-skill-runtime-uat/19-UAT.md` with this EXACT frontmatter (Phase 18 simpler 5-field shape per 19-RESEARCH § 3 — gsd-verifier will append `cross_checked` / `cross_check_status` later per D-09):
       ```yaml
       ---
       phase: 19-skill-runtime-uat
       verified: <leave as TBD until 19-07 finalizes>
       status: <leave as TBD until 19-07 finalizes>
       score: 1/5 truths verified
       overrides_applied: 0
       requirements_verified: 1/5
       ---
       ```

    2. Add the standard `# Phase 19: Skill Runtime UAT — Evidence Index` heading and a 1-paragraph phase-goal summary lifted verbatim from ROADMAP.md Phase 19 goal text.

    3. Add the Observable Truths table with all 5 rows (only row 1 / SC1 populated; rows 2-5 placeholder text "PENDING — see plan 19-NN-PLAN.md"). Use the Phase 18 plain-text status style ("VERIFIED" — no checkmark; per 19-RESEARCH § 3 recommendation):
       ```markdown
       ### Observable Truths (Roadmap Success Criteria)

       | #   | Truth                                                                                                                | Status     | Evidence                                                                                                                |
       | --- | -------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------- |
       | SC1 | <SC1 verbatim from ROADMAP.md Phase 19 — about /localground:seed manifest + tool call visible>                       | VERIFIED   | `19-transcripts/seed.md § Tool call: localground_seed` and `§ On-disk evidence (post-run)` (manifest JSON shape)         |
       | SC2 | <SC2 verbatim — about /localground:migrate two-session loop>                                                         | PENDING    | See plan 19-02-PLAN.md                                                                                                  |
       | SC3 | <SC3 verbatim — about /localground:reap natural-language report>                                                     | PENDING    | See plan 19-03-PLAN.md                                                                                                  |
       | SC4 | <SC4 verbatim — about /localground:cleanup per-item confirmation>                                                    | PENDING    | See plan 19-04-PLAN.md                                                                                                  |
       | SC5 | <SC5 verbatim — about /localground:verify traffic-light report>                                                      | PENDING    | See plan 19-05-PLAN.md                                                                                                  |

       **Score:** 1/5 truths verified
       ```

       Lift each SC truth verbatim from ROADMAP.md Phase 19 success criteria (lines 73-77).

    4. Add the `### Required Artifacts` section with rows for the artifacts produced so far (more rows added by later plans):
       ```markdown
       ### Required Artifacts

       | Artifact                                                                  | Expected                                                                                | Status     | Details                                                                                       |
       | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------- |
       | `19-transcripts/seed.md`                                                  | UAT-01 transcript with section anchors for detect + seed tool calls + on-disk evidence  | VERIFIED   | Authored by plan 19-01 Task 2                                                                 |
       | `<fixture>/.localground-seed-manifest.json`                               | Valid JSON with 6 keys (version, toolkitVersion, created, projectPath, projectName, markers) | VERIFIED   | Captured in 19-transcripts/seed.md § On-disk evidence (post-run)                              |
       | (more rows added by 19-02, 19-03, 19-04, 19-05, 19-06)                    |                                                                                         | PENDING    |                                                                                               |
       ```

    5. Add the `### Behavioral Spot-Checks` section with row 1 (the registration check) populated. Later plans append:
       ```markdown
       ### Behavioral Spot-Checks

       | Behavior                                                | Command                                                                  | Result                                              | Status   |
       | ------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------- | -------- |
       | localground MCP server registered with --scope user      | `claude mcp list`                                                        | localground present with scope=user                 | PASS     |
       | local-dist binary --version short-circuits cleanly       | `node packages/mcp/dist/index.js --version`                              | exit 0; semver to stdout; no transport boot stderr  | PASS     |
       | (more rows added by 19-02, 19-03, 19-04, 19-05, 19-06)  |                                                                          |                                                     | PENDING  |
       ```

    6. Add the `### Requirements Coverage` section with all 5 UAT-NN rows (only UAT-01 marked SATISFIED; others PENDING):
       ```markdown
       ### Requirements Coverage

       | Requirement | Source Plan(s) | Description                                                  | Status      | Evidence                                                                                          |
       | ----------- | -------------- | ------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------- |
       | UAT-01      | 19-01, 19-06   | /localground:seed runtime validation                         | SATISFIED   | 19-transcripts/seed.md § Tool call: localground_seed; manifest on-disk evidence                   |
       | UAT-02      | 19-02, 19-06   | /localground:migrate two-session loop                        | PENDING     | See 19-02-PLAN.md                                                                                 |
       | UAT-03      | 19-03, 19-06   | /localground:reap health check                               | PENDING     | See 19-03-PLAN.md                                                                                 |
       | UAT-04      | 19-04, 19-06   | /localground:cleanup per-item confirmation                   | PENDING     | See 19-04-PLAN.md                                                                                 |
       | UAT-05      | 19-05, 19-06   | /localground:verify environment audit                        | PENDING     | See 19-05-PLAN.md                                                                                 |

       **Coverage so far:** 1/5 requirement IDs satisfied; 4 pending.
       ```

    7. Add the `### Human Verification Required` section. Per 19-RESEARCH § 3 recommendation (Phase 19 inverts Phase 17/18 here — populated up front):
       ```markdown
       ### Human Verification Required

       Phase 19 is manual UAT. The maintainer (Robert LaSalle) executes each /localground:* skill in Claude Code and captures transcripts. The verifier's role is to cross-check that transcripts exist with the required section anchors and that 19-UAT.md frontmatter + tables match the recorded evidence — NOT to re-run the skills.
       ```

    8. Add the `### Gaps Summary` section as a placeholder (19-07 will populate it with any UAT-discovered defects):
       ```markdown
       ### Gaps Summary

       (Populated by 19-07 after all per-skill UATs run.)
       ```

    9. Save the file. Do NOT add a trailing `## Verifier Cross-Check` section — that section belongs to gsd-verifier and gets appended per D-09 / `feedback_plan_authored_verification.md`.
  </action>
  <verify>
    <automated>test -f ".planning/phases/19-skill-runtime-uat/19-UAT.md" && grep -c "phase: 19-skill-runtime-uat" ".planning/phases/19-skill-runtime-uat/19-UAT.md" | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-skill-runtime-uat/19-UAT.md` exists
    - Frontmatter contains `phase: 19-skill-runtime-uat` (grep returns >=1 match excluding comments)
    - Frontmatter contains `score: 1/5 truths verified` (literal — gets edited by 19-02..19-05 as they pass)
    - Frontmatter contains `requirements_verified: 1/5`
    - Body contains `### Observable Truths (Roadmap Success Criteria)` header
    - Body contains exactly 5 rows in the Observable Truths table mapping SC1..SC5
    - Row for SC1 reads `VERIFIED` and references `19-transcripts/seed.md`
    - Body contains `### Requirements Coverage` header with all 5 UAT-NN rows
    - Body does NOT contain a `## Verifier Cross-Check` section (that's gsd-verifier's territory per D-09)
  </acceptance_criteria>
  <done>19-UAT.md plan-authored skeleton in place, UAT-01 row populated. Subsequent plans (19-02..19-05) update their respective rows; 19-07 finalizes frontmatter status and aggregates score.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code skill loader → registered MCP server | stdio JSON-RPC transport; the registration step is the only privileged operation in this plan |
| MCP server → real filesystem (OneDrive fixture) | seed.ts writes manifest + test file + git tag in user-owned project dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `claude mcp add` shell command | mitigate | Use `--scope user` per L-1 (CONTEXT D-04 Correction 2). Forward-slash absolute path inside double quotes — survives PowerShell, cmd, Git Bash without backslash escaping. No `shell: true` on any spawn (project-wide CRIT-3/MOD-3). |
| T-19-02 | Information Disclosure | transcripts capture file paths under user home dir | accept | Per CONTEXT D-08, paths under `C:/Users/rlasalle/Projects/` and OneDrive fixture path are non-sensitive. No secrets, credentials, or PII captured. |
| T-19-03 | Denial of Service | seed refuses on existing test file (L-5) | accept | Documented refuse-on-existing-test-file behavior is the safety mechanism, not a vulnerability. Fresh fixture per D-12 ensures clean run. |
</threat_model>

<verification>
The 19-UAT.md frontmatter row counts (`score: N/5`, `requirements_verified: N/5`) get updated by each subsequent plan as it passes. Plan 19-07 finalizes the frontmatter `status` field once all rows resolve.

The MCP server registration is `--scope user`, so it persists across cwd changes for plans 19-02 through 19-06 without re-registration. Plan 19-06 will swap to a tarball-installed binary path; otherwise no re-registration is needed.
</verification>

<success_criteria>
- localground MCP server registered with `--scope user`, visible from any cwd via `claude mcp list`
- Fresh fixture under OneDrive with >=3 commits, >=10 files, no pre-existing `.localground-seed-test`
- `<fixture>/.localground-seed-manifest.json` exists with all 6 schema keys (L-6)
- `19-transcripts/seed.md` contains `## Tool call: localground_seed` and `## On-disk evidence (post-run)` section anchors
- `19-UAT.md` plan-authored skeleton with frontmatter (Phase 18 5-field shape) + 5-row Observable Truths table + UAT-01 row VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/19-skill-runtime-uat/19-01-SUMMARY.md` per the template — note the exact fixture path (subsequent plans need it as `sourcePath`).
</output>
